# Infrastructure Layer

External adapters implementing inner-layer ports: database, cache, auth,
messaging, scheduler, feature flags, secrets, resilience, audit, crypto, and
observability (`internal/infrastructure/`). See [SPEC.md §3](../../SPEC.md)
(architecture layers). Content is filled by E18; implementation lives in
E07–E10.
