# Verification Evidence: E03-T09

**Task:** E03-T09
**Commit:** branch `feat/E06-application` from `a23e32e70cca5e32cb6096d76f33c47b82c6f1ec` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain (`$HOME/.pixi/bin` on PATH)
**Verified By:** muse-spark
**Verified At:** 2026-09-15T06:30:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E03-T09-R01, S04 | `TestValidateSplit` (missing/other-tenant grant, frozen/closed accounts, unknown accounts) | PASS | `internal/domain/service/platform_split_test.go` |
| E03-T09-R02, S01 | `TestValidateSplit` (gross 10000/fee 290 → net 9710; zero/full fee; zero/negative gross; fee bounds; distinct accounts; charge-asset legs) | PASS | `internal/domain/service/platform_split.go` (`ValidateSplit`, `validateSplitShape`, `lookupSplitAccounts`) |
| E03-T09-R03, S02, S06 | `TestValidateSplit` (USD→EUR conversion 9200/8933/267/drift 0; 1/3 rate drift -1 posts explicitly; stale/missing/mismatched rate; MaxInt64 overflow) | PASS | `internal/domain/service/platform_split.go` (`settleSplitLegs`, `splitDrift`) |
| E03-T09-R04, S03, S05 | `TestValidateSplitRefund` (full/partial/zero-fee refund reverses three legs; over-refund; fee-policy violations; inconsistent originals; FX refund reconversion + errors) | PASS | `internal/domain/service/platform_split.go` (`ValidateSplitRefund`, `validateSplitRefundShape`, `settleSplitRefundLegs`) |
| All (no external imports) | `go test -race ./...` incl. `test/unit/domain` guard + gate-check G2 dep scan | PASS | `test/unit/domain/guard_test.go`, `./tasks/scripts/gate-check.sh G2` |
| All (traceability) | `python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes` | OK | structural checks passed |

## Commands

```text
$ gofmt -s -l internal/ test/ pkg/ cmd/        # empty (after -w on new files)
$ goimports -l -w .                             # clean
$ make lint                                     # 0 issues (exit 0)
$ go test -race ./... -count=1                  # all packages ok, zero races (exit 0)
$ go test ./internal/domain/service/ -run 'TestValidateSplit' -count=1 -v
# TestValidateSplit 26/26 subtests PASS; TestValidateSplitRefund 15/15 PASS
$ ./tasks/scripts/gate-check.sh G2              # Gate G2: PASSED, 96.5% domain coverage (exit 0)
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes
# tasks: 140, epics: 20 / --graph: 140 / --specs: 12 / --events: 32 / --codes: 27 / OK: structural checks passed
```

## Deviations and Residual Risk

- Ready packet supersedes the 2026-09-14 draft (E05-T02 blocker resolved; grant shape `map[childID]bool` reused via `ValidateTransferGrant`). No behavior deviation from the backlog boundary.
- Settlement-time realized gain/loss (provider rates via E10) stays out of scope; capture records conversion legs + drift explicitly. Documented in packet R03 scope.
- G2 gate status flip in `tasks/tracking/` left to owner review; `gate-check.sh G2` PASSED is recorded here as evidence.
