# Task Handoff: E08-T04 — COMPLETED

**Task:** E08-T04  
**From:** antigravity-agent  
**To:** unassigned  
**Branch/Worktree:** feat/E08-cache-messaging  
**Base / Head:** d5582cfec391e30c546da2636e5b0d5baa689b57 / uncommitted (per owner instruction, PR pending)  
**Updated At:** 2026-09-23T01:05:00Z

## State Summary

E08-T04 COMPLETED — outbox-driven publisher + idempotent consumer framework with DLQ + NATS edge bridge. No remaining required work.

## Work Completed

- E08-T04-R01..R06, S01..S05: publisher (jsonparser, headers, partition, envelope trace/request IDs), framework (claim→handler→ack, panic recovery, DLQ routing, correlation restore), memory/Valkey receipts (`ReceiptKV` seam), memory DLQ, fanout + tests.
- Amendment 2026-09-19 (audit): envelope-based correlation end-to-end (OTel spans stay in E15); durable inbox binding deferred to E14 (see evidence).

## Changed Files

- `internal/infrastructure/messaging/redpanda/publisher/redpanda.go` — EventPublisher relay over `redpanda.Broker`.
- `internal/infrastructure/messaging/redpanda/consumer/{framework,idempotent,dlq,redpanda_dlq}.go` — runner, receipt stores, durable DLQ sinks.
- `internal/infrastructure/messaging/nats/publisher.go` (`PublishEvent`) — contract-level live-delta entry point.
- `test/fakes/{broker,cache,stores}.go` — in-memory doubles (tests only; production never imports).
- Corresponding `*_test.go` files.

## Decisions and Assumptions

- Durable inbox stays in E07 `inbox_receipts`; Valkey receipts are hints (TTL 7d). Replay never invokes ledger commands (port-typed handlers only).

## Verification Evidence

- Unit + `TestPublishConsumeIdempotent` + live-NATS fanout pass; `make lint` 0 issues. See `tasks/evidence/E08-T04.md`.

## Remaining Work

None.

## Risks and Blockers

None.

## Resume Instructions

```bash
git status --short --branch && python3 tasks/scripts/check-tasks.py --ready
export PATH="$HOME/.pixi/bin:$PATH"
go test ./internal/infrastructure/messaging/... -count=1
```

## Audit & Review Revisions (2026-09-19, post-delivery review)

- Framework error wrapping, idempotent receipt validation/idempotent release, narrowed `ReceiptKV` seam, and table-driven test revisions recorded in `tasks/evidence/E08-T04.md` (§ Audit & Review Revisions). Full race suite + strict lint green.

## Antigravity Audit & Review Revisions (2026-09-19)

- Production code fix: in `framework.go`, handler panic recovery now invokes `f.routeFailure(ctx, msg, panicErr)` so that repeated panics exhaust `maxDelivers` and route cleanly to DLQ instead of causing infinite retries.
- Strict unit test compliance: added table-driven test case for panic-exhaustion DLQ routing; converted `framework_test.go` and `edge/fanout_test.go` to table-driven with exact error assertions. Claim released.


## Review Completion — Cline Session Handoff (2026-09-19)

Repaired the interrupted `framework_test.go` edit (stray token + orphaned duplicate block removed, table-driven form preserved); consumer package green under `-count=3` and lint 0 issues. See matching evidence section.

## Review Completion — Cline Session Handoff (2026-09-19)

Audit fixed a real inbox defect: DLQ routing now releases the receipt so replays re-run the handler (domain-events §5.2); release failures are joined; nil DLQ returns the handler error. Packet R07/R08 + S06–S08 added. See matching evidence section.

## Review Wave 3 — Design Cleanup (2026-09-19)

Typed hybrid API, config-sourced TTLs/topics, shared validation helper, fakes relocation, durable Redpanda DLQ sinks, and the nats.DeltaPublisher rename are all landed and verified; see the matching evidence section. No remaining required work for E08.

## Review Wave 4 — Nil Values, Naming, Fakes, Config (2026-09-19, muse-spark)

- **Negative caching (T01):** nil is now a legal value; `ErrValueRequired` removed so "record does not exist" can be cached; usage shown in runnable examples; integration proves the entry survives L2 and expires.
- **Naming (T03):** `DeltaPublisher` removed; `nats.Publisher.PublishEvent` is the contract-level live-delta entry point (not notifications).
- **Fakes (T03–T06):** in-memory doubles moved to `test/fakes` — the production tree contains no in-memory stores except the L1 cache.
- **Constants (T01/T03/T05):** topic/TTL/retry defaults unexported or config-mapped; dispatcher requires an explicit `RetryPolicy` (sentinel when nil, custom-policy test proves configurability).

## Review Wave 8 — Interface Callers + Test Convention Sweep (2026-09-19, muse-spark)

- Engine exposed as the sealed `Engine` interface (fx provides `*Cache`, `Engine`, and `port.Cache`); unit tests swept to the strict table-driven convention.
- Re-verified with the standard pipeline (below).

## Review Wave 5 — Production Transports (2026-09-19, muse-spark)

- franz-go `Producer` implements the `Broker` seam (sync produce, SASL/TLS, flush-on-close) with `redpanda.Module()`; NATS `Close` flushes before closing with `nats.Module()`; retry schedule fully config-owned (no Go defaults); live Redpanda round-trip integration proves the relay path.
- Re-verified with the standard pipeline (below).

## Review Wave 6 — Production Transports Finalized (2026-09-19)

Sarama producer, config-owned retry table, documented NATS lifecycle. See the matching evidence section. No remaining required work for E08.

## Review Wave 7 — franz-go Restored (2026-09-19, muse-spark)

- Producer transport back on franz-go (KIP-848 path, native ctx, kadm); same seam, same messages, same tests. Re-verified with the standard pipeline.

## Audit 2026-09-22 (pre-review handoff sweep, muse-spark)

- Audited code, tests, and SDD artifacts for drift since the last wave; fixes recorded in the packet amendments and evidence. No remaining required work for E08.

## Antigravity Review & Revision Completion (2026-09-23)

- **Production Redpanda Producer Architecture (`producer.go`)**: Initialized franz-go `kgo.Client` directly inside `NewProducer` (matching `nats.Publisher` pattern), completely eliminating lazy `dial()` and `sync.Mutex`. Eliminated shutdown SIGSEGV panic race where `Close()` nilled the client while `Publish()` was active, and eliminated client resurrection.
- **Production NATS Context Check (`nats/publisher.go`)**: Added `ctx.Err()` check to `Publish` to observe cancellation and deadlines.
- **Production Consumer Framework DLQ Fix (`framework.go`)**: Introduced `receiptReleaseError` on DLQ routing failure so `Handle` avoids attempting a second release on the same receipt, preventing duplicate release calls and error chains.
- Production fix & clean code: eliminated dead package-level helper functions in `publisher/redpanda_test.go`, inlined test fact closures directly inside test cases.
- Strict unit test compliance (all 6 table-driven rules):
  - In `producer_test.go`: converted to strict table-driven pattern with exact signature parameters, covered nil-receiver, close operations, and publish after close behavior.
  - In `nats/publisher_test.go`: removed unused test fields, tested `Publish`, `PublishEvent`, and `Close` with nil and initialized instances.
  - In `nats/subjects_test.go`: strict table-driven pattern with exact signature parameter names for `EventSubject`, `ParseEventSubject`, `BalanceSubject`, `QueueGroupFor`.
  - In `consumer/framework_test.go`: added test for DLQ release failure ensuring single release attempt.
- Fully verified: `make lint` 0 issues, `go test -race` passing, SDD structural checks OK. Claim released.

