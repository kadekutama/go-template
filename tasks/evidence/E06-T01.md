# Verification Evidence: E06-T01

**Task:** E06-T01
**Commit:** branch `feat/E06-application` from `a23e32e70cca5e32cb6096d76f33c47b82c6f1ec` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain (`$HOME/.pixi/bin` on PATH)
**Verified By:** muse-spark
**Verified At:** 2026-09-15T07:30:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E06-T01-R01 | Compile assertions (`CommandHandler`/`QueryHandler` generic contracts) + build/vet | PASS | `internal/application/command/handler.go`, `query/handler.go` |
| E06-T01-R02, S02, S03 | `TestRunIdempotent` (reserve/replay/conflict) + `TestFingerprint` (determinism, ambiguity guard) + `TestMapParts` | PASS | `internal/application/command/idempotency.go`, `idempotency_test.go` |
| E06-T01-R03, S06 | `TestToAppError` (nil/passthrough/mapped/unmapped/generic) + `TestLocalize` (en/id/unknown-locale) | PASS | `internal/application/service/error.go`, `service/error_test.go` |
| E06-T01-R04, S04 | `TestRequireAuthz` / `TestRequire` (denied + empty-identity fail closed, zero adapter calls) | PASS | `internal/application/command/authz.go`, `composition_test.go` |
| E06-T01-R05, S01, S05 | `TestCompositionPipeline` (sample composes all handler plumbing artifacts) | PASS | `internal/application/command/composition_test.go` |
| All (traceability) | `python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes` | OK | structural checks passed |

## Commands

```text
$ gofmt -s -l internal/ test/    # empty
$ goimports -local github.com/kadekutama/go-template -l -w . # clean
$ make lint                       # 0 issues
$ go vet ./internal/application/...                          # exit 0
$ go test ./internal/application/... -count=3                # pass
$ go test -race ./... -count=1                               # all packages ok, zero races
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes  # OK
```

## Deviations and Residual Risk

- None. `internal/application/.gitkeep` removed (superseded by real packages). HTTP-body fingerprint minting stays with the E11 edge; this task hashes caller-supplied canonical parts.

## Amendment (owner review 2026-09-15)

- Renamed `internal/application/middleware` → `internal/application/pipeline` (package clause, 15 command import sites, 2 test files incl. `middleware_test.go` → `pipeline_test.go`, one `port` doc comment). No signature or behavior changes: `Require`/`Fingerprint`/`MapParts`/`RunIdempotent`/`ToAppError`/`Localize` are explicit pipeline stages composed at handler call sites in data-flow §2 order, not composable `func(next) next` middleware — the old name promised a composition model the code does not implement. A uniform decorator chain was rejected: per-handler interleaving with `UnitOfWork` + domain loads differs by handler and restructuring 15+ services would risk behavior for zero gain.
- Re-verified: `gofmt` clean, `go build` + `go vet` clean, `go test ./internal/application/...` pass, `make lint` 0 issues.

## Amendment (owner review, split 2026-09-15)

- Dissolved `pipeline` into `internal/application/{idempotency,authz,translation}` (one concern per package): `idempotency.Fingerprint`, `authz.Require`, `translation.ToAppError` now read honestly at call sites. Sample composition test moved to `command/composition_test.go`; per-concern unit tests live with their packages (incl. a new direct `RunIdempotent` table).
- Composable `func(next) next` decorators deliberately rejected and recorded in the packet: inline `Require` is fail-closed by construction (a pluggable chain is forgettable); reserve/complete are `tx`-scoped inside `Do`; translation belongs to the edge.
- E06-T13 Coordinates line updated (`idempotency`/`authz`); E06 epic T01 Files/Steps/Acceptance wording updated.
- Re-verified: `gofmt` clean, `go build` + `go vet` clean, `go test ./internal/application/...` pass, `make lint` 0 issues.

## Amendment (owner review, Parameter Object Pattern & Encapsulation 2026-09-16)

- Refactored all application services (`internal/application/command`, `query`, `service`, `workflow`) to the **Parameter Object pattern**:
  - Encapsulation: All service dependencies converted to unexported/private fields (`uow`, `accounts`, `clock`, `authz`, `store`, etc.), preventing caller mutation of active dependencies.
  - Companion Parameter Objects: Exported `<Service>Params` structs containing all required dependencies.
  - Dedicated Constructors: `New<Service>(params <Service>Params) *<Service>` with deterministic parameter assignment and optional field defaulting.
  - Clean DI compatibility: Seamless integration with both compile-time (`wire`) and container-based (`uber-go/fx`) dependency injection frameworks.
- Integrated `gopls-check` into Pixi toolchain and `make lint` (`Makefile` and `scripts/dev/setup.sh`) to catch typechecking issues ahead of static analysis without polluting external editor environments.
- Formalized engineering standards:
  - Documented Parameter Object pattern in `docs/development/go-conventions.md` and `docs/architecture/clean-architecture-guide.md` §5.6.
  - Established quantitative struct receiver decision framework ($\le 64$B CPU cache line / register ABI vs $> 64$B pointer receiver) in `docs/development/go-conventions.md`.
  - Updated `AGENTS.md` architecture compliance rules.
- Rebuilt all unit test suites to use constructors (`New*Service`), adhering strictly to local `testCase` schemas, multi-line tables, inline closures, and parallel execution.

## Amendment (Muse Spark Review Polish 2026-09-16)

- Restored direct idempotency test proofs (`TestFingerprint`, `TestMapParts`, `TestRunIdempotent`) in `internal/application/command/idempotency_test.go`.
- Restored corrupt replay tests (`IDEMPOTENCY_RECORD_INVALID`) across all 11 command services (`payment_test.go`, `account_test.go`, `posting_test.go`, `transfer_test.go`, `payout_test.go`, `refund_test.go`, `dispute_test.go`, `tenant_test.go`, `reconciliation_test.go`, `ops_test.go`, `extended_reports_test.go`).
- Restored query edge-case tests in `internal/application/query/query_test.go` and `transfer_transaction_test.go`: `BALANCE_OVERFLOW` on MinInt64 negation, entry addition overflow, hold addition overflow, and available subtraction overflow; expired-hold exclusion; `STATEMENT_TOO_LARGE` bounded scan abort.
- Restored `INTENT_CONCURRENT_MODIFICATION` in `command/payment_test.go` and `CROSS_CURRENCY_UNSUPPORTED` in `command/transfer_test.go`.
- Decoupled `query/transfer.go` and `query/dashboard.go` from `command.TransferStore` by moving shared persistence models to `internal/application/port/usecase_transfer.go` and defining consumer-owned `TransferReader` interfaces in `query/`.
- Standardized pagination bounds across `command/paging.go` (`DefaultPageLimit = 50`, `MaxPageLimit = 100`).
- Corrected documentation references from `query/translation.go` to `service/error.go`.
- Verified:
  ```text
  $ gofmt -s -w . && goimports -l -w .               # clean
  $ make lint                                         # 0 issues (gopls-check + golangci-lint + shellcheck)
  $ go test -race ./...                               # all packages pass, zero races
  $ ./tasks/scripts/gate-check.sh G3                  # G3 PASSED (86.4% coverage)
  $ ./tasks/scripts/gate-check.sh G2                  # G2 PASSED (96.4% coverage)
  $ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes --handlers --ports # OK
  ```

