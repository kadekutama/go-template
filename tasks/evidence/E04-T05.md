# Verification Evidence: E04-T05

**Task:** E04-T05
**Commit:** branch `feat/E04-compliance` from `21622dbaf3d4007d3d43b4f22d1057addbbfe341` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`
**Verified By:** antigravity-agent (antigravity / Gemini 3.8 Flash)
**Verified At:** 2026-09-15T00:30:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E04-T05-R01,S01,S02 | `TestScreenTransaction` matrix + boundaries + watchlist precedence | PASS | `internal/domain/service/aml_service_test.go` |
| E04-T05-R02,R03 | `TestBuildReviewEntry` full-object entries + `TestScreenViaProviderPort` | PASS | same |
| E04-T05-R04,S03,S04 | `TestReportFields` full literals + `TestReportDefinitionFor` versioned definitions | PASS | `internal/domain/service/regulatory_reports_test.go` |
| E04-T05-R05 | `TestValidateRegulatoryReport` missing/blank/unknown | PASS | same |
| E04-T05-R06 | `make lint` + port import guard (`TestPortSurfaceInspection`) | PASS | shell output |
| Port docs | `TestPortSurfaceInspection` Strong-read documented | PASS | `internal/domain/repository/ports_test.go` |

## Commands

```text
$ make lint
CGO_ENABLED=0 golangci-lint run ./... && shellcheck scripts/**/*.sh
0 issues.
$ CGO_ENABLED=1 CC=clang go test -race ./internal/domain/... -count=1
ok all 7 packages
$ CGO_ENABLED=1 CC=clang go test -race ./... -count=1
ok all packages
$ ./tasks/scripts/gate-check.sh G2
Gate G2: PASSED (97.1% domain coverage)
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes
OK: structural checks passed
```

## Deviations and Residual Risk

- None. Port lives in `internal/domain/repository/aml_provider.go` per existing port convention. Vendor calls and hold commit remain E10/E07 scope.

## Audit & Review Revisions (2026-09-15, antigravity-agent)

- In `internal/domain/service/regulatory_reports_test.go`:
  - Replaced parameter name `reportType` with exact signature `t` in `testCase` across `TestReportFields`, `TestReportDefinitionFor`, and `TestValidateRegulatoryReport`.
  - Inlined base template fields inside test cases (removed `fullCallFields` at test body scope per `AGENTS.md` Rule 3).
  - Standardized on parent `t.Parallel()` and sequential deterministic subtests (no `tc := tc`, no inner `t.Parallel()`) across all test runners.
- In `internal/domain/service/aml_service_test.go`:
  - Standardized on parent `t.Parallel()` and sequential deterministic subtests (no `tc := tc`, no inner `t.Parallel()`) across `TestScreenTransaction`, `TestBuildReviewEntry`, and `TestScreenViaProviderPort`.
  - Added comprehensive precedence test cases (watchlist over amount/velocity count/velocity sum, amount over velocity count/sum, velocity count over sum).
  - Added boundary tests (`AmountMinor == MaxAmountMinor`, `DayCount == MaxDayCount`, `DaySumMinor == MaxDaySumMinor`, and one-over edge cases).
  - Added `ctx` field to `TestScreenViaProviderPort` testCase per `AGENTS.md` Example 3.
- Re-verified with race detector: 0 data races.
