# Verification Evidence: E06-T07

**Task:** E06-T07
**Commit:** branch `feat/E06-application` from `a23e32e70cca5e32cb6096d76f33c47b82c6f1ec` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain (`$HOME/.pixi/bin` on PATH)
**Verified By:** muse-spark
**Verified At:** 2026-09-15T12:40:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E06-T07-R01, S01 | `TestSagaCrashResume` (cancel-resume, one effect per key) | PASS | `workflow/saga.go`, `saga_test.go` |
| E06-T07-R02, S02 | `TestSagaCompensationOrder` (reverse order) + `TestSagaCompensationFailure` (parks FAILED) | PASS | `workflow/compensate.go`, `saga_test.go` |
| E06-T07-R03 | `TestSagaDuplicateStart` + `TestSagaResumeTerminal` + `TestSagaStepTimeout` + `TestSagaValidation` | PASS | same as above |
| E06-T07-R04 | Six builders compile into the matrix tests | PASS | `workflow/{transfer,refund,payout_settlement,reconciliation,period_close,batch}.go` |
| E06-T07-R05, S04 | 11 matrix tests (10 §10 rows + period gates) | PASS | `workflow/failure_matrix_test.go` |
| All | `make lint` (0 issues) + `go test -race ./...` (zero races) + full validator | PASS/OK | — |

## Commands

```text
$ gofmt -s -l internal/ test/    # empty
$ goimports -l -w .               # clean
$ make lint                       # 0 issues (exit 0; shared StepNotify const for goconst)
$ go vet ./...                    # exit 0
$ go test ./internal/application/workflow/ -race -count=3        # pass
$ go test -race ./... -count=1                               # all packages ok, zero races
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes  # OK
```

## Deviations and Residual Risk

- Compensation undoes committed steps only; forward recoveries (void+notify, reversal link) live inside steps — recorded in code and tests.
- `Runner.IDs` reserved for worker-driven key minting (E14); duplicate starts key on explicit saga IDs today.
- Saga orchestration: PR review by repo-owner required before merge (packet approval note).

## Audit fixes (2026-09-15, owner-ordered)

- `Runner.IDs` removed (saga IDs arrive explicit; runner mints nothing); retry loop honors ctx cancellation (cancel abandons without compensating — crash path); `errorCodeOf` uses `errors.As`.
- Re-verified: `make lint` 0 issues, `go test -race ./...` green, G3 PASSED, validator OK after claim release.
