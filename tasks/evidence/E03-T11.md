# Verification Evidence: E03-T11

**Task:** E03-T11
**Commit:** branch `feat/E03-money-movement` from `57de5c86cbc31bb73d69e604a4b93d3783b7285a` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1 (`/tmp/opencode/go-sdk/go/bin`)
**Verified By:** opencode-agent (opencode / Muse Spark)
**Verified At:** 2026-09-14T07:35:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E03-T11-R01/R02, S01/S02 | `TestEvaluateEligibility` (blocks + input errors) + `TestPayoutPolicyValidate` | PASS | `internal/domain/service/topup_policy_test.go`, `internal/domain/valueobject/payout_policy_test.go`, `payout_policy.go`, `valueobject/payout_policy.go` |
| E03-T11-R03–R05, R07, S03–S05 | `TestStartRecovery`, `TestResolveRecoveryOutcome`, `TestConfirmRecovery`, `TestCancelRecovery`, `TestParseRecoveryStatus`, `TestCanTransitionRecovery` | PASS | `internal/domain/service/topup_policy_test.go`, `internal/domain/valueobject/recovery_status_test.go`, `negative_balance_recovery.go` |
| E03-T11-R06 | guard test | PASS | shell output |
| All | `go test ./internal/domain/... -count=3` | exit 0 | shell output |

## Commands

```text
$ go test ./internal/domain/... -count=1
PASS all 7 packages
```

## Deviations and Residual Risk

- Durable uniqueness/locking/outbox atomicity are E07-owned; this task defines the domain record, key scheme, and posting shape the adapter must enforce.
- Supersedes the `repository-audit-agent` draft packet: jurisdiction defaults resolved as versioned caller configuration.

## Follow-up audit (2026-09-14, same owner/session, claim reopened per SDD §6)

- Brittle `BlockReason` message-parsing removed: `EvaluateEligibility` returns `(EligibilityResult{Eligible, Reason}, error)` with exported reason constants and `BlockedError()` for the wire; `RecoveryStatus` moved to `valueobject`; `ConfirmRecovery` requires PENDING status, distinct accounts, and credit == recovery account; `CancelRecovery` added; packet R05/R07/S05 and interfaces updated.
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 94.2%; `go vet`/`gofmt` clean; `check-tasks.py --format --graph --sdd` OK.

## Testify migration (2026-09-14, same owner/session, claim reopened per SDD §6)

- Test-only change: `topup_policy_test.go` eligibility/recovery halves converted to the exact table shape with parent `t.Parallel()` and sequential deterministic subtests.
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 95.2%; `go vet`/`gofmt` clean.

## Exact-shape rewrite + ctx revert (2026-09-14, same owner/session, claim reopened per SDD §6)

- Reverted the mistaken `_ context.Context` threading from all T11 production code; signatures are ctx-free again. Tests rewritten to the exact shape (incl. new `TestBlockedError` and `TestRecoveryKey`).
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 96.6%; `go vet`/`gofmt` clean.

## Normative-policy compliance (2026-09-14, same owner/session, claim reopened per SDD §6)

- All T11 parent tests (service + `payout_policy`/`recovery_status` suites) run parallel (`t.Parallel()`) with sequential deterministic subtests (no `tc := tc`, no inner `t.Parallel()`) — `-race` clean.
- Mandatory protocol: `gofmt -s` clean, `make lint` 0 issues, `CGO_ENABLED=1 CC=clang go test -race ./internal/domain/... -count=1` green, `gate-check.sh G2` PASSED, `check-tasks.py --format --graph --sdd --specs --events --codes` OK.
