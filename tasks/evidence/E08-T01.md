# Verification Evidence: E08-T01

**Task:** E08-T01  
**Commit:** branch `feat/E08-cache-messaging` from `d5582cfec391e30c546da2636e5b0d5baa689b57` (uncommitted per owner instruction; PR pending)  
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang` for race, Valkey `valkey/valkey:9.0.6` Testcontainers image (9.1.2 wire-compatible via go-redis v9.22.0), Otter v2.3.0  
**Verified By:** muse-spark  
**Verified At:** 2026-09-19T00:55:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E08-T01-R01 (Otter L1 sync visibility, per-key TTL) | `TestNewOtterCache`, `TestOtterCacheSetGetDelete`, `TestOtterCacheExpiry` | PASS | `internal/infrastructure/cache/local/otter_test.go` |
| E08-T01-R02 (Valkey go-redis client, validation) | `TestNewValkeyClient`, `TestValkeyClientValidation` | PASS | `internal/infrastructure/cache/valkey/client_test.go` |
| E08-T01-R03 (hybrid L1→L2→miss, mandatory TTL) | `TestNewHybridCache`, `TestHybridCacheSetGetDelete`, `TestHybridCacheMissAndCancel`, `TestHybridCacheTTLs` | PASS | `internal/infrastructure/cache/hybrid/cache_test.go` |
| E08-T01-R04 (post-commit invalidation, hint-only) | `TestHybridHitMissInvalidation` (E08-T06 suite) | PASS | `test/integration/cache/cache_test.go` |
| E08-T01-R05 (builder-only keys) | Code review: no hand-concatenated prefixes in adapters; isolation builders reused in messaging | PASS | `internal/infrastructure/cache/hybrid/cache.go` |
| E08-T01-R06 (port conformance) | Compile assertion `var _ appport.Cache` + vet | PASS | `internal/infrastructure/cache/hybrid/cache.go` |
| E08-T01-S01..S05 | Unit + integration cases above | PASS | same files |

## Commands

```text
$ go test ./internal/infrastructure/cache/... -count=1
ok hybrid, local, valkey
$ go test -race -count=1 ./test/integration/cache/... ./test/integration/messaging/...
ok cache 14.6s, messaging 5.3s (full -race ./... green, zero failures)
$ go vet ./internal/infrastructure/cache/... ./test/integration/cache/...
clean
$ make lint
0 issues (after gocyclo/staticcheck/goimports fixes)
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes
OK: structural checks passed (post-artifact run recorded in E08-T06)
```

## Deviations and Residual Risk

- Testcontainers Valkey image is 9.0.6 (repo harness pin) vs SPEC 9.1.2 target; go-redis v9.22.0 negotiates both (Sentinel/Cluster paths unchanged). Bumping the harness pin to 9.1.2 is E17 delivery work.
- L1<1ms/L2<5ms benchmarks are informational on shared CI (logged, not hard-gated).

## Audit & Review Revisions (2026-09-19, post-delivery review)

- `populateL1` now bounds every L1 copy by `min(L1PopulateTTL, remaining L2 TTL)` and never copies an already-expiring record, so an L1 entry cannot outlive the L2 value it was copied from; `valkey.NoExpiry` keeps the `L1PopulateTTL` bound (table-driven cases added for each TTL scenario).
- Test-correctness fixes in `internal/infrastructure/cache/hybrid/cache_test.go` (no production behavior change): the "l1 hit skips l2" case previously asserted L1 *presence*, which pre-seeding also satisfied, so it could not detect a broken short-circuit; it now asserts `l2.get`/`l1.set` op-count deltas against a post-setup baseline (`countOps` + `expectL2Read` field).
- `TestHybridCacheDeleteOrder` previously concatenated two independent per-layer journals, which never proved cross-layer ordering; it now records ops into a shared `opRecorder` in real time and asserts the exact interleaving `l1.set, l2.set, l2.delete, l1.delete` (proving the remote, failable L2 delete lands before the local L1 delete).
- Re-verified: `go test -race -count=1 ./...` green (zero failures, zero races), `make lint` 0 issues, `python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes` → `OK: structural checks passed`.

## Antigravity Audit & Review Revisions (2026-09-19, Antigravity Agent)

- Production code fix: corrected copy-pasted `SetNX` docstring above `Eval` method in `internal/infrastructure/cache/valkey/client.go`.
- Unit test refactoring to strict table-driven pattern (all 6 rules in `AGENTS.md`):
  - In `client_test.go`: declared locally scoped `type testCase struct` inside each test function; exact signature parameter names (`params`, `expectedError error`, `expectedResult`); inlined closures for variants; fixed `"whitespace addr rejected"` case which passed `""` instead of `"   "`; folded canceled context into table cases.
  - In `hybrid/cache_test.go`: converted `TestNewHybridCache`, `TestHybridCacheSet`, `TestHybridCacheDelete`, `TestHybridCacheGetValidation` to table-driven with exact error assertions and boundary tests.
- Re-verified: `golangci-lint run` 0 issues, unit tests PASS.


## Review Completion — Cline Session Handoff (2026-09-19, muse-spark)

- Completed the interrupted review wave and answered its 13 findings. Repairs to interrupted edits:
  `framework_test.go` (stray token + orphaned duplicate block removed), `cache_test.go` `wg.Go` closings.
- **Q2 generics**: `OtterCache[V]` is already generic (with `Clone` + `Codec[V]`/`JSONCodec`); bytes remain at the `port.Cache` boundary because the application port contract IS `[]byte` — the codec seam is the deliberate translation point, not a limitation.
- **Q3 validator**: `ValkeyParams`/`PublisherParams`/`SignerParams` now carry `validate` tags enforced via a loader-pattern `validateParams` (all violations joined). Nil-dependency checks stay explicit (validator cannot express interface-nil). Tests updated for the two new messages.
- **Q9 RWMutex**: NATS `Publisher` switched to `RWMutex` (`Publish` is the hot read; `Connect`/`Close` rare writes). Write-dominated guards stay `Mutex` (receipts, attempts, broker).
- **Q11**: `IsKnownTopic` is now a map lookup (`TopicRegistry`) — O(1), supersedes `slices.Contains` on the loop it replaced.
- Re-verified after completion: `go test -race -count=1 ./internal/... ./pkg/...` green; unit `-count=3` green; integration `-race` green (cache 21.6s, messaging 7.2s); `make lint` **0 issues**; full validator incl. `--subjects --ports --handlers` → OK.

## Review Wave 3 — Design Cleanup Evidence (2026-09-19, muse-spark)

- **Typed hybrid API (review Q2):** `Get`/`Set` now exchange `V`; L1 stores `V` directly, `Codec` runs once per L2 crossing, and an L1 hit performs no conversion. `V=[]byte` still satisfies `port.Cache` exactly (compile assertion). Codec became optional for L1-only caches (tested).
- **Config-sourced TTLs (review Q1):** contract defaults unexported; `TTLSet.WithDefaults()` (zero→default, negative→error) + `Validate()`; `config.CacheTTLsConfig.HybridTTLs()` is the production source (mapping tests cover zero/override/negative). `L1PopulateTTL` is now a `HybridParams` field.
- **Validation consistency (review Q4):** all tag-based constructors go through the shared `internal/shared/kernel/validate` helper with one joined-violation format; rule documented in `docs/development/go-conventions.md`.
- Commands: `go test ./internal/infrastructure/cache/... ./internal/infrastructure/config/... -count=3` → green; lint 0 issues.

## Review Wave 4 — Nil Values, Naming, Fakes, Config (2026-09-19, muse-spark)

- **Negative caching (T01):** nil is now a legal value; `ErrValueRequired` removed so "record does not exist" can be cached; usage shown in runnable examples; integration proves the entry survives L2 and expires.
- **Naming (T03):** `DeltaPublisher` removed; `nats.Publisher.PublishEvent` is the contract-level live-delta entry point (not notifications).
- **Fakes (T03–T06):** in-memory doubles moved to `test/fakes` — the production tree contains no in-memory stores except the L1 cache.
- **Constants (T01/T03/T05):** topic/TTL/retry defaults unexported or config-mapped; dispatcher requires an explicit `RetryPolicy` (sentinel when nil, custom-policy test proves configurability).
- Commands: `go test -race -count=1 ./internal/... ./pkg/...` green; wave-scoped `-count=3` green; integration `-race` green; `make lint` 0 issues; `gate-check.sh G4` PASSED; full validator OK.

## Review Wave 5 — Type Checks + Caller-Facing Constructors (2026-09-19, muse-spark)

- Restored `ErrUnsupportedValueType` (func/chan/unsafe pointer/complex) while keeping nil legal for negative caching; table-driven test covers both.
- Added `Config`/`NewCache`/`NewTypedCache`/`Module()` so callers import only the hybrid package and wiring happens once; examples now show the service-facing side.
- Re-verified with unit `-count=3`, integration `-race`, lint 0 issues.

## Review Wave 6 — Shared Engine + Typed Views (2026-09-19, muse-spark)

- Redesigned per the owner's direction: `hybrid.Cache` is the single non-generic engine (one L1 bytes budget + ONE shared L2 client, satisfies `port.Cache`); `hybrid.Typed[V](engine, codec, clone)` binds typed `TypedCache[V]` views with their own L1 partition — no repeated `Config`, no per-type Valkey pools, callers and services see only `hybrid`.
- Renamed seam constructors (`NewWithParams`/`NewTypedWithParams`) for tests; hybrid logic tests converted to internal-package tests to reach the seams; integration uses `hybrid.New(cfg)`.
- Re-verified: unit `-count=3`, `-race` tree, integration `-race` (cache/messaging), lint 0 issues.

## Review Wave 7 — Final Shape (2026-09-19, muse-spark)

- Uniform seam, injected instances, pure views, graceful `Close`, single fake. Re-verified with the standard pipeline (below).

## Review Wave 8 — Interface Callers + Test Convention Sweep (2026-09-19, muse-spark)

- Engine exposed as the sealed `Engine` interface (fx provides `*Cache`, `Engine`, and `port.Cache`); unit tests swept to the strict table-driven convention.
- Re-verified with the standard pipeline (below).

## Audit 2026-09-22 (pre-review handoff sweep, muse-spark)

- Full audit: build/vet/fmt/lint clean; unit `-race` green; validator OK; SDD packets vs shipped code reconciled (see packet amendments below); test conventions re-checked (`expectedResult`/`expectedError`, no vague names, hardened assertions, no `tc := tc`, no subtest parallelism); coverage re-measured; business requirements re-mapped to tests.
- Findings fixed in this wave are recorded in `tasks/specs/E08-<ID>.md` amendments and the evidence commands below.

## Audit 2026-09-22 — Final Verification (pre-review handoff sweep, muse-spark)

- `go build ./...`, `go vet ./...`, `gofmt -s`, `goimports`: clean.
- `make lint` (golangci-lint strict + shellcheck): 0 issues.
- `go test -race -count=1 ./internal/... ./pkg/...`: green, zero data races.
- `go test -race -count=3 ./test/integration/cache/... ./test/integration/messaging/...`: green.
- `./tasks/scripts/gate-check.sh G4`: PASSED (see note below on the NATS timeout hardening).
- `python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes --subjects --ports --handlers`: OK.

## Audit 2026-09-22 — Gate Flake Note (pre-review handoff sweep, muse-spark)

- One full-tree `gate-check.sh G4` run failed in `test/integration/persistence` (E07-owned, outside this epic's change surface) while E08 suites were green; the package passes standalone at the identical `-race -count=3`, and a clean re-run of the full gate PASSED. No shared production code in this wave explains it (additive config sections, additive kernel helpers, E08-owned files only); recorded as known full-tree load flakiness, consistent with prior Citus/persistence flake notes.

## Antigravity Review & Revision Verification (2026-09-23, Antigravity Agent)

- **Production Fast-Abort Fix (`valkey/client.go`)**: Added `ctx.Err()` checks to `Ping` and `Get`, aligning them with `TTL`, `Set`, `Delete`, `Eval`, and `SetNX` to ensure fast-fail semantics and consistent error wrapping on canceled contexts.
- Clean code & docstring fixes: corrected copy-pasted SetNX docstring above Eval in `internal/infrastructure/cache/valkey/client.go`.
- Unit test refactoring to strict table-driven pattern (all 6 rules in `AGENTS.md`):
  - `client_test.go`: converted to strict table-driven pattern, exact parameter naming, added parameter validation tests for `Delete`, `TTL`, `SetNX`, `Eval`, `Ping`, `Close`, and canceled contexts (including `Ping` and `Get` canceled context assertions).
  - `hybrid/cache_test.go` & `ttl_test.go`: eliminated single-use variables outside table cases, deduplicated `TTLSet` tests, added `TestTypedCacheValidation` and `TestTypedCacheUnsupportedTypes`.
  - `local/otter_test.go`: verified strict table-driven compliance with locally scoped `testCase`.
- Verification Commands:
  ```text
  $ go test -race -count=1 ./internal/infrastructure/cache/...
  ok  	github.com/kadekutama/go-template/internal/infrastructure/cache/hybrid	1.150s
  ok  	github.com/kadekutama/go-template/internal/infrastructure/cache/local	1.099s
  ok  	github.com/kadekutama/go-template/internal/infrastructure/cache/valkey	1.037s
  $ make lint
  0 issues.
  $ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes
  OK: structural checks passed
  ```

