# Epic E07.1: Distributed Persistence (Citus Sharding, Patroni HA, CloudNativePG, etcd)

**Status:** pending
**Story Points:** 19
**Phase:** 5.1
**Dependencies:** E07
**SDD Gate:** G4
**Design refs:** `SPEC.md §7.1`, `SPEC.md §7.10`, `docs/architecture/ADR-013-citus-sharding-and-flexible-ha.md`, `docs/architecture/ADR-017-distributed-coordination-etcd.md`, `docs/architecture/ADR-018-database-migrations-goose-and-atlas.md`, `docs/infrastructure/README.md`

> Why a separate epic: Citus multi-tenant horizontal sharding, Patroni/etcd high
> availability clustering, and CloudNativePG Kubernetes integration extend the
> baseline single-node persistence (E07) into an enterprise-scale distributed
> architecture without breaking the Clean Architecture boundary.

## Tasks

### E07.1-T01: Migration framework evolution (Goose v3 + Atlas) & Citus distribution
**Status:** pending
**Background:** Transition database migration engine from bespoke stdlib runner and golang-migrate CLI to Pressly Goose v3 (v3.28.0) and Ariga Atlas CLI (v1.3.0) per ADR-018; consolidate baseline migrations 000001–000004 into single-file Goose format preserving numeric prefixes without renumbering; implement automatic idempotent ledger bootstrap from schema_migrations to goose_db_version; update check-tasks.py --migrations verification; establish pure-compute Go migration policy; and extend schema with Citus 14.0 distribution sharded on tenant_id (20260901000005_citus_distribution.sql).
**Files:**
- Create: `internal/infrastructure/database/migration/versions/20260901000005_citus_distribution.sql`
- Modify: `internal/infrastructure/database/migration/migrate.go`, `internal/infrastructure/database/migration/embed.go`, `internal/infrastructure/database/migration/migration_test.go`, `scripts/db/migrate.sh`, `scripts/dev/setup.sh`, `Makefile`, `go.mod`, `internal/shared/deps/deps.go`, `tasks/scripts/check-tasks.py`, `test/integration/persistence/persistence_test.go`
**Steps:**
1. Upgrade migration runner to Pressly Goose v3 (`github.com/pressly/goose/v3` v3.28.0) using `goose.NewProvider` with out-of-order support and automatic idempotent bootstrap from `schema_migrations` (ADR-018).
2. Consolidate legacy `000001`–`000004` split `.up.sql`/`.down.sql` migrations into single-file Goose files preserving numeric prefixes `000001`–`000004`.
3. Add Atlas CLI schema validation, CI linting, and `atlas.sum` generation (`scripts/db/migrate.sh lint`, `make migrate-lint`).
4. Add Citus distribution migration (`20260901000005_citus_distribution.sql`) with `-- +goose NO TRANSACTION` sharded on tenant_id placed at top of file (ADR-013).
5. Update `tasks/scripts/check-tasks.py --migrations` to validate unified Goose files and `atlas.sum`.
6. Update migration unit tests and persistence integration tests.
**Acceptance Criteria:**
- [ ] Goose runner applies all migrations including Citus distribution on PostgreSQL and Citus coordinator cleanly (test).
- [ ] Down migrations revert distributed tables and core schema cleanly (test).
- [ ] Atlas migration checksums pass validation (`atlas migrate validate`).
- [ ] Unit tests pass with race detector and zero data races.
**Story Points:** 5
**Depends On:** E07-T01, E07-T02
**Related Docs:** `SPEC.md §7.1`, `docs/architecture/ADR-013-citus-sharding-and-flexible-ha.md`, `docs/architecture/ADR-018-database-migrations-goose-and-atlas.md`, `docs/infrastructure/README.md`
**SDD Gate:** G4

---

### E07.1-T02: Multi-Node Citus + Patroni + etcd HA cluster topology
**Status:** pending
**Background:** Multi-node cluster topology deploying Citus with Patroni v4.1.5 HA
and etcd v3.7.0 DCS for bare-metal and Docker Compose environments.
**Files:**
- Create: `deployments/docker/docker-compose.citus-patroni.yml`,
  `deployments/patroni/citus-coordinator.yml`,
  `deployments/patroni/citus-worker.yml`
**Steps:**
1. Define 3-node etcd cluster (etcd1, etcd2, etcd3) for distributed consensus.
2. Define Citus coordinator with 1 primary + 1 standby managed by Patroni.
3. Define 2 Citus worker shards (each 1 primary + 1 standby managed by Patroni).
4. Configure service endpoints routing traffic to the active Citus coordinator primary.
**Acceptance Criteria:**
- [ ] All containers start and healthchecks report healthy (test).
- [ ] Worker nodes are registered in pg_dist_node on coordinator (test).
**Story Points:** 4
**Depends On:** E07.1-T01
**Related Docs:** `docs/architecture/ADR-013-citus-sharding-and-flexible-ha.md`, `docs/architecture/ADR-017-distributed-coordination-etcd.md`, `docs/infrastructure/README.md`
**SDD Gate:** G4

---

### E07.1-T03: CloudNativePG (CNPG) Kubernetes deployment manifests
**Status:** pending
**Background:** Declarative Kubernetes manifests for running Citus with CloudNativePG v1.30.0 utilizing native K8s leases HA. CloudNativePG manages PostgreSQL clusters independently; Citus distributed topology is orchestrated by deploying coordinator and worker Cluster CRs with Citus extension enabled in image, linked via post-init node registration scripts.
**Files:**
- Create: `deployments/k8s/cloudnativepg/citus-coordinator.yaml`,
  `deployments/k8s/cloudnativepg/citus-workers.yaml`,
  `deployments/k8s/cloudnativepg/kustomization.yaml`
**Steps:**
1. Define CNPG Cluster custom resource for the Citus coordinator with 3 replicas and Citus extension enabled.
2. Define CNPG Cluster custom resources for Citus worker shards with automated backup config and post-init worker registration on the coordinator.
3. Configure service endpoints, network policies, and connection secrets.
**Acceptance Criteria:**
- [ ] Manifests pass kubectl apply dry-run check (test).
- [ ] Citus extension is declared in bootstrap parameters and worker registration script is documented (review).
**Story Points:** 3
**Depends On:** E07.1-T01
**Related Docs:** `docs/architecture/ADR-013-citus-sharding-and-flexible-ha.md`, `docs/infrastructure/README.md`
**SDD Gate:** G4

---

### E07.1-T04: etcd coordination adapter (dynamic config + worker election)
**Status:** pending
**Background:** Infrastructure client adapter for etcd v3.7.0 supporting gRPC
streaming config watchers and single-writer worker leader election.
**Files:**
- Create: `internal/infrastructure/coordination/etcd/client.go`,
  `internal/infrastructure/coordination/etcd/watcher.go`,
  `internal/infrastructure/coordination/etcd/election.go`,
  `internal/infrastructure/coordination/etcd/options.go`
**Steps:**
1. Wrap go.etcd.io/etcd/client/v3 with connection lifecycle and retry resilience.
2. Implement prefix-based watcher streaming configuration updates into in-memory state.
3. Implement Campaign and Resign for single-writer background worker leader election.
4. Register fx module in internal/infrastructure/coordination/etcd/module.go.
**Acceptance Criteria:**
- [ ] Watcher reflects key mutations in <10ms in container integration test without polling (test).
- [ ] Pod death releases election lease and standby acquires leadership within TTL (test).
**Story Points:** 4
**Depends On:** E01-T02, E01-T08
**Related Docs:** `docs/architecture/ADR-017-distributed-coordination-etcd.md`, `docs/infrastructure/README.md`
**SDD Gate:** G4

---

### E07.1-T05: Multi-node Citus integration test suite
**Status:** pending
**Background:** Testcontainers-based multi-node Citus integration tests verifying
tenant sharding and local ACID transactions.
**Files:**
- Create: `test/integration/citus/citus_test.go`,
  `test/testcontainers/citus.go`
**Steps:**
1. Build Testcontainers fixture for Citus coordinator + worker cluster.
2. Verify tenant co-location: entries and postings for the same tenant execute locally.
3. Verify cross-tenant aggregations (SUM balance across all accounts).
4. Test Patroni failover simulation with synchronous standby replication (`synchronous_commit = on`) ensuring zero transaction loss (RPO=0) or bounded asynchronous replication (RPO < 1s) under ungraceful primary kill; validates zero data corruption.
**Acceptance Criteria:**
- [ ] Multi-node Citus integration tests pass with zero data races (test).
- [ ] Patroni failover preserves data integrity without transaction corruption (test).
- [ ] Statement coverage meets Gate G4 criteria (test).
**Story Points:** 3
**Depends On:** E07.1-T01, E07.1-T02, E07-T09
**Related Docs:** `docs/architecture/ADR-013-citus-sharding-and-flexible-ha.md`, `docs/infrastructure/README.md`
**SDD Gate:** G4

## Acceptance Criteria

- [ ] E07.1-T01 … E07.1-T05 all completed (count 19 SP in tasks/tracking/PROGRESS.md)
- [ ] Multi-node Citus cluster operational and tenant sharding verified
- [ ] Both Patroni and CloudNativePG deployment profiles validated
- [ ] SDD gate G4 checks pass — `tasks/tracking/GATES.md#G4`
