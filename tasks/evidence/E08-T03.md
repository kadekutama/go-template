# Verification Evidence: E08-T03

**Task:** E08-T03  
**Commit:** branch `feat/E08-cache-messaging` from `d5582cfec391e30c546da2636e5b0d5baa689b57` (uncommitted per owner instruction; PR pending)  
**Environment:** Linux x86_64, Go 1.27.1  
**Verified By:** muse-spark  
**Verified At:** 2026-09-19T00:55:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E08-T03-R01 (4 topics, partition key, DLQ) | `TestRegistry`, `TestPartitionKey`, `TestDLQFor` | PASS | `internal/infrastructure/messaging/redpanda/topics_test.go` |
| E08-T03-R02 (versioned subjects, round-trip) | `TestEventSubject`, `TestBalanceSubject`, `TestQueueGroupFor` | PASS | `internal/infrastructure/messaging/nats/subjects_test.go` |
| E08-T03-R03 (TLS/SASL options, no-dial validation) | `TestNewClient`, `TestClientOptions`, `TestNewMesh`, `TestMeshPublishRequiresConnect` | PASS | `client_test.go`, `mesh_test.go` |
| E08-T03-R04 (every event routable) | `python3 tasks/scripts/check-tasks.py --subjects` | PASS | validator output |
| E08-T03-R05 (compose fragments pinned) | Inspection: redpanda v26.2.1 + nats 2.14.6 with healthchecks | PASS | `deployments/redpanda/`, `deployments/nats/` |
| E08-T03-S01..S04 | Unit + validator cases above | PASS | same files |

## Commands

```text
$ go test ./internal/infrastructure/messaging/... -count=1
ok (nats, edge, redpanda)
$ python3 tasks/scripts/check-tasks.py --subjects
green (recorded in E08-T06 full run)
$ make lint
0 issues
```

## Deviations and Residual Risk

- Real franz-go broker wiring is deferred to E14/E17 worker composition; the Client here validates without dialing and the data plane (E08-T04) runs against the Broker seam + NATS. Topology data (topics/partitions/DLQ) is final.

## Audit & Review Revisions (2026-09-19, post-delivery review)

- NATS mesh: inline-seed nkey authentication added (`nkeyAuth`) — the seed parses via `nkeys.ParseDecoratedNKey`, the public key is validated with `IsValidPublicUserKey`, and the keypair stays in memory because NATS re-signs the server nonce on every (re)connect; the seed is never logged.
- Connection lifecycle reworked: lazy connect (validate without dialing), mutex-guarded idempotent `Connect`, and drain/flush on close so edge fanout tests stay hermetic without a broker.
- Lint fix: removed two `unconvert` violations (`string(public)` where `PublicKey()` already returns `string`); `make lint` exits 0 issues.
- Re-verified: `go test -race -count=1 ./internal/infrastructure/messaging/nats/...` green; full-suite race run green.

## Antigravity Audit & Review Revisions (2026-09-19, Antigravity Agent)

- Unit test refactoring to strict table-driven pattern (all 6 rules in `AGENTS.md`):
  - In `topics_test.go`: converted `TestIsKnownTopic`, `TestPartitionKey`, `TestDLQFor` to table-driven with exact error matching, whitespace trimming edge cases, and colon check assertions.
  - In `nats/subjects_test.go`: converted `TestEventSubject`, `TestBalanceSubject`, `TestQueueGroupFor` to table-driven with exact domain error assertions (`entity.NewError("ISOLATION_SUBJECT_INVALID", ...)`).
  - In `nats/mesh_test.go`: converted `TestNewMesh` and `TestMeshPublish` to table-driven, inlining keys via closures.
- Re-verified: `golangci-lint run` 0 issues, unit tests PASS.

## Cross-Review Refinements (2026-09-19, Cline)

- `IsKnownTopic` and `DLQFor` membership loops simplified to `slices.Contains` (Go idiom per repo conventions); the already-DLQ-qualified passthrough branch was preserved (an input like `ledger.events.v1.dlq` must not become `.dlq.dlq`). Topic names stay code-owned contract constants (ADR-014, `check-tasks.py --subjects`), not config.
- Re-verified: `go test -race -count=1 ./...` green, `make lint` 0 issues.


## Review Completion — Cline Session Handoff (2026-09-19, muse-spark)

- `PublisherParams` now validated with `go-playground/validator` tags (all violations joined); edge publisher switched to `sync.RWMutex` (Publish is the read-hot path, Connect/Close rare writes); `NotificationPublisher` precedence conflict recorded in the packet + epic (E06-T12 owns ports; no port invented by E08).
- Packet E08-T03 amended (R01–R06, S01–S06, interfaces). Re-verified: unit `-count=3`, live NATS `-race`, lint 0 issues.

## Review Wave 3 — Design Cleanup Evidence (2026-09-19, muse-spark)

- **Config-driven topics (review Q1):** `TopicsConfig.WithDefaults()` exported and used by `config.RedpandaConfig.Topics()`; registry topic accessors (`LedgerEventsTopic()`, `OutboxFactsTopic()`, `WebhookJobsTopic()`) feed the relay and DLQ sinks so no production path reads topic constants.
- **Naming (review Q3):** the `nats/edge` subpackage was merged into `nats.DeltaPublisher` (`delta_publisher.go`); transport stays `nats.Publisher`. Integration suite exercises `NewDeltaPublisher` + real NATS.
- **Seam (review Q6/Q8):** `redpanda.Broker` is now the single transport seam shared by the relay publisher and both durable DLQ sinks.
- Commands: `go test ./internal/infrastructure/messaging/... -count=3` green; integration `-race` green.

## Review Wave 4 — Nil Values, Naming, Fakes, Config (2026-09-19, muse-spark)

- **Negative caching (T01):** nil is now a legal value; `ErrValueRequired` removed so "record does not exist" can be cached; usage shown in runnable examples; integration proves the entry survives L2 and expires.
- **Naming (T03):** `DeltaPublisher` removed; `nats.Publisher.PublishEvent` is the contract-level live-delta entry point (not notifications).
- **Fakes (T03–T06):** in-memory doubles moved to `test/fakes` — the production tree contains no in-memory stores except the L1 cache.
- **Constants (T01/T03/T05):** topic/TTL/retry defaults unexported or config-mapped; dispatcher requires an explicit `RetryPolicy` (sentinel when nil, custom-policy test proves configurability).
- Commands: `go test -race -count=1 ./internal/... ./pkg/...` green; wave-scoped `-count=3` green; integration `-race` green; `make lint` 0 issues; `gate-check.sh G4` PASSED; full validator OK.

## Review Wave 8 — Interface Callers + Test Convention Sweep (2026-09-19, muse-spark)

- Engine exposed as the sealed `Engine` interface (fx provides `*Cache`, `Engine`, and `port.Cache`); unit tests swept to the strict table-driven convention.
- Re-verified with the standard pipeline (below).

## Review Wave 5 — Production Transports (2026-09-19, muse-spark)

- franz-go `Producer` implements the `Broker` seam (sync produce, SASL/TLS, flush-on-close) with `redpanda.Module()`; NATS `Close` flushes before closing with `nats.Module()`; retry schedule fully config-owned (no Go defaults); live Redpanda round-trip integration proves the relay path.
- Re-verified with the standard pipeline (below).
- Commands: `go test -race -count=1 ./internal/... ./pkg/...` green; integration `-race` green; `make lint` 0 issues; `gate-check.sh G4` PASSED; full validator OK.

## Review Wave 6 — Production Transports Finalized (2026-09-19, muse-spark)

- Commands: `go test -race -count=1 ./internal/... ./pkg/...` green; integration `-race` green; `make lint` 0 issues; `gate-check.sh G4` PASSED; full validator OK.

## Review Wave 7 — Constructor Dial (2026-09-19, muse-spark)

- NATS `NewPublisher` validates and dials (fail fast; no lazy `Connect`, no mutex — pointer assigned once, reconnection library-managed). Re-verified with the standard pipeline.

## Review Wave 7 — franz-go Restored (2026-09-19, muse-spark)

- Producer transport back on franz-go (KIP-848 path, native ctx, kadm); same seam, same messages, same tests. Re-verified with the standard pipeline.

## Audit 2026-09-22 (pre-review handoff sweep, muse-spark)

- Full audit: build/vet/fmt/lint clean; unit `-race` green; validator OK; SDD packets vs shipped code reconciled (see packet amendments below); test conventions re-checked (`expectedResult`/`expectedError`, no vague names, hardened assertions, no `tc := tc`, no subtest parallelism); coverage re-measured; business requirements re-mapped to tests.
- Findings fixed in this wave are recorded in `tasks/specs/E08-<ID>.md` amendments and the evidence commands below.

## Audit 2026-09-22 — Final Verification (pre-review handoff sweep, muse-spark)

- `go build ./...`, `go vet ./...`, `gofmt -s`, `goimports`: clean.
- `make lint` (golangci-lint strict + shellcheck): 0 issues.
- `go test -race -count=1 ./internal/... ./pkg/...`: green, zero data races.
- `go test -race -count=3 ./test/integration/cache/... ./test/integration/messaging/...`: green.
- `./tasks/scripts/gate-check.sh G4`: PASSED (see note below on the NATS timeout hardening).
- `python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes --subjects --ports --handlers`: OK.

## Audit 2026-09-22 — Gate Flake Note (pre-review handoff sweep, muse-spark)

- One full-tree `gate-check.sh G4` run failed in `test/integration/persistence` (E07-owned, outside this epic's change surface) while E08 suites were green; the package passes standalone at the identical `-race -count=3`, and a clean re-run of the full gate PASSED. No shared production code in this wave explains it (additive config sections, additive kernel helpers, E08-owned files only); recorded as known full-tree load flakiness, consistent with prior Citus/persistence flake notes.

## Antigravity Review & Revision Verification (2026-09-23, Antigravity Agent)

- Bug fix & specification alignment: corrected default retention in `internal/infrastructure/messaging/redpanda/topics_test.go` from 168h to 720h (30 days), matching `SPEC.md` and `topics.go`.
- Strict unit test compliance (all 6 table-driven rules):
  - In `topics_test.go`: converted all topic tests to strict table-driven pattern with exact signature parameters and multi-line formatting.
  - In `client_test.go`: added nil-receiver and uninitialized client checks.
- Verification Commands:
  ```text
  $ go test -race -count=1 ./internal/infrastructure/messaging/redpanda/...
  ok  	github.com/kadekutama/go-template/internal/infrastructure/messaging/redpanda	1.026s
  ok  	github.com/kadekutama/go-template/internal/infrastructure/messaging/redpanda/consumer	1.122s
  ok  	github.com/kadekutama/go-template/internal/infrastructure/messaging/redpanda/publisher	1.058s
  $ make lint
  0 issues.
  $ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes
  OK: structural checks passed
  ```

