# Verification Evidence: E01-T07

**Task:** E01-T07  
**Commit:** branch `feat/E01-platform-core` from `559f045a20ab087793e72553de6fcd2928189c2d` (uncommitted per owner instruction; PR review pending)  
**Environment:** Linux x86_64, Go 1.27.1, stdlib `encoding/json` backend  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-14T02:25:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E01-T07-R01, S01 | `TestRoundTripMinorMoney` (int64 exact, no exponents) + `TestRoundTripLargePayload` (≥1MB) | PASS | `pkg/jsonparser/json_test.go` |
| E01-T07-R02, S02 | `TestGetTraversal` (leaf, range/key/scalar/invalid errors) | PASS | same suite |
| E01-T07-R03, S03 | `TestCodecSeam` (repo-wide `go list`, empty offender list) | PASS | `pkg/jsonparser/seam_test.go` |
| E01-T07-R04, S04 | `BenchmarkMarshal` | completes | record below |
| All | `check-tasks.py --format --graph --sdd` | pass, exit 0 | validator output |

## Commands

```text
$ go test ./pkg/jsonparser/ -v -bench=. -run='Test|Benchmark'  # 4/4 PASS + bench
```

## Benchmark record (stdlib baseline; a non-stdlib adapter must beat this
## with compatibility evidence before it may ship)

```text
BenchmarkMarshal-20  3116293  432.9 ns/op  128 B/op  2 allocs/op
(i7-12700H, linux/amd64, Go 1.27.1)
```

## Deviations and Residual Risk

- Enforcement is test-based, not depguard (no shared lint-config touch).
- No grandfathered offenders: the seam holds on the whole tree today.

## Amendment 2026-09-14 (ADR-012, owner-directed: stdlib → Sonic)

- Backend is now `sonic.ConfigStd` v1.15.4 (v1.12.0 does not link on Go 1.27:
  `encoding/json.unquoteBytes` relocation removed upstream — verified in
  scratch; v1.15.4 links and runs). API unchanged; all 4 existing tests pass
  unmodified (compatibility proof for covered shapes).
- New pin: `github.com/bytedance/sonic v1.15.4` (+arch/asm transitives).
- Benchmark re-run (same host):

```text
BenchmarkMarshal-20  5571139  255.3 ns/op  101 B/op  2 allocs/op   (Sonic v1.15.4)
BenchmarkMarshal-20  3116293  432.9 ns/op  128 B/op  2 allocs/op   (stdlib baseline, superseded)
```

Sonic is ~1.7x faster with fewer bytes/allocs on the money-shape payload.

- Re-verified: `go build/vet/test`, `make build-all`, `make lint` (0 issues),
  `govulncheck` (0 findings), validator `--format --graph --sdd` (exit 0).
