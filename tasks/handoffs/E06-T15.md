# Task Handoff: E06-T15 — COMPLETED

**Task:** E06-T15  
**From:** antigravity  
**To:** unassigned  
**Branch/Worktree:** feat/E07.1-distributed-persistence  
**Base / Head:** 0aedc3838e85aa0948e547070ede9df5e7636225 / work-in-progress (uncommitted per owner instruction)  
**Updated At:** 2026-09-18T09:15:00Z  

## State Summary

E06-T15 COMPLETED — Removed entity pre-mint across all flows. Repositories supply database-generated native UUIDv7 identities. Aggregate constructors and domain service entry points accept empty pre-persist IDs (`validate-when-set`) and reject malformed ones with `*_INVALID`. Full test pyramid passes with `-race -count=3`, 0 lint issues, and all Gate checks (G1, G2, G3) passing. Claim released.

Following review from Muse, all feedback items were resolved and verified:
1. Slice fixtures in `test/unit/domain/slice_test.go` and `test/unit/domain/tenant/tenancy_test.go` updated to canonical UUIDs; empty-ID acceptance and invalid-ID rejection verified.
2. Direct unit tests added for `AssignID` and empty-ID construction across all three aggregates (`Tenant`, `Account`, `Posting`), asserting success, double-assign immutability (`*_ID_IMMUTABLE`), and malformed/empty ID rejection (`*_INVALID`).
3. Outbox relay order in `ClaimSQL` corrected to `ORDER BY id` with monotonic `BIGSERIAL` sequence and composite `PRIMARY KEY (tenant_id, id)`.

4. Evidence R01/R04 wording refined to distinguish DB-persisted entity tables from workflow-level records.
5. Added `'audit_refs'` to the Citus partition inspection query in `citus_test.go`.

## Work Completed

- Domain Aggregates & Entities:
  - `internal/domain/entity/`: Relaxed pre-persistence ID validation on `AccountData`, `TenantData`, `PostingData`, `HoldData`, `Entry`, and `Ledger` to permit empty IDs prior to persistence, while rejecting malformed IDs with canonical `*_INVALID` codes.
  - `internal/domain/aggregate/account.go`: `OpenAccount` accepts empty ID; defers `account.created.v1` until `AssignID` is called with database-generated ID.
  - `internal/domain/aggregate/tenant.go`: `OpenTenant` accepts empty ID/LedgerID; defers `tenant.created.v1` until `AssignID`.
  - `internal/domain/aggregate/posting.go`: `ConstructPosting` accepts empty ID; defers `transaction.posted.v1` until `AssignID`. `ReversePosting` supports unpersisted new postings.
- Application Command Handlers:
  - `account.go`: Removed `s.ids.NewID()` for AccountID; attaches DB-generated ID returned by `s.accounts.Create`.
  - `tenant.go`: Removed `s.ids.NewID()` for TenantID and LedgerID; uses deterministic platform namespace for pre-intake idempotency reservation and attaches DB-generated ID returned by `s.tenants.Create`.
  - `posting.go`: Removed `s.ids.NewID()` for PostingID and EntryIDs; commits empty-ID posting and attaches DB-returned IDs.
  - `transaction.go`: Reversal commits with empty-ID posting and attaches DB-returned IDs. Removed unused pre-mint fields and `fixedIDGen`.
  - `transfer.go`: Removed pre-minted `"post:" + transferID` and entry IDs in `commitTransferPosting`; commits empty-ID posting and attaches DB-returned IDs.
- Tests & Verification:
  - All unit test suites updated and table-driven rules (Rules 1–6) preserved.
  - `models_test.go` and `models/ledger.go` updated to validate stored DB rows.
  - `make lint` passed with 0 issues (`gocyclo` issues resolved by clean decomposition).
  - Race-detector test suite passed with zero data races.
  - Gates G1, G2 (95.3%), G3 (88.3%) passed.

## Changed Files

- `internal/domain/entity/account.go`, `entry.go`, `hold.go`, `posting.go`, `tenant.go`
- `internal/domain/aggregate/account.go`, `posting.go`, `tenant.go`
- `internal/application/command/account.go`, `posting.go`, `tenant.go`, `transaction.go`, `transfer.go`
- `internal/infrastructure/database/postgres/models/ledger.go`
- Corresponding unit tests across `internal/domain/...`, `internal/application/...`, and `internal/infrastructure/...`

## Decisions and Assumptions

- Non-entity identities (`eventID`, object storage keys, idempotency keys, request correlation) continue to use `port.IDGenerator`.
- In-memory mock repositories generate deterministic canonical UUIDv7s when given entities with empty IDs, mirroring PostgreSQL/GORM `RETURNING` behavior.

## Verification Evidence

- Recorded in `tasks/evidence/E06-T15.md`.

## Remaining Work

- None for E06-T15.

## Resume Instructions

```bash
git status --short --branch && python3 tasks/scripts/check-tasks.py --ready
export PATH="$HOME/.pixi/bin:$PATH" CGO_ENABLED=1 CC=clang
go test -race -count=1 ./internal/domain/... ./internal/application/...
```
