# Task Handoff: E00-T00

**Task:** E00-T00  
**From:** opencode-agent  
**To:** unassigned  
**Branch/Worktree:** feat/E00-T00-git-baseline  
**Base / Head:** b51d5c94f01cdb96c6f5f55a0802da2c786af6fa / 398b6cd25781e2636a3ae1af1d25b59985f9345f  
**Updated At:** 2026-09-13T05:08:36Z

## State Summary

All E00-T00 work except the shared-remote steps is done and committed on
`feat/E00-T00-git-baseline`. Task is `blocked` waiting on the owner-supplied
remote URL. First unverified items: E00-T00-R03/S01 (origin) and S03
(claim-race exercise).

## Work Completed

- E00-T00-R01: owner standalone-repo decision recorded (packet + governance §1).
- E00-T00-R02: baseline commits `6eca386`/`b51d5c9` verified.
- E00-T00-R04: `.github/CODEOWNERS` + `docs/development/repository-governance.md`
  created (commit `398b6cd`).
- E00-T00-R05/S02/S04: validator full suite green; no sibling paths; no nested `.git`.
- Packet `tasks/specs/E00-T00.md` ready (approved); claim active; evidence written.

## Changed Files

- tasks/specs/E00-T00.md — draft → ready (refreshed normative inputs, remote as
  owner-supplied input with local stub, open questions none, approved).
- .github/CODEOWNERS — new; merge authority for claims, migrations, contracts, ADRs.
- docs/development/repository-governance.md — new; boundary, branches, merge and
  claim-serialization policy, fresh-checkout proof.
- tasks/claims/E00-T00.md — new active claim (base `b51d5c9`).
- tasks/evidence/E00-T00.md — full requirement coverage + commands.
- tasks/epics/E00-foundation.md — E00-T00 pending → blocked; background refreshed.

## Decisions and Assumptions

- Standalone `go-template` + `main` per owner approval 2026-09-13 (owner-reversible).
- Serialized local `main` is the approved interim coordinator until
  `<owner-remote-url>` is configured (reversible by adding origin).
- `CODEOWNERS` `@repository-owner` is a placeholder for the real GitHub
  user/team (replace when origin is created).

## Verification Evidence

- `python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs
  --events --handlers --ports --subjects --docs --codes` → exit 0 (140 tasks /
  20 epics / 432 SP); see `tasks/evidence/E00-T00.md`.

## Remaining Work

1. Owner supplies remote URL (already chose "provide URL", awaiting the value).
2. `git remote add origin <url> && git push -u origin main && git push -u origin feat/E00-T00-git-baseline`.
3. Run the S03 claim-race exercise (two fixture claims, exactly one merges).
4. Replace `@repository-owner` with the real handle; open a PR; flip E00-T00 to
   `completed`, tick `tasks/tracking/PROGRESS.md`, set claim to `released`.

## Risks and Blockers

- Blocker: shared remote URL outstanding (owner). No push/PR/multi-harness
  racing possible until then. Claim stays `active`; branch holds all work.
- No competing claims; `feat/E00-T00-git-baseline` is the only writer.

## Resume Instructions

Exact first command and expected baseline result. Do not rely on chat history.

```bash
git status --short --branch && git log --oneline -4 && python3 tasks/scripts/check-tasks.py --format --graph --sdd
```

Expected: on `feat/E00-T00-git-baseline`, clean tree, HEAD at `398b6cd` (or its
successor), validator OK. Then continue from Remaining Work step 1.
