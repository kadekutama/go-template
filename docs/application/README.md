# Application Layer

CQRS use cases orchestrating domain behavior: commands, queries, DTOs, ports,
services, and workflows/sagas (`internal/application/`). See
[SPEC.md §3](../../SPEC.md) (architecture layers). Content is filled by E18;
implementation lives in E06.
