# Verification Evidence: E03-T03

**Task:** E03-T03
**Commit:** branch `feat/E03-money-movement` from `57de5c86cbc31bb73d69e604a4b93d3783b7285a` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1 (`/tmp/opencode/go-sdk/go/bin`)
**Verified By:** opencode-agent (opencode / Muse Spark)
**Verified At:** 2026-09-14T07:10:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E03-T03-R01, S04 | `TestExpectedSettlementLag` (§5 table) | PASS | `internal/domain/service/payout_service_test.go`, `valueobject/payout_method.go` |
| E03-T03-R02/R06, S01/S02 | `TestCancelPayout` + `TestTransitionPayout` | PASS | same + `valueobject/payout_status.go` |
| E03-T03-R03/R04/R08, S05 | `TestValidateSubmission` + `TestValidateSettlement` | PASS | `internal/domain/service/payout_service.go` |
| E03-T03-R05, S03 | `TestIsOverdue` + `TestExpectedSettlementAt` | PASS | same |
| E03-T03-R07 | guard test | PASS | shell output |
| All | `go test ./internal/domain/... -count=3` | exit 0 | shell output |

## Commands

```text
$ go test ./internal/domain/... -count=1
PASS all 7 packages
```

## Deviations and Residual Risk

- Eligibility/reserves excluded (E03-T11 owns policy); failure/return reversal constructs submission reversal + separate fee leg per ledger-core §6.3.

## Follow-up audit (2026-09-14, same owner/session, claim reopened per SDD §6)

- Settlement MUST reuse the submission rail; staged accounts MUST be pairwise distinct (`PAYOUT_ACCOUNT_INVALID`); method lag table converted to a data registry (OCP); packet R08/S05.
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 94.2%; `go vet`/`gofmt` clean; `check-tasks.py --format --graph --sdd` OK.

## Exact-shape rewrite + ctx revert (2026-09-14, same owner/session, claim reopened per SDD §6)

- Reverted the mistaken `_ context.Context` threading from all T03 production code; signatures are ctx-free again. Tests rewritten to the exact shape (one function per production function, local `testCase`, one list/loop/`t.Run`).
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 96.6%; `go vet`/`gofmt` clean.

## Normative-policy compliance (2026-09-14, same owner/session, claim reopened per SDD §6)

- All T03 subtests (service + `payout_method`/`payout_status` suites) now run parallel (`tc := tc; t.Parallel()`) — `-race` clean.
- Mandatory protocol: `gofmt -s` clean, `make lint` 0 issues, `CGO_ENABLED=1 CC=clang go test -race ./internal/domain/... -count=1` green, `gate-check.sh G2` PASSED, `check-tasks.py --format --graph --sdd --specs --events --codes` OK.

## Uniform-table exemplar (2026-09-14, same owner/session, claim reopened per SDD §6)

- Test-only change: `TestParseAndTransitionTables` restructured into named `testCase` tables with a single runner per subtest (parse vocabulary incl. full payout set, eager spot-transition table, registries table).
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 95.2%; `go vet`/`gofmt` clean.
