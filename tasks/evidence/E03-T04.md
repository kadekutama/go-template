# Verification Evidence: E03-T04

**Task:** E03-T04
**Commit:** branch `feat/E03-money-movement` from `57de5c86cbc31bb73d69e604a4b93d3783b7285a` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1 (`/tmp/opencode/go-sdk/go/bin`)
**Verified By:** opencode-agent (opencode / Muse Spark)
**Verified At:** 2026-09-14T07:10:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E03-T04-R01–R03, R07/R08, S01/S02/S05/S06 | `TestAssessTransactionFee`, `TestAssessMonthlyFee`, `TestSplitFee` | PASS | `internal/domain/service/fee_interest_test.go`, `fee_service.go` |
| E03-T04-R04/R05, S03/S04 | `TestAccrueDaily` (Actual/365, direction, zero/negative) | PASS | same + `interest_service.go` |
| E03-T04-R06 | guard test | PASS | shell output |
| All | `go test ./internal/domain/... -count=3` | exit 0 | shell output |

## Commands

```text
$ go test ./internal/domain/... -count=1
PASS all 7 packages
```

## Deviations and Residual Risk

- Rounding boundary is integer truncation at the daily computation; sub-minor-unit accrual posts nothing. Scheduling lives in E14.

## Follow-up audit (2026-09-14, same owner/session, claim reopened per SDD §6)

- Zero-bps schedules assess zero (explicit free tier, floor skipped); `FeeSplit`/`SplitFee` added for processor/platform separation (packet R07/R08/S05/S06); three checked-mul copies consolidated into `service/arithmetic.go` (DRY).
- Re-verified: `go test ./internal/domain/... -count=3` exit 0, incl. MaxInt64 overflow branches; domain coverage 94.2%; `go vet`/`gofmt` clean; `check-tasks.py --format --graph --sdd` OK.

## Exact-shape rewrite + ctx revert (2026-09-14, same owner/session, claim reopened per SDD §6)

- Reverted the mistaken `_ context.Context` threading from all T04 production code; signatures are ctx-free again. Tests rewritten to the exact shape (multi-return functions use `expectedAmountMinor`/`expectedDebitNormal` fields).
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 96.6%; `go vet`/`gofmt` clean.

## Normative-policy compliance (2026-09-14, same owner/session, claim reopened per SDD §6)

- All T04 subtests now run parallel (`tc := tc; t.Parallel()`) — `-race` clean.
- Mandatory protocol: `gofmt -s` clean, `make lint` 0 issues, `CGO_ENABLED=1 CC=clang go test -race ./internal/domain/... -count=1` green, `gate-check.sh G2` PASSED, `check-tasks.py --format --graph --sdd --specs --events --codes` OK.
