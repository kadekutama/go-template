# Verification Evidence: E03-T08

**Task:** E03-T08
**Commit:** branch `feat/E03-money-movement` from `57de5c86cbc31bb73d69e604a4b93d3783b7285a` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1 (`/tmp/opencode/go-sdk/go/bin`)
**Verified By:** opencode-agent (opencode / Muse Spark)
**Verified At:** 2026-09-14T07:25:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E03-T08-R01–R05, R07, S01–S04 | `TestAuthorize`, `TestCapture`, `TestSweepExpired`, `TestValidateDescriptor`, `TestRequireAction`, `TestResolveAction`, `TestParseAuthStatus`, `TestCanTransitionAuth` | PASS | `internal/domain/service/dispute_auth_test.go`, `internal/domain/valueobject/auth_status_test.go`, `internal/domain/valueobject/descriptor_test.go`, `authorization_service.go`, `valueobject/auth_status.go`, `descriptor.go` |
| E03-T08-R06 | guard test | PASS | shell output |
| All | `go test ./internal/domain/... -count=3` | exit 0 | shell output |

## Commands

```text
$ go test ./internal/domain/... -count=1
PASS all 7 packages
```

## Deviations and Residual Risk

- Provider challenge completion (E10-T03) and hold persistence (E07) deferred; SCA maps to `payment_intent.requires_action.v1` shape.

## Follow-up audit (2026-09-14, same owner/session, claim reopened per SDD §6)

- `CanTransitionAuth` table added; `Capture` rejects expired/voided/challenged auths (`AUTH_NOT_CAPTURABLE`, amount checked first so over-capture still reports remaining); `RequireAction`/`ResolveAction` return errors and fail closed outside their states; packet R07.
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 94.2%; `go vet`/`gofmt` clean; `check-tasks.py --format --graph --sdd` OK.

## Testify migration (2026-09-14, same owner/session, claim reopened per SDD §6)

- Test-only change: `dispute_auth_test.go` auth half converted to `testify require/assert` with `t.Run` subtests.
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 95.2%; `go vet`/`gofmt` clean.

## Uniform-table exemplar (2026-09-14, same owner/session, claim reopened per SDD §6)

- Test-only change: shared `TestParseAndTransitionTables` exemplar (covers auth vocab); no T08 production change.
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 95.2%; `go vet`/`gofmt` clean.

## Exact-shape rewrite + ctx revert (2026-09-14, same owner/session, claim reopened per SDD §6)

- Reverted the mistaken `_ context.Context` threading from all T08 production code; signatures are ctx-free again. Tests rewritten to the exact shape (dedicated `TestRequireAction`/`TestResolveAction`, challenged-auth capture row).
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 96.6%; `go vet`/`gofmt` clean.

## Normative-policy compliance (2026-09-14, same owner/session, claim reopened per SDD §6)

- All T08 parent tests (service + `auth_status`/`descriptor` suites) run parallel (`t.Parallel()`) with sequential deterministic subtests (no `tc := tc`, no inner `t.Parallel()`) — `-race` clean.
- Mandatory protocol: `gofmt -s` clean, `make lint` 0 issues, `CGO_ENABLED=1 CC=clang go test -race ./internal/domain/... -count=1` green, `gate-check.sh G2` PASSED, `check-tasks.py --format --graph --sdd --specs --events --codes` OK.
