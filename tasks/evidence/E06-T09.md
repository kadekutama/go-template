# Verification Evidence: E06-T09

**Task:** E06-T09
**Commit:** branch `feat/E06-application` from `a23e32e70cca5e32cb6096d76f33c47b82c6f1ec` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain (`$HOME/.pixi/bin` on PATH)
**Verified By:** muse-spark
**Verified At:** 2026-09-15T12:10:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E06-T09-R01, S01 | `TestReconReports` (summary totals + by-status; status-filtered list) | PASS | `query/recon_reports.go`, `extended_reports_test.go` (query) |
| E06-T09-R02, S02 | `TestRegulatoryAllTypes` (CALL_REPORT/1099/FATCA/CRS validated + stored through T05 service) | PASS | `command/extended_reports_test.go` |
| E06-T09-R03, S03 | `TestSubscriptions` (template validation, due listing, delivery + fact + last-run, unsubscribe, replay) | PASS | `command/extended_reports.go`, tests |
| E06-T09-R04, S04 | `TestDashboardVolume` (day/week buckets, bad granularity, 25-page bound error, status counts) | PASS | `query/dashboard.go`, tests |
| DTOs | `ToRunSummaryDTO/ToSubscriptionDTO/ToVolumeBuckets` covered in query/command assertions | PASS | `dto/extended_reports.go` |
| All | `make lint` (0 issues) + `go test -race ./...` (zero races) + full validator | PASS/OK | — |

## Commands

```text
$ gofmt -s -l internal/ test/    # empty
$ goimports -l -w .               # clean
$ make lint                       # 0 issues (exit 0; shared runners/validators for gocyclo ≤ 12)
$ go vet ./...                    # exit 0
$ go test ./internal/application/... -count=3                # pass
$ go test -race ./... -count=1                               # all packages ok, zero races
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes  # OK
```

## Deviations and Residual Risk

- Additive `ReconStore.ListBreaksByRun` (+ T05 fakes extended; no behavior change).
- Regulatory single-type rendering stays owned by E06-T05 (reused, not duplicated); this task adds all-4-types validation coverage.
- Storage writes land before `Do` in report staging (same key/bytes: overwrite-safe), keeping callbacks retry-safe.
