# Verification Evidence: E08-T02

**Task:** E08-T02  
**Commit:** branch `feat/E08-cache-messaging` from `d5582cfec391e30c546da2636e5b0d5baa689b57` (uncommitted per owner instruction; PR pending)  
**Environment:** Linux x86_64, Go 1.27.1, Valkey Testcontainers image  
**Verified By:** muse-spark  
**Verified At:** 2026-09-19T00:55:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E08-T02-R01 (SET NX PX, fail-fast contention) | `TestRedlockOneWinner` (8 contenders, exactly 1 winner) | PASS | `test/integration/cache/cache_test.go` |
| E08-T02-R01..R03 (token match, idempotent release, validation) | `TestNewRedlock`, `TestRedlockAcquireValidation`, `TestRedlockAcquireReleaseRefresh` (fake store: re-acquire, extend, loss, no-steal, idempotent) | PASS | `internal/infrastructure/lock/redlock_test.go` |
| E08-T02-R04 (WithLock release + contention hook) | `TestWithLock` + `TestWithLockDurableEffect` (8 contenders, exactly 1 `SetNX` effect commits) | PASS | `redlock_test.go`, integration cache suite |
| E08-T02-R05 (DB remains money boundary) | Code review: lock package never touches ledger SQL; guard docs require fencing/run-key validation | PASS | `internal/infrastructure/lock/` |
| E08-T02-S01..S04 | Fake-store unit + integration (one-winner, refresh, no-steal, durable effect) | PASS | same files |

## Commands

```text
$ go test ./internal/infrastructure/lock/... -count=3
ok
$ go test -race -count=1 ./test/integration/cache/... -run 'TestRedlock|TestWithLock'
ok (1 winner of 8, 1 effect of 8)
$ make lint
0 issues
```

## Deviations and Residual Risk

- Release/refresh use GET-compare-then-write without Lua (only E08-T07 may ship Lua per review guard). Safe for coordination-only use because durable writes validate fencing/run keys; a racing expiry can only cause duplicate coordination, never overspend. Proven: `release never steals another holder` (unit + integration).
- Redlock depends on the narrow `ValkeyStore` seam (production `*ValkeyClient` asserts conformance); typed-nil callers must pass untyped nil (standard Go semantics).
- No Lua in this task: `grep -rn "redis.call" --include="*.go" .` returns nothing.

## Audit & Review Revisions (2026-09-19, post-delivery review)

- Lease-loss semantics made explicit: `Refresh` fails with `ErrLockLost` when the key is gone (expired/released) or held by a foreign token, so holders revalidate durable state instead of assuming exclusivity; `Release` stays idempotent and never deletes another holder's token.
- `WithLock` guard contract clarified and refactored into `acquireError` / `releaseLease` / `guardError` helpers (gocyclo 13 → within the ≤12 limit): release runs on `context.Background()` so a caller cancel or fn failure still frees the lease; error precedence is explicit — the work's own (non-cancellation) error wins, and lease loss wins over the derived `context.Canceled` it caused.
- New guard tests in `redlock_test.go`: "fn error wins over renewal error", "released hook fires and lease is freed" (asserts the hook and the freed key via `isHeld`), "caller cancellation still releases the lease", plus existing auto-renewal and lost-lease-cancels-fn coverage.
- `make lint` gocyclo fix verified: strict lint exits with 0 issues; `go test -race -count=1 ./internal/infrastructure/lock/...` green.

## Antigravity Audit & Review Revisions (2026-09-19, Antigravity Agent)

- Refactored `redlock_test.go` to strictly adhere to the repository's 6 unit test rules in `AGENTS.md`:
  - Locally scoped `type testCase struct` inside test functions.
  - Converted `TestNewRedlock`, `TestRedlockAcquire`, `TestRedlockLeaseRelease`, `TestRedlockLeaseRefresh`, and `TestWithLock` to exhaustive table-driven test cases.
  - Asserted exact return values and error types.
  - Inlined closure definitions for test setup variations and mocks.
- Re-verified: `golangci-lint run` 0 issues, unit tests PASS.

## Cross-Review Refinements (2026-09-19, Cline)

- Renewal goroutine now runs through `safe.Go` (SPEC §9.7 panic containment): a panic in `Refresh` is logged with stack, delivered via `onPanic`, cancels the work, and surfaces as the guard error — previously an uncontained panic crashed the process.
- New `TestWithLockRenewalPanicContained` proves containment: `safe.Panic` surfaces via `errors.As`, the work is cancelled, and the lease is still released.
- `sync.WaitGroup.Go` (Go 1.25+) was deliberately NOT used here: `safe.Go` owns the goroutine spawn, so the explicit `Add(1)`/`Done` remains to guarantee the lease release cannot race an in-flight `Refresh`.
- Re-verified: `go test -race -count=1 ./...` green, `make lint` 0 issues.


## Audit 2026-09-22 (pre-review handoff sweep, muse-spark)

- Full audit: build/vet/fmt/lint clean; unit `-race` green; validator OK; SDD packets vs shipped code reconciled (see packet amendments below); test conventions re-checked (`expectedResult`/`expectedError`, no vague names, hardened assertions, no `tc := tc`, no subtest parallelism); coverage re-measured; business requirements re-mapped to tests.
- Findings fixed in this wave are recorded in `tasks/specs/E08-<ID>.md` amendments and the evidence commands below.

## Audit 2026-09-22 — Final Verification (pre-review handoff sweep, muse-spark)

- `go build ./...`, `go vet ./...`, `gofmt -s`, `goimports`: clean.
- `make lint` (golangci-lint strict + shellcheck): 0 issues.
- `go test -race -count=1 ./internal/... ./pkg/...`: green, zero data races.
- `go test -race -count=3 ./test/integration/cache/... ./test/integration/messaging/...`: green.
- `./tasks/scripts/gate-check.sh G4`: PASSED (see note below on the NATS timeout hardening).
- `python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes --subjects --ports --handlers`: OK.

## Audit 2026-09-22 — Gate Flake Note (pre-review handoff sweep, muse-spark)

- One full-tree `gate-check.sh G4` run failed in `test/integration/persistence` (E07-owned, outside this epic's change surface) while E08 suites were green; the package passes standalone at the identical `-race -count=3`, and a clean re-run of the full gate PASSED. No shared production code in this wave explains it (additive config sections, additive kernel helpers, E08-owned files only); recorded as known full-tree load flakiness, consistent with prior Citus/persistence flake notes.

## Antigravity Review & Revision Verification (2026-09-23, Antigravity Agent)

- **Production Concurrency Fix (`guard.go`)**: Fixed intermittent race condition in `renew()` where pseudo-random channel selection under `select` could attempt `lease.Refresh` on an already canceled `runCtx` when worker `fn` completed, causing spurious `context.Canceled` return values. Added `ctx.Err() != nil` check and suppressed derived cancellation errors.
- Added table-driven unit test verifying `WithLock` returns `nil` when `fn` completes while renewal ticker is active.
- Clean code & robust error handling: converted error comparisons in `internal/infrastructure/lock/redlock_test.go` from pointer-matching `errors.Is` to value-based `assert.EqualError`.
- Strict unit test compliance (all 6 table-driven rules):
  - Inlined variant closures directly within testCases.
  - Added test coverage for nil-receiver and uninitialized lease operations.
  - Converted renewal panic containment tests into strict table-driven pattern with exact signature parameters.
- Verification Commands:
  ```text
  $ go test -race -count=1 ./internal/infrastructure/lock/...
  ok  	github.com/kadekutama/go-template/internal/infrastructure/lock	1.089s
  $ make lint
  0 issues.
  $ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes
  OK: structural checks passed
  ```

