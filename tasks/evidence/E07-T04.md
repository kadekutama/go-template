# Verification Evidence: E07-T04

**Task:** E07-T04
**Commit:** branch `feat/E07-persistence` from `3d794105864c4e91c6efd990feb2adc57380e029` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain
**Verified By:** antigravity
**Verified At:** 2026-09-16T12:50:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E07-T04-R01 | `TestDevPlanShape` (1 ledger, 9-account chart, funding/transfer/fee/refund postings) | PASS | `database/seed/seed.go` |
| E07-T04-R02, S01 | `seed.sh`/`reset.sh` review + `make db-seed`/`db-reset` targets; executable seeder compiled in `cmd/seed/main.go` and executed via `scripts/db/seed.sh`; shellcheck via lint; `-tags postgres` compiled migrate CLI; help paths work without DATABASE_URL | PASS | `scripts/db/{seed,reset}.sh`, `cmd/seed/main.go`, `internal/infrastructure/database/seed/postgres.go`, `Makefile` |
| E07-T04-R03, S02 | `TestApplyIdempotent` (double apply, same row set), `TestPostgresSeedEndToEnd` live against PostgreSQL 18.6 with idempotency | PASS | `seed_test.go`, `seed/postgres.go`, `persistence_test.go` |
| E07-T04-R04, S03 | `TestSeedMatchesFixtureIDs` (every seeded ID in fixtures) | PASS | `seed_test.go` |
| E07-T04-R05 | Code review: narrow store interfaces, no ports implemented | PASS | `seed.go` |
| All (traceability) | validator | OK | structural checks passed |

## Commands

```text
$ go test ./internal/infrastructure/database/seed/... -count=3   # pass
$ go test -v -race -run TestPostgresSeedEndToEnd ./test/integration/persistence/... # pass live vs PostgreSQL
$ go build ./cmd/seed/...                                        # compiles cleanly
$ ./scripts/db/seed.sh --help && ./scripts/db/reset.sh --help    # help paths verify cleanly
$ make lint                                                      # shellcheck & linter clean
```

## Deviations and Residual Risk

- Revisions implemented:
  - Ensured executable bits on all `scripts/db/*.sh` scripts.
  - Updated `scripts/dev/setup.sh` to install `migrate` with `-tags 'postgres'`.
  - Fixed PostgreSQL `22P02 invalid input syntax for type json` in `TenantModel.Settings`, `AccountModel.Metadata`, and `PostingModel.Metadata` by adding GORM `BeforeCreate`/`BeforeUpdate` hooks that set default `"{}"` when empty.
  - Aligned `seed.go` account class constants (`ClassOperating`, `ClassFee`, `ClassSuspense`) with domain `valueobject.AccountClass` (`valueobject.ClassLiability`, `valueobject.ClassRevenue`, `valueobject.ClassAsset`), resolving `ACCOUNT_CLASS_INVALID` on `acct-test-operating-USD`.
  - Disentangled fee operation literal from class in `seed.go` by explicitly assigning `Operation: "FEE"`.
  - Added `TestPostgresSeedEndToEnd` in `persistence_test.go` confirming live end-to-end seed application and strict idempotency against PostgreSQL 18.6.
  - Updated `scripts/db/seed.sh` and `scripts/db/reset.sh` to evaluate `-h`/`--help` before requiring `DATABASE_URL`.

## Audit fix addendum (2026-09-17, TAKEOVER muse-spark, owner-directed "fix them all")

Seed CLI now drains its pool on exit (`sqlDB.Close` via defer in `cmd/seed/main.go`); `go build ./...` + `go vet ./cmd/...` clean. No behavior change on the success path beyond releasing connections instead of relying on process teardown.

## fx-composition addendum (2026-09-17)

`cmd/seed/main.go` now composes through the shared `di.RunApp` (binary `seed`, `version` var, work closure over the populated runner, `OnStop` pool drain) with operational lines on the kernel logger and bootstrap failures on stderr. `TestSeedGraphValidates` proves wiring with no database; `TestSeedAppRequiresDSN` proves the missing-DSN exit path hermetically. Covers E07-T04-R06/S04.
