# Verification Evidence: E01-T02

**Task:** E01-T02  
**Commit:** branch `feat/E01-platform-core` from `559f045a20ab087793e72553de6fcd2928189c2d` (uncommitted per owner instruction; PR review pending)  
**Environment:** Linux x86_64, Go 1.27.1, fx v1.24.0  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-14T02:00:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E01-T02-R01, S01 | 8 constructors return `fx.Option`; `grep -rn "^var .*=" internal/shared/di/` empty | pass | `internal/shared/di/*.go` |
| E01-T02-R02, S01 | same grep | pass (exit 1, empty) | shell output |
| E01-T02-R03, S02 | `go test ./internal/shared/di/ -run TestGraphValidates -v` | PASS | `internal/shared/di/di_test.go` |
| E01-T02-R04, S03 | `make build-all` (5 binaries) + `go vet ./...` | pass | `bin/`, vet clean |
| All | `check-tasks.py --format --graph --sdd` | pass, exit 0 | validator output |

## Commands

```text
$ grep -rn "^var .*=" internal/shared/di/   # empty (exit 1)
$ go test ./internal/shared/di/ -run TestGraphValidates -v  # PASS
$ make build-all && go vet ./...                             # 5 binaries, vet clean
```

## Deviations and Residual Risk

- `cmd/*/main.go` (E00-T01) edited minimally for fx composition; recorded in
  packet coordination notes. Binaries now block in `fx.Run()` awaiting signals.
- fx blank import removed from `internal/shared/deps` (real imports retain it).
- Real logger replaces `fx.NopLogger` in E01-T04.

## Post-completion note (added during E01-T04, 2026-09-14)

- Binary modules no longer nest Domain/Application/Infrastructure: the first
  `fx.Provide` (E01-T04's logger) exposed duplicate provides, failing
  `TestGraphValidates`. Mains now compose shared modules + their binary module
  explicitly. T02's proof commands were re-run green after the restructure.

## Review amendment (Gemini, verified 2026-09-14)

- `di.InfrastructureModule` gained `ProvideClock` (`kernel.SystemClock`) and
  `ProvideIDGenerator` (`kernel.NewUUIDGenerator()`) — T06 products were
  previously unwired. `TestGraphValidates` re-run green (unused provides are
  valid fx).

## Audit fix addendum (2026-09-17, TAKEOVER muse-spark, owner-directed "fix them all")

| Requirement/Scenario | Proof | Result |
|----------------------|-------|--------|
| E01-T02-R05, S04 (no NopLogger) | `grep -rn "fx.NopLogger" cmd/` empty; `make build-all` green | PASS |
| E01-T02-R06, S05 (explicit timeouts) | `TestFxLifecycle` (positive bounds, option composes, graph validates) | PASS |

Change surface delta: `internal/shared/di/fxlifecycle.go` (`FxLogger()`, `FxStartTimeout`/`FxStopTimeout` 30s), all 5 mains compose logger + timeouts. Config-file-driven lifecycle tuning waits for config in the fx graph (E11).

## Configurable-timeouts addendum (2026-09-17)

| Requirement/Scenario | Proof | Result |
|----------------------|-------|--------|
| E01-T02-R06, S05 (env timeouts) | `TestFxLifecycle` (unset→30s defaults, valid durations apply, malformed/non-positive fail fast) | PASS |

`di.FxTimeouts()` resolves `APP_FX_START_TIMEOUT` / `APP_FX_STOP_TIMEOUT` (Go durations); all 5 mains fail fast on malformed values before building the graph. Config-file sourcing waits for config in the fx graph (E11).

## Lifecycle-demo addendum (2026-09-17)

Live `rest-api` binary under SIGTERM (`timeout -s TERM`) emits the full
lifecycle with `binary`+`version` on every line and exits 0:
`app initialising` → `fx started` → `app running` → `app shutting down` →
`fx stopped` → `app finished successfully`. `-ldflags "-X
main.version=v1.2.3-test"` surfaces in the version field, proving the
release-stamping contract for E17.
