# Verification Evidence: E06-T04

**Task:** E06-T04
**Commit:** branch `feat/E06-application` from `a23e32e70cca5e32cb6096d76f33c47b82c6f1ec` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain (`$HOME/.pixi/bin` on PATH)
**Verified By:** muse-spark
**Verified At:** 2026-09-15T10:30:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E06-T04-R01, S01–S05 | `TestIntentCreateConfirm` (create/confirm/failure-retry/challenge/resolve/over-capture/cancel) | PASS | `command/payment.go`, `payment_test.go` |
| E06-T04-R02, S06 | `TestRefundCreate` (linked success, exceeds-remainder, window expiry, processor-failed record) | PASS | `command/refund.go`, `refund_test.go` |
| E06-T04-R03, S07 | `TestPayoutCreate` (eligible stages; 3 blocked reasons, no record) + `TestPayoutCancel` | PASS | `command/payout.go`, `payout_test.go` |
| E06-T04-R04, S08 | `TestTopupCreate` (verified persists; unverified rejected; cancel-once) | PASS | same as above |
| E06-T04-R05 | `TestWebhookEventNames` (§10 names + id/status/original payload keys) | PASS | `command/payout_test.go` |
| Reads/DTOs | Delegation + mapping tests | PASS | `query/payment_refund_payout_test.go`, `dto/payment_refund_payout_test.go` |
| All | `make lint` (0 issues) + `go test -race ./...` (zero races) + full validator | PASS/OK | — |

## Commands

```text
$ gofmt -s -l internal/ test/    # empty
$ goimports -l -w .               # clean
$ make generate-mocks            # exit 0 (PaymentQuery Actor + PayoutRequest fields regenerated)
$ make lint                       # 0 issues (exit 0; shared payload keys, gatePayout extraction, actor-param removal)
$ go vet ./...                    # exit 0
$ go test ./internal/application/... -count=3                # pass
$ go test -race ./... -count=1                               # all packages ok, zero races
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes  # OK
```

## Deviations and Residual Risk

- Additive port fields: `PaymentQuery.Actor` (cancel authz) + `PayoutRequest.DestinationVerified/TenantAgeDays` (eligibility inputs) + E06-T12 packet amendment notes.
- Live processor/sandbox (E10), settlement workers (E14), provider void on cancel: documented follow-ups; cancel is local state + fact.
- Top-up settlement events come from the bank-settlement path (E10/E14); creation stages no webhook (catalog has no created event).
- Money-movement path: PR review by repo-owner required before merge (packet approval note).

## Audit fixes (2026-09-15, owner-ordered)

- All webhook names versioned (`.v1`); payout cancel stages no fact (catalog has no cancel event).
- `ConfirmIntent`/`CompleteChallenge` replay completed operations on the same key (op-tagged fingerprints incl. amount/outcome); `transitionIntent` reload-compare guards concurrent transitions; `ChargeRequest.IntentID` added for provider-side dedupe (+ T12 amendment).
- Refund validates before charge plus an atomic recheck inside persist (concurrent-refund safe).
- Payout eligibility/balance reads moved inside `Do`; payout schedule GET/PUT handlers; list handlers for intents/refunds/payouts/topups.
- Re-verified: `make lint` 0 issues, `go test -race ./...` green, G3 PASSED, validator OK after claim release.
