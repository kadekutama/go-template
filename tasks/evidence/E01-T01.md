# Verification Evidence: E01-T01

**Task:** E01-T01  
**Commit:** branch `feat/E01-T01-gomod-pins` from `559f045a20ab087793e72553de6fcd2928189c2d` (merge to `main` on completion)  
**Environment:** Linux x86_64, Go 1.27.1 (`/tmp/opencode/go-sdk/go/bin`, no system Go on host), govulncheck v1.8.0 (DB 2026-09-10), golangci-lint v2.13.2, python3 validator  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-14T01:42:34Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E01-T01-R01, S04 | `go list -m` inventory of 22 direct modules (table below) | pass | `go.mod` + `go.sum` (170 lines) |
| E01-T01-R02, S01 | anchor `internal/shared/deps` builds; consecutive `go mod tidy` runs produce no new diff | pass | `internal/shared/deps/deps.go`, `go build ./...` exit 0 |
| E01-T01-R03, S01 | `go mod tidy && go mod verify` | pass, exit 0 | `all modules verified` |
| E01-T01-R04/R04a, S03 | `govulncheck ./...` full report triaged (12 findings → 1 recorded residual; OTel upgraded to v1.40.0; testcontainers deferred) | dispositioned, exit 3 with 1 residual | report + triage below |
| E01-T01-R05, S02 | `check-tasks.py --versions` | pass, exit 0 | validator output |
| E01-T01-R06, S01 | `go build ./...`, `go vet ./...`, `gofmt -l` clean, `./scripts/build/build-all.sh` (5 binaries), `make lint` | pass, exit 0 | `bin/` (5 binaries), lint `0 issues` |
| All | `check-tasks.py --format --graph --sdd` | pass, exit 0 | validator output |

## Final version inventory (`go list -m`)

Exact SPEC pins (15): fx v1.24.0, echo/v5 v5.3.1, gqlgen v0.17.94,
gorm v1.31.2, migrate/v4 v4.19.1, ristretto/v2 v2.4.2 (`dgraph-io` path),
go-redis/v9 v9.22.0, nats.go v1.53.1, jwt/v5 v5.3.1, casbin/v2 v2.8.0,
open-feature/go-sdk v1.17.2, gobreaker v0.5.0, validator/v10 v10.22.0,
gocron v1.5.0, koanf/v2 v2.3.0.
SPEC-minimum satisfied (1): protobuf v1.36.11 (SPEC `v1.34.0+`, via gqlgen).
Recorded upgrades (3): grpc v1.74.2 (SPEC v1.66.0; forced by migrate v4.19.1's
go.mod — proxy .mod verified), oauth2 v0.30.0 (SPEC v0.23.0; forced by migrate
v4.19.1's go.mod), OTel family v1.40.0 (SPEC v1.26.0; GO-2026-4394 security
upgrade per packet R04a).
Substitution (1): go-i18n/v2 v2.6.1 (SPEC v2.7.0 exists on neither proxy nor
direct; latest available — bump when released).
Bootstrap pin, no SPEC version (1): testify v1.11.1.

## Commands

```text
$ export PATH=/tmp/opencode/go-sdk/go/bin:$HOME/go/bin:$PATH
$ go get <each table pin>… && go mod tidy && go mod verify   # exit 0, all modules verified
$ go build ./... && go vet ./...                              # exit 0
$ ./scripts/build/build-all.sh                                # exit 0, bin/{rest-api,grpc-api,graphql-api,cron,consumer}
$ make lint                                                   # 0 issues, exit 0
$ govulncheck ./...                                           # exit 3, 1 recorded residual (below)
$ python3 tasks/scripts/check-tasks.py --versions             # pinned versions checked, exit 0
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd # OK, exit 0
```

## Vulnerability triage (govulncheck v1.8.0, DB 2026-09-10)

Initial scan: 12 findings / 4 modules. Final scan: 1 finding / 1 module.

- UPGRADED: `go.opentelemetry.io/otel/sdk` GO-2026-4394 (arbitrary code
  execution via PATH hijacking, reachable via `sdk.init`) — OTel family
  `v1.26.0 → v1.40.0` (only fixed version; OTel 1.x API-stable, E01-T05
  unaffected). Cascade-free (verified: no other selection moved).
- DEFERRED with module: testcontainers cluster (10 findings: containerd v1.7.18
  ×8 incl. three with no fixed version, docker v27.1.1 ×2 with no fixed
  version) — module deferred to E07-T09 with its consuming harness; packet
  Out of Scope records rationale.
- RECORDED RESIDUAL: `google.golang.org/grpc` GO-2026-6061 (xDS RBAC + HTTP/2
  transport server; fixed upstream only in v1.82.1). No SPEC-compatible fix:
  v1.82.1 requires otel v1.43.0 + oauth2 v0.36.0, breaking those pins.
  Mitigations: template enables no xDS (no bootstrap resources; E12 MUST NOT
  enable xDS without upgrading grpc); gRPC server exposure begins at E12, which
  MUST re-run govulncheck and upgrade grpc then; release gate G8 / E19-T01
  enforces vuln-zero before release.

## Dependency-update record (per `SPEC.md §2`)

1. grpc `v1.66.0 → v1.74.2` and oauth2 `v0.23.0 → v0.30.0`: forced by MVS from
   `golang-migrate/migrate/v4@v4.19.1`'s own go.mod (proxy .mod verified
   2026-09-14). SPEC pins for these two modules are stale relative to the
   migrate pin; resolved upward per packet invariants.
2. OTel family `v1.26.0 → v1.40.0`: security upgrade (GO-2026-4394); no ADR
   (same component, 1.x API-compatible, packet-owned reversible decision).
3. go-i18n `v2.7.0 → v2.6.1`: SPEC version does not exist (proxy + direct
   checked); follow-up: bump when v2.7.0 releases (E18 dependency review).
4. testcontainers: version choice deferred to E07-T09 (see triage above).
5. Incident during implementation (recovered): plain `go get grpc@v1.69.0`
   silently downgraded migrate v4.19.1 → v4.17.1; caught by the packet's
   `go list -m` inventory check and corrected upward. Lesson recorded in packet
   invariants (upward-only resolution).

## Deviations and Residual Risk

- Validator observation (NOT changed here; E00-T08 owns `check-tasks.py`):
  `--versions` only enforces modules whose `mod` string literally appears in
  the SPEC §2 table text, so `golang-migrate/migrate/v4` is silently skipped
  (table says `golang-migrate`). Follow-up suggested for the E00-T08 owner:
  match on the version-adjacent name instead. The migrate downgrade above was
  caught by this task's own inventory check, not the validator.
- Residual risk: grpc GO-2026-6061 (see triage; owners E12 then E19-T01).
- Residual risk: go-i18n/testcontainers follow-ups noted above (owners: E18
  dependency review, E07-T09).
