# Verification Evidence: E05-T02

**Task:** E05-T02
**Commit:** branch `feat/E05-tenancy` from `bfb34b014234973573db18ecaa961aadc090eb7a` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain
**Verified By:** antigravity-agent (audit review & hardening of opencode / Muse Spark baseline)
**Verified At:** 2026-09-15T04:30:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E05-T02-R01, S01–S02 | `TestTenantLinkValidate`, `TestValidateHierarchy` (cycle, self-parent, duplicate edges, deterministic traversal, at-limit pass, limit+1/deep fail) | PASS | `internal/domain/entity/tenant_hierarchy_test.go`, `internal/domain/entity/tenant_hierarchy.go` |
| E05-T02-R02, S03, S05 | `TestConsolidateBalances` (same-asset, FX roll-up 20850 hard-coded half-up, whitespace asset checks, nil grants map, empty/missing/mismatched/stale/overflow/negative) | PASS | `internal/domain/service/consolidation_test.go`, `internal/domain/service/consolidation.go` |
| E05-T02-R03, S04, S06 | `TestValidateTransferGrant`, `TestHasTransferGrant`, `TestValidateReadGrant`, `TestHasReadGrant` | PASS | same as above |
| E05-T02-R04 | guard `TestDomainHasNoExternalImports` | PASS | `test/unit/domain/guard_test.go` |
| All | `go test ./internal/domain/entity/ ./internal/domain/service/ -count=1` | exit 0 | shell output |
| All | `make lint` | 0 issues | shell output |

## Commands

```text
$ go test ./internal/domain/entity/ -run TestValidateHierarchy -count=1
ok

$ go test ./internal/domain/service/ -run TestConsolidat -count=1
ok

$ go test ./internal/domain/service/ -run TestTransferGrant -count=1
ok

$ make lint
0 issues.

$ CGO_ENABLED=1 CC=clang go test -race ./internal/domain/... -count=1
ok all 7 packages (entity 97.1%, service 96.0%)
```

## Deviations and Residual Risk

- Grants are `map[childID]bool` (frozen shape for E03-T09); persistence FKs and grant storage live in E07. FX uses supplied `FxRate` at `At` via E03-T05 `Convert`; fetching lives in E10.
- Depth limit `MaxTenantHierarchyDepth = 5` (nodes, documented v1 policy); E03-T09 consumes the grant API as specified.
- Hardening & Audit Revisions (2026-09-15):
  - Deterministic DAG traversal: nodes and adjacency children are sorted lexicographically so traversal order is completely stable across runs.
  - Duplicate edge deduplication handled gracefully without false cycles.
  - Hardened consolidation against whitespace base assets, child assets, and nil grants maps.
  - Strict table-driven unit tests complying with `AGENTS.md` (locally scoped testCase structs, exact parameter names, inline IIFE mutations, parent `t.Parallel()` only).
