# Verification Evidence: E06-T03

**Task:** E06-T03
**Commit:** branch `feat/E06-application` from `a23e32e70cca5e32cb6096d76f33c47b82c6f1ec` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain (`$HOME/.pixi/bin` on PATH)
**Verified By:** muse-spark
**Verified At:** 2026-09-15T09:50:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E06-T03-R01, S01, S02, S07 | `TestTransferCreate` (funded/underfunded) + `TestReverseTransaction` (linked correction, replay, reason, unknown original) + `TestPostTransactionDelegates` | PASS | `command/transfer.go`, `transaction.go`, tests |
| E06-T03-R02, S03, S04 | `TestTransferCreate` (scheduled PENDING, no funds check) + `TestTransferExecute` (execute on funds, fail-record without, replay, canceled) | PASS | same as above |
| E06-T03-R03, S05, S06 | `TestTransferBatch` (>1000 / mixed-tenant rejected pre-intake; COMPLETED all-ok; PARTIAL with per-item codes + sibling intact) | PASS | same as above |
| E06-T03-R04, S08 | Replay/conflict cases; DTO mapping tests (parity fields) | PASS | `dto/{transfer,batch,transaction}_test` |
| Reads | Delegation tests via generated mocks | PASS | `query/transfer_transaction_test.go` |
| All | `make lint` (0 issues) + `go test -race ./...` (zero races) + full validator | PASS/OK | — |

## Commands

```text
$ gofmt -s -l internal/ test/    # empty
$ goimports -l -w .               # clean
$ make generate-mocks            # exit 0 (TransferQuery Actor addition regenerated)
$ make lint                       # 0 issues (exit 0; extracted reverseInTx/executeInTx/failExecution for gocyclo ≤ 12)
$ go vet ./...                    # exit 0
$ go test ./internal/application/... -count=3                # pass
$ go test -race ./... -count=1                               # all packages ok, zero races
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes  # OK
```

## Deviations and Residual Risk

- Additive domain loader `aggregate.LoadPosting` (pure rehydration; packet-noted; no E02 behavior change).
- Additive port field `TransferQuery.Actor` (+ E06-T12 packet amendment note) so `CancelTransfer` authorizes.
- `BuildPostingEntries` exported from T13's `posting.go` for transfer construction reuse (additive, no caller change).
- Batch item records persist best-effort; intake pinning makes item failures non-fatal by design.
- Money-movement path: PR review by repo-owner required before merge (packet approval note).

## Audit fixes (2026-09-15, owner-ordered)

- Batch replay convergence: intake replay rebuilds PENDING intents and re-executes with original `batchID:index` keys; close state recomputed from stored outcomes; `transfer.batch.completed.v1` staged idempotently. Fixed a real index bug (converged runs re-indexed from 0) with a partial-convergence regression test.
- `completeTransfer` duplicate fallback removed (duplicates fail loudly; fakes stage atomically).
- Same-asset guard (`CROSS_CURRENCY_UNSUPPORTED`) instead of opaque construction failure; posting IDs namespaced (`post:<transferID>`).
- Envelope/reads moved inside `Do` after replay check (data-flow §2 order); `ExecuteTransfer`/`CancelTransfer` replay-before-state inside the unit.
- Fingerprints cover all committed fields (asset on batch items, schedule fields).
- `TransferQuery.Actor` + `BuildPostingEntries` export noted in T12 packet.
- Re-verified: `make lint` 0 issues, `go test -race ./...` green, G3 PASSED, validator OK after claim release.
