# Verification Evidence: E03-T02

**Task:** E03-T02
**Commit:** branch `feat/E03-money-movement` from `57de5c86cbc31bb73d69e604a4b93d3783b7285a` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1 (`/tmp/opencode/go-sdk/go/bin`)
**Verified By:** opencode-agent (opencode / Muse Spark)
**Verified At:** 2026-09-14T07:10:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E03-T02-R01–R07, S01–S05 | `TestValidateRefund` (13-row table) | PASS | `internal/domain/service/refund_service_test.go` |
| E03-T02-R08 | guard test | PASS | shell output |
| All | `go test ./internal/domain/... -count=3` | exit 0 | shell output |

## Commands

```text
$ go test ./internal/domain/... -count=1
PASS all 7 packages
```

## Deviations and Residual Risk

- Fee refunds explicit via `FeeRefundPolicy`; proportionality never assumed per ledger-core §6.4.

## Follow-up audit (2026-09-14, same owner/session, claim reopened per SDD §6)

- `RefundLines` no longer collapses all legs into one account: `RefundRequest` now carries `MerchantPayable`/`RefundsPayable`/`CashAccount`, enforced pairwise distinct, all ACTIVE with matching asset (ledger-core §6.4 legs); packet R05/R08/S05.
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 94.2%; `go vet`/`gofmt` clean; `check-tasks.py --format --graph --sdd` OK.

## Exact-shape rewrite + ctx revert (2026-09-14, same owner/session, claim reopened per SDD §6)

- Reverted the mistaken `_ context.Context` threading from `ValidateRefund`; signature is ctx-free again. Test rewritten to the exact shape (local `testCase`, param-named fields, full expected errors, one list/loop/`t.Run`).
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 96.6%; `go vet`/`gofmt` clean.

## Normative-policy compliance (2026-09-14, same owner/session, claim reopened per SDD §6)

- `TestValidateRefund` subtests now run parallel (`tc := tc; t.Parallel()`); fixture map is read-only — `-race` clean.
- Mandatory protocol: `gofmt -s` clean, `make lint` 0 issues, `CGO_ENABLED=1 CC=clang go test -race ./internal/domain/... -count=1` green, service coverage 97.7%, `gate-check.sh G2` PASSED, `check-tasks.py --format --graph --sdd --specs --events --codes` OK.
