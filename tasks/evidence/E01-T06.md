# Verification Evidence: E01-T06

**Task:** E01-T06  
**Commit:** branch `feat/E01-platform-core` from `559f045a20ab087793e72553de6fcd2928189c2d` (uncommitted per owner instruction; PR review pending)  
**Environment:** Linux x86_64, Go 1.27.1, oklog/ulid v2.1.2, yaml.v3 v3.0.1, go-i18n v2.6.1  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-14T02:20:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E01-T06-R01, S01 | `TestNewRejectsUnregisteredCode` + duplicate/malformed `Register` panics | PASS | `kernel/error/error_test.go` |
| E01-T06-R02, S02 | `TestTranslateEnAndId` (EN vs ID strings) | PASS | same suite + `locale/*.yaml` |
| E01-T06-R03, S03 | `TestCursorRoundTrip` + `TestCursorRejectsForgery` + bounds tests | PASS | `kernel/pagination/*_test.go` |
| E01-T06-R04, S04 | `TestGoRecoversPanicAndReports` + `TestGoSilentOnSuccess` | PASS | `kernel/safe/go_test.go` |
| E01-T06-R05, S05 | `TestFixedClockReplaysTimes` + `TestULIDUniqueAndOrdered` (100 IDs) | PASS | `kernel/kernel_test.go` |
| E01-T06-R06, S06 | `TestRunDrainsOnCancel` + `TestRunReturnsServeErrorImmediately` | PASS | `kernel/shutdown/*_test.go` |
| All | `go build ./... && go vet ./internal/shared/...` | exit 0 | shell output |
| All | `check-tasks.py --format --graph --sdd` | pass, exit 0 | validator output |

## Commands

```text
$ go test ./internal/shared/...        # all ok (di, kernel, error, pagination, safe, shutdown)
$ go build ./... && go vet ./internal/shared/...  # exit 0
```

## Deviations and Residual Risk

- New pins recorded: oklog/ulid/v2 v2.1.2, yaml.v3 v3.0.1 (go-i18n needs an
  explicitly registered YAML unmarshaler — verified against both message forms).
- Embed confinement: catalogs stay at epic paths `internal/shared/locale/`;
  the `locale` package owns the FS, `apperror` consumes it.
- Cursor checksum is CRC interim; E09 may upgrade to HMAC (packet records).
- i18n blank pruned from anchor (real import); testify stays anchored (no test
  imports it yet).

## Amendment 2026-09-14 (ADR-012, owner-directed: ULID → UUIDv7)

- `id.go` now implements `IDGenerator` with UUIDv7 (`google/uuid` v1.6.0,
  indirect → direct); `oklog/ulid` removed from `go.mod` by tidy (verified absent).
- `TestULIDUniqueAndOrdered` replaced with `TestUUIDv7UniqueAndVersioned`
  (100 unique, parse-valid, version-7 bits). Same-ms monotonicity is knowingly
  lost (UUIDv7 sub-ms bits are random) — recorded consequence for any consumer
  relying on ID ordering.
- Re-verified: `go build/vet/test`, `make build-all`, `make lint` (0 issues),
  `govulncheck` (0 findings), validator `--format --graph --sdd` (exit 0).
