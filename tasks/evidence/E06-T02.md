# Verification Evidence: E06-T02

**Task:** E06-T02
**Commit:** branch `feat/E06-application` from `a23e32e70cca5e32cb6096d76f33c47b82c6f1ec` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain (`$HOME/.pixi/bin` on PATH)
**Verified By:** muse-spark
**Verified At:** 2026-09-15T09:10:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E06-T02-R01, S01–S03 | `TestAccountOpen` (create/replay/conflict/validation/denial) + `TestAccountLifecycle` (double-freeze, unfreeze-active, close, stale version) | PASS | `command/account.go`, `account_test.go` |
| E06-T02-R02, S05 | `TestTenantProvision` (create/replay/taken-name/bad-region/denial) + `TestTenantSettingsUpdate` (current/stale version) | PASS | `command/tenant.go`, `tenant_test.go` |
| E06-T02-R03, S04, S06 | Replay/conflict/denial cases across account + tenant tables | PASS | same as above |
| E06-T02-R04 | `TestToAccountDTO` / `TestToTenantDTO` + `TestAccountQueryDelegation` / `TestTenantQueryDelegation` (mock-backed) | PASS | `dto/`, `query/` tests |
| S01 (event fact) | Outbox assertions in open/provision tests | PASS | `account_test.go`, `tenant_test.go` |
| All | `make lint` (0 issues) + `go test -race ./...` (zero races) + full validator | PASS/OK | — |

## Commands

```text
$ gofmt -s -l internal/ test/    # empty
$ goimports -l -w .               # clean
$ make lint                       # 0 issues (exit 0; extracted tenant runner to satisfy gocyclo ≤ 12)
$ go vet ./internal/application/...                          # exit 0
$ go test ./internal/application/... -count=3                # pass
$ go test ./internal/application/command/ -race -count=5     # pass (concurrency fixed: winner-ID assertion)
$ go test -race ./... -count=1                               # all packages ok, zero races
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes  # OK
```

## Deviations and Residual Risk

- Additive domain loader `aggregate.LoadAccount` (pure rehydration, empty events; no behavior change). Recorded in packet Change Surface; no E02 test changes (covered via handler tests).
- Additive port field `UpdateTenantSettingsRequest.IdempotencyKey` (+ amendment note in E06-T12 packet).
- Account writes go through the domain repository directly; E07 binds all stores into the commit transaction (same ambient-tx note as E06-T13).
- `TenantStore` is consumer-owned in `command` (go-conventions consuming-boundary rule); E07-T10 implements it.

## Audit fixes (2026-09-15, owner-ordered)

- `OpenAccountRequest.Metadata` added (was silently dropped; aggregate supports it) + T12 amendment note; fingerprint now covers purpose + metadata.
- Tenant settings update stages no webhook (catalog carries `tenant.created` only).
- `ListTenants` handler + fake; fingerprint covers settings JSON.
- Re-verified: `make lint` 0 issues, `go test -race ./...` green, G3 PASSED, validator OK after claim release.
