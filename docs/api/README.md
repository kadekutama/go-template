# API Documentation

Public contracts and transport adapters: REST (Echo), gRPC, GraphQL, workers,
and versioning (`api/` contracts plus interface adapters). See
[SPEC.md §3](../../SPEC.md) (architecture layers). Content is filled by E18;
implementation lives in E11–E14.
