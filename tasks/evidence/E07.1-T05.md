# Verification Evidence: E07.1-T05

**Task:** E07.1-T05
**Commit:** branch `feat/E07.1-distributed-persistence` from `0aedc3838e85aa0948e547070ede9df5e7636225` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Citus image `citusdata/citus:14.0`, Rancher Desktop daemon
**Verified By:** muse-spark
**Verified At:** 2026-09-17T10:30:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E07.1-T05-R01 (isolated Citus fixture, pinned image, skip w/o Docker) | `StartCitus` boots coordinator+worker on isolated network; `SkipIfNoDocker` first call | PASS | `test/testcontainers/citus.go` |
| E07.1-T05-R02, S01 (co-location) | Probe tables share one colocation group (`pg_dist_partition` single `colocationid`); worker registered+active; ledger distribution state logged | PASS | `TestCitusShardingMetadata` 13.97s |
| E07.1-T05-R03, S02 (cross-tenant aggregation) | Two-tenant ledger seed: per-tenant DEBIT=CREDIT (5000/3000), grand total 8000 | PASS | `TestCitusTenantAggregation` 11.66s |
| E07.1-T05-R04, S03 (RPO=0 kill/restart) | SIGKILL coordinator → restart → all committed rows present, version intact | PASS | `TestCitusCrashRecovery` 15.01s |
| E07.1-T05-R05, S04 (race + vet + skip posture) | `-race` full suite green; `go vet` clean; Docker-less runs skip via harness | PASS | commands below |
| T01-S06 (migration applies on Citus, no txn-block conflict) | `runner.Up` → 20260901000005 on Citus coordinator in every test setup | PASS | `openCitus` helper |

## Commands

```text
$ go vet ./test/testcontainers/... ./test/integration/citus/...
ok (exit 0)

$ CGO_ENABLED=1 CC=clang go test -race -count=1 ./test/integration/citus/...
--- PASS: TestCitusShardingMetadata (13.97s)
--- PASS: TestCitusTenantAggregation (11.66s)
--- PASS: TestCitusCrashRecovery (15.01s)
ok (0 races)

$ CGO_ENABLED=1 CC=clang go test -count=1 -run TestHarnessSmoke ./test/testcontainers/...
--- PASS: TestHarnessSmoke (8.39s)
```

## Deviations and Residual Risk

- Empirical Citus finding: of the 6 ADR tables, only `idempotency_records` distributed (`PRIMARY KEY (tenant_id, key)` already includes the distribution column). The other five have FKs not covering `tenant_id`, so Citus declines and the migration's per-table EXCEPTION path logs NOTICE — no failure, schema untouched. Recorded in test output (`ledger distribution state: map[idempotency_records:h]`). Full FK-inclusive sharding needs composite-key redesign (new task, touches E07 ledger-core semantics; not smuggled in here). *Addendum (2026-09-18, E07.1-T06):* Following composite PK modernization, Citus distributes 3–4 tables: `idempotency_records`, `outbox_events`, and `audit_refs` always distribute, while `ledgers` (first in the migration loop) distributes when worker-sync completes before the loop begins or takes the resilient NOTICE path if worker registration is still settling.

- `CitusHandle.Refresh` added after measurement: this runtime reassigns auto-allocated host ports across stop/start (observed 33594→33595); boot-time DSNs go stale while the container is healthy. The crash test refreshes endpoints post-restart and opens fresh pools.
- `TestHarnessSmoke` failed once at 120s when run in the same `go test` invocation as the Citus suite (parallel heavy clusters starved the daemon); it passes standalone in 8.4s. Suites should run as separate commands in CI until runner sizing is addressed (E17 note).
- Packet T05 amended before implementation (R02/S01 probe-table mechanism) per SDD §3.3; no requirement weakened.
