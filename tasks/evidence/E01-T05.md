# Verification Evidence: E01-T05

**Task:** E01-T05  
**Commit:** branch `feat/E01-platform-core` from `559f045a20ab087793e72553de6fcd2928189c2d` (uncommitted per owner instruction; PR review pending)  
**Environment:** Linux x86_64, Go 1.27.1, OTel family + OTLP exporter v1.46.0, grpc v1.83.1, oauth2 v0.36.0  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-14T02:40:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E01-T05-R01/R02, S01 | `TestPropagateKeepsTraceIDStrict` (inject→extract, IDs equal) | PASS | `tracing/otel_test.go` |
| E01-T05-R03, S02/S03 | `TestBootstrapRejectsBadEndpoint` + `TestBootstrapRequiresEndpointInProduction` + identity test | PASS | same suite |
| E01-T05-R04/R05, S04/S05 | `TestBootstrapLocalWithoutEndpoint` (exporting=false, shutdown flush) + `TestRecordErrorMarksSpan` (Error status + event) | PASS | same suite |
| All | `go build ./... && go vet ./...` + full unit suite | exit 0 | shell output |
| All | `govulncheck ./...` | **exit 0 — no vulnerabilities found** | report |
| All | `check-tasks.py --format --graph --sdd` | pass, exit 0 | validator output |

## Commands

```text
$ go test ./internal/shared/kernel/trace/ ./internal/infrastructure/tracing/ -v  # 6/6 PASS
$ go build ./... && go vet ./...          # exit 0
$ govulncheck ./...                       # exit 0, no vulnerabilities found
```

## Dependency-update record (extends E01-T01)

OTel family + OTLP exporter resolved to **v1.46.0** (2026-08-25 release),
grpc to **v1.83.1**, oauth2 to **v0.36.0** — all upward-only MVS resolution:
the v1.40.0 exporter pin pulled grpc v1.78+ whose chain selects trace v1.46.0,
and trace v1.46.0 changed `Span.RecordError` (ctx removed), which mixed minors
cannot satisfy. The kernel port drops the redundant ctx parameter to match
upstream. Side effect: grpc v1.83.1 ≥ v1.82.1 **closes GO-2026-6061**
(the E01-T01 recorded residual) — the tree is now vuln-zero.

## Deviations and Residual Risk

- gRPC-metadata/NATS carriers deferred to E12/E14 (thin, transport-owned).
- Bootstrap sets OTel globals per upstream guidance (sanctioned exception, packet).
- otel/otel-sdk anchor blanks pruned (real imports).
- No residuals: G8/E19-T01 vuln-zero gate inherits a clean tree.
