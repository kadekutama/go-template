# Verification Evidence: E02-T01

**Task:** E02-T01  
**Commit:** branch `feat/E02-ledger-domain` from `8f3107d2a36d8f69f2483fc045caccb6ffa3146e` (uncommitted per owner instruction; PR pending)  
**Environment:** Linux x86_64, Go 1.27.1 (`/tmp/opencode/go-sdk/go/bin`, no C compiler in this shell)  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-14T03:32:32Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E02-T01-R01, S01 | `TestNewBaseEventAccessors` (all 9 accessors, versioned type, UTC) | PASS | `internal/domain/event/base_test.go` |
| E02-T01-R03, S02 | `TestNewBaseEventValidationMatrix` (8 invalid dimensions, error + no panic) | PASS | same suite |
| E02-T01-R02/R04, S03 | `TestBaseEventImmutability` (input-map mutation + accessor-copy mutation) | PASS | same suite |
| R01 (UTC note) | `TestBaseEventOccurredAtNormalizedUTC` (non-UTC input → UTC instant) | PASS | same suite |
| E02-T01-R05–R06, S04–S06 | `TestAllAggregatesEveryViolationInOrder`, `TestAnyShortCircuitsOnFirstPass`, `TestAnyAggregatesWhenAllFail`, `TestNotInvertsChild`, `TestAllPassesWhenEveryChildPasses` | PASS | `internal/domain/specification/specification_test.go` |
| E02-T01-R07, S07 | `TestEmptySemantics` (`All()` passes, `Any()` one explicit violation) | PASS | same suite |
| E02-T01-R07–R08, S08 | `TestNilChildIsViolationNotPanic`, `TestNilCandidateDoesNotPanic` | PASS | same suite |
| E02-T01-R05, S09 | `TestConcurrentReuseIsRaceSafe` (8 goroutines × 50 iters, identical results) | PASS | same suite |
| Immutability follow-up | `TestViolationDetailsAreCopied` — FAILED on first run (stored `Details` map aliased), fixed by copying the violation at `NewFuncSpec`/`Not` construction; re-run PASS | PASS after fix | `specification.go` + same suite |
| All | `go test ./internal/domain/event ./internal/domain/specification -count=3` | exit 0, all PASS | shell output |
| All | `go test -count=1 -coverprofile` | event 97.1%, specification 98.0%, total 97.6% | `/tmp/opencode/e02t01.cov` (ephemeral) |
| E02-T01-R09 | `go list -deps ./internal/domain/...` dot-segment check → only `github.com/kadekutama/go-template/` internal + std; gate-check.sh G2 external-module grep → clean | PASS | shell output |
| All | `go build ./...`, `go vet ./internal/domain/...`, `gofmt -l` | exit 0, clean | shell output |
| All | `check-tasks.py --format --graph --sdd` | pass, exit 0 (after handoff written) | validator output |

## Commands

```text
$ gofmt -l internal/domain/                      # clean (one test file reformatted during work)
$ go vet ./internal/domain/...                   # exit 0
$ go test ./internal/domain/event ./internal/domain/specification -count=3   # ok × 2
$ go test -count=1 -coverprofile=... ./internal/domain/...                  # 97.6% total
$ go list -deps ./internal/domain/... | grep "\." | grep -v "^github.com/kadekutama/go-template/" # empty
$ go build ./...                                 # exit 0
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd                # OK
```

## Deviations and Residual Risk

- `-race` NOT run in this shell: no C compiler (`go test -race` requires cgo). Same caveat as E01 close-out: concurrency proof here is the deterministic 8×50 shared-composite test (passes `-count=3`); `-race` must be confirmed in CI or a gcc-equipped env before the G2 gate can close. Recorded in handoff resume instructions.
- Backlog wording note: the literal guard `go list -deps ... | grep -v -E "^(internal/|std)"` cannot be empty for ANY Go package (std packages print bare names like `errors`, `context` with no `std` prefix). Proven instead via the enforceable equivalents: gate-check.sh G2 external-module grep (clean) + dot-segment check (only `github.com/kadekutama/go-template/` + std). No implementation change; packet quotes the backlog verbatim for traceability.
- One real bug found by the new tests (violation `Details` map aliased at construction) and fixed before completion; see row above.
