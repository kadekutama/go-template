# Verification Evidence: E03-T01

**Task:** E03-T01
**Commit:** branch `feat/E03-money-movement` from `57de5c86cbc31bb73d69e604a4b93d3783b7285a` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1 (`/tmp/opencode/go-sdk/go/bin`)
**Verified By:** opencode-agent (opencode / Muse Spark)
**Verified At:** 2026-09-14T07:00:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E03-T01-R01/R02/R09, S01/S06/S07 | `TestValidateImmediate` (13-row table) | PASS | `internal/domain/service/transfer_service_test.go` |
| E03-T01-R03, S02 | `TestValidateSchedule` + `TestValidateExecution` | PASS | same |
| E03-T01-R04, S05 | `TestExpandRecurrence` | PASS | same |
| E03-T01-R05, S03 | `TestEvaluateBatch` + `TestBatchItemKey` | PASS | same |
| E03-T01-R06 | `TestValidateTemplate` + `TestApplyTemplate` | PASS | same |
| E03-T01-R07, S04 | `TestValidateImmediate` (fx rows) | PASS | same |
| E03-T01-R08 | guard test + `go list -deps` inspection | PASS | shell output |
| All | `go test ./internal/domain/service/ -count=3` | exit 0 | shell output |

## Commands

```text
$ go test ./internal/domain/service/ -run 'TestValidateImmediate|TestCrossCurrency|TestSchedule|TestExpandRecurrence|TestBatchPartial|TestTransferTemplate' -count=1 -v
PASS ok (7 tests)
$ go test ./internal/domain/... -count=1
PASS (all packages, verified at T01 checkpoint; full re-run at epic close)
```

## Deviations and Residual Risk

- Cross-currency with FX passes T01 shape check; balanced lots enforced by E03-T05 FX service and posting construction (E02-T04).
- Schedule-time hold creation is policy-shaped (`HoldAtSchedule` flag); atomic hold+reservation at execution belongs to E07/E14.

## Follow-up audit (2026-09-14, same owner/session, claim reopened per SDD §6)

- `SELF_TRANSFER_REJECTED` added to `ValidateImmediate`/`ValidateSchedule` (journal no-ops must not post); packet R09/S07.
- `checkTransferAssets` simplified to an early-return shape; `itoa` replaced with `strconv.Itoa`; dead `HoldAtSchedule` field and ignored `ApplyTemplate` idempotency param removed.
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 94.2% (`-coverpkg`); `go vet`/`gofmt` clean; `check-tasks.py --format --graph --sdd` OK.

## Exact-shape rewrite + ctx revert (2026-09-14, same owner/session, claim reopened per SDD §6)

- Reverted the mistaken `_ context.Context` threading from all T01 production code; signatures are ctx-free again. Tests rewritten to the exact shape: per-function local `testCase` (fields named after params, real types, `expectedError` only where returned), one list + one loop + one `t.Run`.
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 96.6%; `go vet`/`gofmt` clean.

## Normative-policy compliance (2026-09-14, same owner/session, claim reopened per SDD §6)

- Parent tests invoke `t.Parallel()` with clean, sequential, deterministic subtests (no `tc := tc`, no inner `t.Parallel()`; 82 runners across `service` + per-package `entity`/`valueobject` suites). Fixtures are loop-external or per-row owned; domain functions are pure — `-race` clean.
- Mandatory protocol: `gofmt -s` clean, `make lint` 0 issues, `CGO_ENABLED=1 CC=clang go test -race ./internal/domain/... -count=1` green, per-package coverage entity 96.2% / valueobject 99.3% / service 97.7%, `gate-check.sh G2` PASSED, `check-tasks.py --format --graph --sdd --specs --events --codes` OK.
