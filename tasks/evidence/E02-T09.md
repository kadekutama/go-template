# Verification Evidence: E02-T09

**Task:** E02-T09  
**Commit:** branch `feat/E02-ledger-domain` from `8f3107d2a36d8f69f2483fc045caccb6ffa3146e` (uncommitted per owner instruction; PR pending)  
**Environment:** Linux x86_64, Go 1.27.1 (`/tmp/opencode/go-sdk/go/bin`, no C compiler in this shell)  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-14T03:38:49Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E02-T09-R01, S01 | `go test ./internal/domain/... -count=3` all 7 pkgs PASS; coverprofile total 96.1% (min pkg 92.5%) | PASS | shell output |
| E02-T09-R02, S02 | `TestDomainHasNoExternalImports` PASS + negative control (temp `go.uber.org/fx` import → FAIL, removed → PASS) | PASS | `test/unit/domain/guard_test.go` |
| E02-T09-R03, S02 | `TestLedgerSlice` (open → construct → specs → reverse → event counts) | PASS | `test/unit/domain/slice_test.go` |
| Property | `TestAllocateLargestRemainderExactness` (1000 seeded) + `TestMoneyAddAssociativitySeeded` (500) | PASS | `allocation_property_test.go` |
| E02-T09-R04, S03 | `./scripts/test/unit.sh` exit 0 (internal + pkg + test/unit) | PASS | shell output |
| E02-T09-S04 | `check-tasks.py --format --graph --sdd --specs --events --subjects --codes` | all OK | validator output |
| E02-T09-R05 | file list: test/unit/domain (3), unit.sh, PROGRESS/epic/claims, tasks/*T09, amended money.go + money_test.go + E02-T02 packet | PASS (recorded exception) | `git status` |

## Commands

```text
$ go test ./internal/domain/... -count=3
$ go test ./internal/domain/... -coverprofile=...   # total 96.1%
$ go test ./test/unit/domain/ -count=1 -v            # 4/4 PASS
$ ./scripts/test/unit.sh                             # exit 0
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --subjects --codes
```

## Deviations and Residual Risk

- `-race` not runnable in this shell (no gcc); G2 keeps this plus `generate-mocks` (E06) and E03–E05 specs outstanding — G2 stays `pending`.
- Codec-seam conflict found by this task's `unit.sh` run: `TestCodecSeam` (E01-T07) bans `encoding/json` outside `pkg/jsonparser`, which the original E02-T02 `MarshalJSON` design violated. Resolved per SDD precedence (accepted E01 packet wins): domain keeps the codec-free `MoneyDTO` shape; E02-T02 packet amended (R05/interfaces/verification/notes) by the same owner in-session; `unit.sh` + `TestCodecSeam` now green. No E01 files touched; no seam exception added.
- ID-generation duplication found on owner review: domain-local UUIDv7 generator duplicated kernel logic. Resolved via DIP: domain owns the `IDGenerator` port, kernel implements it implicitly, `ReversePosting` takes it as a param; `New*` constructors deleted, tests use deterministic stubs. Packets E02-T02 (R06/interfaces/verification/notes) and E02-T04 (R05/signature/notes) amended; full suite re-run green (recorded here).
- Cross-harness reconciliation & governance resolution (2026-09-14, owner-ruled accept):
  1. Second-harness E02 revision absorbed after independent verification (unit 20 pkgs, coverage 96.1%, lint 0, validator all-OK). Genuine fix kept: Hare-Niemeyer allocation. "141 linter issues" not re-runnable post-overwrite but credible per checkable citations (see E02-T02 evidence); 0-issue after-state independently confirmed under repo gate and strict probe. Follow-ups completed: §2.13 corrected, E02-T02 packet R07/S08/notes updated.
  2. Canonical module import migration: All occurrences of `example.com/go-template` updated to `github.com/kadekutama/go-template` across all Go code, configs, and tasks.
  3. Secondary audit and hardening across E00/E01: OTel sample ratio bounds rejection, config secret slice validation, error translator nil safety, shutdown lifecycle guards, and nil-safe trace/log context injection. Full test suite (24 packages) passes `go test -count=3 ./...` and `golangci-lint run ./...` reports 0 issues.
  4. Governance gap addressed: In response to Muse Spark 1.3 governance critique, the "shared filesystem ≠ permission" rule was formalized in `tasks/SDD.md §3.2, §6`, `docs/development/repository-governance.md §4.1`, `tasks/SDD-INTEROP.md`, and `AGENTS.md`. Working trees are frozen upon claim release, un-claimed edits are strictly blocked, and multi-harness physical worktree isolation is mandatory for Phase 3 (E03+).

## Review amendment: Unit Test Policy Refactoring & Verification (Gemini, 2026-09-14)

- Complete refactoring of all E02 core ledger unit test suites according to the repository's unit test conventions:
  - `internal/domain/valueobject/`: `account_type_test.go`, `currency_test.go`, `money_test.go`, `edges_test.go`, `ids_test.go`.
  - `internal/domain/entity/`: `account_test.go`, `journal_test.go`, `posting_test.go`.
  - `internal/domain/event/`: `base_test.go`, `catalog_test.go`.
  - `internal/domain/repository/`: `ports_test.go`.
  - `internal/domain/service/`: `period_service_test.go`.
  - `internal/domain/specification/`: `account_test.go`, `period_test.go`, `refund_test.go`, `specification_test.go`, `transaction_test.go`, `transfer_test.go`.
  - `internal/domain/aggregate/`: `account_test.go`, `hold_test.go`, `period_test.go`, `posting_test.go` (made `stubIDs` thread-safe with `sync.Mutex`, refactored `TestConstructPosting` and `TestReversePosting` to table tests).
- All suites feature locally-scoped `type testCase struct`, exact parameter/return naming, elimination of single-use template variables outside `testCases`, multi-line formatting, parent `t.Parallel()` with sequential deterministic subtests, and rigorous edge-case coverage.
- Full verification:
  - `export CGO_ENABLED=1 CC=clang`
  - `CGO_ENABLED=1 CC=clang go test -v -race ./internal/domain/...` passed with zero data races.
  - `make lint` passed with 0 issues.
  - `./tasks/scripts/gate-check.sh G2` PASSED with 97.4% domain statement coverage (minimum threshold 90%).
  - `python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes` reports `OK: structural checks passed`.

