# Verification Evidence: E05-T03

**Task:** E05-T03
**Commit:** branch `feat/E05-tenancy` from `bfb34b014234973573db18ecaa961aadc090eb7a` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain
**Verified By:** antigravity-agent (audit review & hardening of opencode / Muse Spark baseline)
**Verified At:** 2026-09-15T04:30:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E05-T03-R01, S01–S02 | `TestBuildBalanceKey`, `TestBuildBalanceCursorKey`, `TestParseBalanceKey`, `TestBuildIdempotencyKey`, `TestParseIdempotencyKey` (incl. segment-count error), `TestBuildRateLimitKey`, `TestParseRateLimitKey` (round-trip + prefix/count errors), `TestIsolationRoundTrip` | PASS | `internal/domain/service/isolation_test.go`, `internal/domain/service/isolation.go` |
| E05-T03-R02, S03 | `TestBuildSubject`, `TestParseSubject`, round-trip | PASS | same as above |
| E05-T03-R03, S04 | `TestRLSPolicyMatrix` (table presence + scope assertions: tenants self, ledgers tenant, inbox service, asset_registry shared-read/service), `TestRequireTenant` | PASS | same; matrix covers 11 §5 core tables |
| E05-T03-R04, S05 | `TestResidencySettingsValidate`, `TestWhiteLabelSettingsValidate` (incl. valid color/logo accept, short-hex/non-https reject) | PASS | same |
| E05-T03-R05 | Inspection `grep ADR-004 docs/architecture/README.md` | PASS | `docs/architecture/ADR-004-tenant-isolation.md` indexed |
| E05-T03-R06 | guard `TestDomainHasNoExternalImports` | PASS | `test/unit/domain/guard_test.go` |
| All | `make lint` | 0 issues | shell output |

## Commands

```text
$ go test ./internal/domain/service/ -run TestBuildBalanceKey -count=1
ok

$ go test ./internal/domain/service/ -run TestBuildSubject -count=1
ok

$ go test ./internal/domain/service/ -run TestRLSPolicyMatrix -count=1
ok

$ grep ADR-004 docs/architecture/README.md
| [ADR-004](./ADR-004-tenant-isolation.md) | Tenant isolation (shared DB + RLS) | Accepted |

$ make lint
0 issues.

$ CGO_ENABLED=1 CC=clang go test -race ./internal/domain/... -count=1
ok all 7 packages
```

## Deviations and Residual Risk

- Contract only; RLS SQL enforcement (E07-T02), NATS topology (E08), and REST endpoints (E11) implement against this matrix. Cache keys are hints; authority stays in PostgreSQL.
- Hardening & Audit Revisions (2026-09-15):
  - Replaced basic ASCII space check with `unicode.IsSpace` to reject all Unicode whitespace variants across subject tenants, event types, and residency regions.
  - Strict table-driven unit tests complying with `AGENTS.md` (locally scoped testCase structs, exact parameter names, inline IIFE mutations, parent `t.Parallel()` only).
