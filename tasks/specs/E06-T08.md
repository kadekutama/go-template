# Task Specification: E06-T08 — Application test suite (G3 gate)

**Task:** E06-T08
**Spec Status:** ready
**Author:** muse-spark
**Reviewer:** muse-spark (self-review recorded per `tasks/SDD.md §3.1`; verification task only, no product behavior, persistence, authorization, cryptographic, migration, or public-contract changes)
**Last Updated:** 2026-09-15

## Objective

Close the G3 gate: prove `go test ./internal/application/... -race -count=3` passes at ≥85% coverage with mocked ports, every api-contracts §7 group backed by handlers, and sagas crash-safe — then record the gate.

## Scope

### In Scope

- Coverage-gap tests to reach ≥85% across `internal/application/...` (reads, delegates, corrupt replays, validation matrices, overflow/bound edges).
- `test/unit/application/...` gate documentation if warranted (coverage profile as CI artifact — recorded here; CI wiring stays with E17).
- This packet, claim, evidence, handoff; epic + PROGRESS + GATES flips for E06/G3.

### Out of Scope

- New product behavior (all handlers/sagas landed in E06-T01–T07/T09–T13), adapter/integration suites (E07–E10 own G4), CI pipeline changes (E17).

## Normative Inputs

- `tasks/epics/E06-application.md#E06-T08` for the backlog boundary.
- `SPEC.md §10.2`, `SPEC.md §16` (coverage bars: application ≥85%).
- `tasks/tracking/GATES.md#G3` (the three checks closed here).
- `tasks/specs/E06-T01.md` … `E06-T13.md` (evidence incorporated by reference).

## Requirements

| ID | Requirement | Priority |
|----|-------------|----------|
| E06-T08-R01 | `go test ./internal/application/... -race -count=3` MUST pass with statement coverage ≥85%. | must |
| E06-T08-R02 | Every `api-contracts.md §7` endpoint group MUST have handlers (`check-tasks.py --handlers` green). | must |
| E06-T08-R03 | Saga crash-resume MUST pass and every `money-flow.md §10` row MUST have a saga test. | must |
| E06-T08-R04 | Table tests per handler MUST cover valid/invalid/boundary/auth-denied/idempotent-replay; sagas MUST test crash-resume with in-memory fakes. | must |

## Executable Scenarios

| ID | Given | When | Then |
|----|-------|------|------|
| E06-T08-S01 | Full application tree | `gate-check.sh G3` | PASSED at ≥85% |
| E06-T08-S02 | Epic texts + handlers | `check-tasks.py --handlers --ports` | Green |
| E06-T08-S03 | Coverage profile | Inspected per package | command ≥80%, all others ≥85% (ports: no statements) |

## Interfaces and Data

None (verification task; no interface change).

## Invariants and Failure Semantics

- Coverage is measured with `-race -count=3` (no cache, no flakes).
- Gate stays green: any later E06 change must re-run this gate before release.

## Change Surface

**Owns:** coverage-gap tests under `internal/application/...` (`coverage_*_test.go`), this packet/claim/evidence/handoff.
**Coordinates:** E06-T01–T07/T09–T13 evidence (referenced, not rewritten).
**Must Not Touch:** product code (unless a gap test exposes a defect — then packet-first per SDD), other epics, infrastructure, Git remotes.

## Verification Plan

| Requirement/Scenario | Proof | Command | Expected Evidence |
|----------------------|-------|---------|-------------------|
| E06-T08-R01, S01 | Gate script | `./tasks/scripts/gate-check.sh G3` | PASSED, 86.6% |
| E06-T08-R02, S02 | Traceability | `python3 tasks/scripts/check-tasks.py --handlers --ports` | green |
| E06-T08-R03 | Saga suite | `go test ./internal/application/workflow/ -race -count=3` | pass |
| E06-T08-R04 | Suite | `go test ./internal/application/... -race -count=3` | pass |
| All | Lint + validator | `make lint` + full `check-tasks.py` | 0 issues, OK |

## Acceptance Mapping

- [ ] Every requirement and scenario appears in the verification plan.
- [ ] Backlog acceptance criteria are represented without weakening them.
- [ ] Relevant gate checks are named (G3 — closed by this task).

## Open Questions

None.

## Approval

**Decision:** approved
**Approved By:** muse-spark (self-review recorded per `tasks/SDD.md §3.1`)
**Date:** 2026-09-15
**Notes:** Verification task only; no product behavior changes. Reviewer identical to author by the self-review allowance, recorded explicitly here.
