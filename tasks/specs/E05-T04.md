# Task Specification: E05-T04 — Tenancy unit tests (G2 slice)

**Task:** E05-T04
**Spec Status:** ready
**Author:** opencode-agent
**Reviewer:** repo-owner
**Last Updated:** 2026-09-14

## Objective

Prove the E05 G2 slice: onboarding, hierarchy, consolidation, isolation builders, residency/white-label validation pass with `-race -count=3` and domain coverage stays ≥90% with no external imports.

## Scope

### In Scope

- `test/unit/domain/tenant/tenancy_test.go`: cross-package G2 slice (onboarding checklist, hierarchy DAG, FX consolidation, key/subject round-trips, residency/white-label).
- G2 evidence: race suite, coverage report, import guard.

### Out of Scope

- New production behavior (owned by E05-T01…T03); persistence/integration tests (E07); application/API tests (E06/E11).

## Normative Inputs

- `tasks/epics/E05-tenancy.md#E05-T04` for the backlog boundary.
- `SPEC.md §10.2` (test pyramid; domain pure-function tests) and `SPEC.md §16` (definition of done).
- `tasks/tracking/GATES.md#G2` (domain ≥90%, executable specs, no external imports).
- `tasks/specs/E05-T01.md`, `tasks/specs/E05-T02.md`, `tasks/specs/E05-T03.md` (behaviors under test).

## Requirements

| ID | Requirement | Priority |
|----|-------------|----------|
| E05-T04-R01 | Slice MUST exercise onboarding all-or-nothing, hierarchy cycle/depth/grant, consolidation FX roll-up, key/subject round-trips, residency/white-label validation using only public domain APIs. | must |
| E05-T04-R02 | `go test ./internal/domain/... -race -count=3` MUST pass with new packages. | must |
| E05-T04-R03 | Domain coverage MUST remain ≥90% (`gate-check.sh G2` PASSED). | must |
| E05-T04-R04 | No external imports in new code (guard from E02-T09). | must |

## Executable Scenarios

| ID | Given | When | Then |
|----|-------|------|------|
| E05-T04-S01 | Full E05 behaviors | `go test ./internal/domain/... -race -count=3` | exit 0 |
| E05-T04-S02 | Coverage gate | `./tasks/scripts/gate-check.sh G2` | PASSED, domain ≥90% |
| E05-T04-S03 | Import guard | `go test ./test/unit/domain/ -count=1` | pass, no external imports |

## Interfaces and Data

None — test-only task. Uses public APIs from E05-T01…T03.

## Invariants and Failure Semantics

- Tests follow `AGENTS.md` normative policy: local `testCase`, exact param names, inline IIFE variants, multi-line entries, parent `t.Parallel()` only, sequential deterministic subtests.
- No package-level shared mutable fixtures.

## Change Surface

**Owns:** `test/unit/domain/tenant/tenancy_test.go`.
**Coordinates:** `internal/domain/...` read-only (APIs under test).
**Must Not Touch:** production code, persistence/application/API, Git remotes.

## Verification Plan

| Requirement/Scenario | Proof | Command | Expected Evidence |
|----------------------|-------|---------|-------------------|
| E05-T04-R01, S01 | Race suite | `CGO_ENABLED=1 CC=clang go test -race ./internal/domain/... -count=3` | exit 0 |
| E05-T04-R03, S02 | Gate | `./tasks/scripts/gate-check.sh G2` | PASSED |
| E05-T04-R04, S03 | Guard | `go test ./test/unit/domain/ -count=1` | pass |
| All | Structural | `python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes` | OK |

## Acceptance Mapping

- [ ] Every requirement and scenario appears in the verification plan.
- [ ] Backlog acceptance criteria are represented without weakening them.
- [ ] Relevant gate checks are named (G2).

## Open Questions

None.

## Approval

**Decision:** approved
**Approved By:** repo-owner
**Date:** 2026-09-14
**Notes:** G2 slice only; no production change.
