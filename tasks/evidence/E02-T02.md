# Verification Evidence: E02-T02

**Task:** E02-T02  
**Commit:** branch `feat/E02-ledger-domain` from `8f3107d2a36d8f69f2483fc045caccb6ffa3146e` (uncommitted per owner instruction; PR pending)  
**Environment:** Linux x86_64, Go 1.27.1 (`/tmp/opencode/go-sdk/go/bin`, no C compiler in this shell)  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-14T03:38:49Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E02-T02-R01, S03 | `TestOverflowRejected` (Max/MinInt64 add/mul/sub, div-by-zero) | PASS | `valueobject/money_test.go`, `edges_test.go` |
| E02-T02-R02, S01–S02, S09 | `TestTenthPlusFifthEqualsThreeTenths`, `TestCrossCurrencyMismatch`, `TestArithmeticProperties` (1000 seeded cases) | PASS | same |
| E02-T02-R03, S04 | `TestNoSettersOnValueObjects`, `TestNoFloatsInValueObjects` | PASS | `valueobject/currency_test.go` |
| E02-T02-R04, S05 | `TestRegistryRoundTrip`, `TestRegistryZeroValue` | PASS | same + `edges_test.go` |
| E02-T02-R05, S06 | `TestMoneyJSONRoundTrip` (float quantity + missing asset rejected) | PASS | `valueobject/money_test.go` |
| E02-T02-R06, S07 | `TestIDRoundTripAndParse`, `TestIDUniquenessAndVersion`, `TestAllIDTypesTable`, `TestIDEqualsAllTypes` | PASS | `ids_test.go`, `edges_test.go` |
| E02-T02-R07, S08 | `TestAllocateLargestRemainder`, `TestAllocateEdges` (negative, zero-weight, overflow) | PASS | `money_test.go`, `edges_test.go` |
| E02-T02-R08 | dot-segment guard empty + `grep float64` empty | PASS | shell output |
| All | `go test ./internal/domain/... -count=3` | exit 0 | shell output |
| All | coverage | total 97.6% (event 97.1, specification 98.0, valueobject 97.6) | coverprofile |
| All | `go build ./...`, `go vet`, `gofmt -l` clean | exit 0 | shell output |
| All | `check-tasks.py --format --graph --sdd` | exit 0 | validator output |

## Commands

```text
$ go test ./internal/domain/... -count=3                                   # ok × 3 pkgs
$ go test ./internal/domain/... -coverprofile=...                          # total 97.6%
$ go list -deps ./internal/domain/... | grep "\." | grep -v "^github.com/kadekutama/go-template/" # empty
$ grep -rn "float64" internal/domain/valueobject/ --include="*.go" | grep -v _test # empty
$ go build ./... && go vet ./internal/domain/...                           # exit 0
```

## Deviations and Residual Risk

- `-race` not run in this shell (no gcc); must be confirmed in CI/gcc env before G2.
- Zero-amount Money JSON (`{"amount_minor":0,...}` or missing amount defaulting to 0 with valid asset) is accepted as representable; entry positivity is enforced by E02-T04 per packet. One initial test expectation asserted otherwise and was corrected to the packet.
- `AllocateLargestRemainder` deals remainder to current-largest computed shares (ties → lowest index) per §2.13, not largest weights; fixed during implementation before tests passed.
- Amendment 2 (2026-09-14, same owner/session, re-verified below): deleted the domain-local UUIDv7 generator and all `New*` constructors — they duplicated the kernel `google/uuid` logic. Domain now owns only the `IDGenerator` port + `Parse*` validation; kernel `UUIDGenerator` implements the port implicitly. Tests use a deterministic stub; v7 uniqueness properties remain covered by kernel E01-T06 tests. Re-verification: `go test ./internal/... ./pkg/... ./test/unit/...` exit 0; `check-tasks.py --format --graph --sdd` exit 0.
- Amendment 3 — robustness sweep on review (2026-09-14, same owner/session): `validUUID` rewritten from index-fiddly char ranges to split + `encoding/hex` group validation (same accepted language; uppercase + malformed-group tests added); fixed a real `checkedMul` bug where `MinInt64 * -1` slipped through the `hi/b != a` check (explicit guard + regression tests); simplified `Sub`'s negation path; hardened `Format` for `MinInt64` via uint64 magnitude (+ exact-string test). Replaced four hand-rolled `map[string]string` clone helpers (event/aggregate/specification) with stdlib `maps.Clone`. No packet change needed: behavior now matches R01/R06 as written. Full suite re-run green (domain total 95.9%).
- Amendment 4 (2026-09-14, same owner/session): `validUUID` now delegates to stdlib `uuid.Parse` (Go 1.27 RFC 9562 package) — no hand-written validation remains. A length-36 gate preserves the strict canonical contract (`Parse` also accepts urn:/braced/bare-hex forms; new test locks their rejection). The nine copy-paste `Parse*` wrappers collapsed into one generic `parseID[T]` helper with byte-identical error messages. Same accepted language, no packet change. Full suite re-run green.
- Reconciliation (2026-09-14, owner-ruled): a second harness revised E02 files without a claim (Hare-Niemeyer allocation fix + lint/test refactors incl. `rand/v2`, `slices.Equal`, constant extraction, subtest splits). Independently verified: Hare vectors [2,5,4] and [50,48,1,1] correct (the prior greedy dealing demonstrably violated quota); `golangci-lint run ./...` 0 issues; full suite + coverage (96.1%) green. On the review's "141 linter issues": the total cannot be re-run (originals overwritten while uncommitted), but the checkable citations match real flaws in the pre-rewrite code, the category arithmetic is internally consistent, and the in-tree fixes correspond 1:1 — credible, with only the 0-issue after-state independently confirmed. Linter liveness proven via deliberate-violation probe. §2.13 corrected per SDD §1 conflict record in the packet; R07/S08 updated.
