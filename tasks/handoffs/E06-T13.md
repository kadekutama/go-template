# Task Handoff: E06-T13 — COMPLETED

**Task:** E06-T13
**From:** muse-spark
**To:** unassigned
**Branch/Worktree:** feat/E06-application
**Base / Head:** a23e32e70cca5e32cb6096d76f33c47b82c6f1ec / work-in-progress (uncommitted per owner instruction)
**Updated At:** 2026-09-15T08:40:00Z

## State Summary

E06-T13 COMPLETED — core posting + strong-balance use cases with DTOs, proven against fakes (incl. concurrency + unknown-outcome). Claim released. No remaining required work on this task.

## Work Completed

- `internal/application/command/posting.go` (`PostingService` implements `port.PostLedgerPosting`) + `posting_test.go` (execute table, unknown-outcome, 8-way concurrency).
- `internal/application/query/{posting,balance,entries}.go` + `query_test.go` (strong reads, normal-side balances, cursor pages).
- `internal/application/dto/{posting,balance,entry}.go` + `dto_test.go` (edge mapping).
- Packet `tasks/specs/E06-T13.md` (ready/approved; owner PR review required before merge).
- Epic E06-T13 + PROGRESS checkbox flipped to completed.

## Changed Files

- New: command/query/dto files + tests, `tasks/specs/E06-T13.md`, `tasks/claims/E06-T13.md` (released), `tasks/evidence/E06-T13.md`, this handoff.
- Modified: `tasks/epics/E06-application.md` (T13 completed), `tasks/tracking/PROGRESS.md` (T13 ticked).

## Decisions and Assumptions

- IDs minted once before `Do` (retried callbacks reuse them; duplicates fail at commit for replay resolution).
- Balances sign by `AccountClass.NormalSide()` (ledger-core §5); ACTIVE holds subtract; scans page at 500 with defensive cursor stop.
- `AccountSeq` provisional per-account positions; E07-T01 owns durable sequencing (see evidence residual risk).

## Verification Evidence

- See `tasks/evidence/E06-T13.md` (`make lint` 0 issues, race green incl. concurrency test, validator OK).

## Remaining Work

- None for E06-T13. Next: E06-T02 (account + tenant handlers; deps E06-T01/E06-T12/E05-T01 — packet needed), then T03/T04/T05/T11/T10.

## Risks and Blockers

- None blocking. Merge requires owner posting-path review (packet note).

## Resume Instructions

```bash
git status --short --branch && python3 tasks/scripts/check-tasks.py --ready
export PATH="$HOME/.pixi/bin:$PATH" CGO_ENABLED=1 CC=clang
go test -race ./internal/application/... -count=1
```

## Normative-policy compliance (2026-09-15)

- Command/query/DTO tests comply with the normative unit-test policy (`AGENTS.md`): local `testCase`, exact param names, inline closures for variants, multi-line entries, sequential deterministic subtests (`t.Parallel` only at parent top; concurrency test uses `sync.WaitGroup`, not subtest parallelism).
