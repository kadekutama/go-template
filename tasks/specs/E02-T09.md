# Task Specification: E02-T09 — Domain test suite + external-import guard (G2 gate)

**Task:** E02-T09  
**Spec Status:** ready  
**Author:** opencode-agent  
**Reviewer:** opencode-agent (self-review recorded per `tasks/SDD.md §3.1`; test harness only, no production behavior change)  
**Last Updated:** 2026-09-14

## Objective

Prove the G2 domain bar from a second angle: a `test/unit/domain/` mirror suite with a hermetic external-import guard, a full-slice composition test (account → posting → specs → reversal), and allocation exactness properties — plus wiring the mirror into `scripts/test/unit.sh`.

## Scope

### In Scope

- `test/unit/domain/guard_test.go`: hermetic guard parsing `internal/domain/` imports (fail on any module path outside stdlib + `github.com/kadekutama/go-template/internal/domain/`), runnable via plain `go test`.
- `test/unit/domain/slice_test.go`: end-to-end domain slice composing T01–T08 APIs (open account, construct balanced posting, evaluate the spec composition, reverse, assert events).
- `test/unit/domain/allocation_property_test.go`: seeded property — `AllocateLargestRemainder` sums exactly across 1000 random cases; Money add-associativity spot check.
- `scripts/test/unit.sh`: include `./test/unit/...` in the unit run (E00-T07 coordination; header comment updated).
- Epic E02 close-out: epic Status → `completed`, acceptance boxes ticked, PROGRESS E02 30/30 + total updated.

### Out of Scope

- G2 gate sign-off itself (needs `-race`, `generate-mocks`, E03–E05 specs; G2 stays `pending`).
- Integration/contract/perf suites (E07/E16 own them).
- New production code (test-only task; any production fix found goes through its owning packet).

## Normative Inputs

- `tasks/epics/E02-ledger-domain.md#E02-T09` for the backlog boundary and acceptance criteria.
- `SPEC.md §10.2`, `SPEC.md §16` (test pyramid, per-phase gates).
- `tasks/tracking/GATES.md#G2` (≥90% coverage, `--specs`/`--events`, no external imports, `generate-mocks`).
- `tasks/SDD.md §3, §7`.

## Requirements

| ID | Requirement | Priority |
|----|-------------|----------|
| E02-T09-R01 | `go test ./internal/domain/... -count=3` MUST pass with total coverage ≥90%. | must |
| E02-T09-R02 | The hermetic guard test MUST fail if any non-test domain file imports outside stdlib + domain (proven by running it green + a negative control in review). | must |
| E02-T09-R03 | The slice test MUST compose open → construct → spec-evaluate → reverse → events without infrastructure. | must |
| E02-T09-R04 | `scripts/test/unit.sh` MUST execute the mirror suite (verified by running it). | must |
| E02-T09-R05 | No production files may change in this task except the codec-seam fix in `valueobject/money.go`, which is owned by the amended E02-T02 R05 (same owner/session; recorded here and in the T02 packet). | must |

## Executable Scenarios

| ID | Given | When | Then |
|----|-------|------|------|
| E02-T09-S01 | Full domain suite | `go test ./internal/domain/... -count=3` (and `-race` where toolchain allows) | All pass; coverage ≥90%. |
| E02-T09-S02 | Mirror suite | `go test ./test/unit/domain/ -count=1 -v` | Guard + slice + property all pass. |
| E02-T09-S03 | `scripts/test/unit.sh` | Executed | Runs internal + pkg + test/unit suites, exit 0. |
| E02-T09-S04 | Epic E02 state | All T01–T09 completed + validator | Epic `completed`, PROGRESS 30/30, `--format --graph --sdd` green. |

## Interfaces and Data

- No production interfaces change. Test-only API use of T01–T08 packages.
- `unit.sh` change: `go test ./internal/... ./pkg/... ./test/unit/...`.

## Invariants and Failure Semantics

- Guard parses imports with go/parser (no toolchain execution inside the test; hermetic).
- Seeded RNG (deterministic, no flakiness).

## Change Surface

**Owns:** `test/unit/domain/{guard,slice,allocation_property}_test.go` (+ `doc.go` package comment if needed).  
**Coordinates:** `scripts/test/unit.sh` (E00-T07-owned; one-line scope expansion + comment), `tasks/epics/E02-ledger-domain.md` (close-out), `tasks/tracking/PROGRESS.md` (E02 row + total).  
**Must Not Touch:** any production file under `internal/`, other scripts, task statuses outside E02-T09 (except epic close-out fields), Git remotes.

## Verification Plan

| Requirement/Scenario | Proof | Command | Expected Evidence |
|----------------------|-------|---------|-------------------|
| E02-T09-R01, S01 | full suite + coverage | `go test ./internal/domain/... -count=3` + coverprofile | exit 0, total ≥90% |
| E02-T09-R02–R03, S02 | mirror suite | `go test ./test/unit/domain/ -count=1 -v` | all PASS |
| E02-T09-R04, S03 | unit script | `./scripts/test/unit.sh` | exit 0, runs 3 trees |
| E02-T09-S04 | validator + gates | `python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events` | exit 0 |
| E02-T09-R05 | `git status` file list | inspection | test/script/tracking/tasks + amended `money.go` (+ its test) |

## Acceptance Mapping

- [x] Every requirement and scenario appears in the verification plan.
- [x] Backlog acceptance criteria represented (`-race -count=3` run where toolchain allows + documented caveat; coverage ≥90%; guard in `gate-check.sh G2` pre-existing, hermetic Go guard added).
- [x] Relevant gate checks named: G2 (left `pending`: `-race`, mocks, E03–E05 still outstanding).

## Open Questions

None.

## Approval

**Decision:** approved  
**Approved By:** opencode-agent (self-review recorded per `tasks/SDD.md §3.1`)  
**Date:** 2026-09-14  
**Notes:** G2 completion is separate from task completion per SDD §3.4; this task closes the epic, not the gate.
