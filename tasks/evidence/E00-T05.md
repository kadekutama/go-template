# Verification Evidence: E00-T05

**Task:** E00-T05  
**Commit:** branch `feat/E00-T05-ci-gate` merged into `main` via `--no-ff`
(PR equivalent; pushed to origin — the push itself triggers this workflow)  
**Environment:** Linux, python3+pyyaml; no GitHub API token (live-run status
observable at repo Actions page)  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-13

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E00-T05-R01/R02, S02 | Workflow parse: PR trigger; ordered steps Checkout → setup-go 1.27.1 → `go mod verify` → tidy+`git diff --exit-code` → `make lint` → `make build-all` (fail-fast by step order) | pass | `.github/workflows/ci.yml` |
| E00-T05-R03, S03 | JSON/YAML parse: renovate weekly weekend schedule, non-major group, `automerge: false` + `platformAutomerge: false`; dependabot gomod weekly | pass | `renovate.json`, `.github/dependabot.yml` |
| E00-T05-R04 | `ls .github/workflows/` → exactly `ci.yml`; E17-extension header present naming this file as the single pipeline | pass | `.github/workflows/` |
| E00-T05-S01 | Live PR-triggered run | deferred | observable at Actions page after push (see Deviations) |
| Packet gate | `check-tasks.py --format --graph --sdd` | pass | exit 0 |

## Commands

```text
$ python3 -c "<ci.yml/renovate/dependabot assertions>"  # triggers, order, schedules, no auto-merge
$ ls .github/workflows/                                 # ci.yml only
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd  # OK, exit 0
```

## Follow-up fix 2 (2026-09-13, on `fix/E00-T05-ci-lint-tool`, unpushed)

- **CI notice observed by owner (lint job green):** Node 20 deprecation —
  `actions/checkout@v4` / `actions/setup-go@v5` still target Node 20.
- **Fix:** bumped to `actions/checkout@v7.0.1` + `actions/setup-go@v7.0.0`
  (latest tags verified via the GitHub API the same day).
- **Proof here:** workflow YAML re-parsed (single job, step order intact).
  **Live proof requires a CI run after push.**

## Follow-up fix (2026-09-13, branch `fix/E00-T05-ci-lint-tool`, unpushed)

- **CI failure observed by owner:** `make lint` failed with
  `golangci-lint: not found` (exit 127) — `actions/setup-go` does not ship
  the linter. Root cause: missing install step (authoring miss, not a design
  flaw).
- **Fix:** added an `Install golangci-lint` step pinned to `v2.13.2`
  (verified real via the Go module proxy, 2026-08-27). `shellcheck` needs no
  install step (preinstalled on `ubuntu-latest`).
- **Proof here:** workflow YAML re-parsed (single job, ordered steps
  verify→tidy→install-lint→lint→build); `check-tasks.py` green. **Live proof
  still requires a CI run after push** (needs owner push approval per the
  no-push-without-permission rule).

## Deviations and Residual Risk (original)

- **Live CI run not observed from here** (no API token to poll Actions).
  Mitigation: this very push triggers the workflow; owner confirms green at
  the repo Actions page. If red (e.g. golangci-lint version drift), the fix
  belongs to the failing task's follow-up, not to this gate's structure.
- Task completed on parse + inventory + validator proofs above.

## Version-guard addendum (2026-09-17, TAKEOVER muse-spark, owner-directed)

`ci.yml` checks out full history (`fetch-depth: 0` — shallow clones would
stamp count 1 and miss tags), resolves `VERSION` via `version.sh`, fails the
job on a `dev` fallback, exports it for later steps, and echoes it to the
job summary. `make build-all` then stamps identically to local builds.
Verified statically (YAML parses, single workflow file, guard present); live
PR-run proof remains observable at the repo Actions page post-push.
Docker `--build-arg VERSION` wiring belongs to E17 (no Dockerfiles exist yet).
