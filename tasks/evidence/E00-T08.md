# Verification Evidence: E00-T08

**Task:** E00-T08  
**Commit:** branch `feat/E00-T08-sdd-plane` merged into `main` via `--no-ff`
(PR equivalent; pushed to origin)  
**Environment:** Linux, python3  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-13

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E00-T08-R01, S01 | `check-tasks.py --ready` on the real tree lists the next runnable task with packet state (`E01-T01 [needs-packet] …`) | pass | validator output |
| E00-T08-R02, S02 | `test-sdd-lifecycle.py`: valid cycle accepted + 5 rejections (cycle, missing packet, wrong/missing claim, progress mismatch, missing evidence) + real-tree guard | 7/7 pass | `tasks/scripts/test-sdd-lifecycle.py` |
| E00-T08-R03, S03 | `ci.yml` parse: metadata-validation step inside the existing lint job; job set unchanged (lint+build only) | pass | `.github/workflows/ci.yml` |
| E00-T08-R04, S04 | Scan: `AGENTS.md` + 5 adapters link `tasks/SDD.md`; quote-blocks are pointers, no status/evidence/takeover redefinitions | pass | `AGENTS.md`, `.opencode/agents/*.md` (verify-only, unmodified) |
| Packet gate | `check-tasks.py --format --graph --sdd` | pass | exit 0 |

## Commands

```text
$ python3 tasks/scripts/check-tasks.py --ready
# --ready: 1 task(s) / E01-T01 [needs-packet] Pin go.mod dependencies ...
$ python3 tasks/scripts/test-sdd-lifecycle.py
# 7/7 fixture cases passed (exit 0)
$ python3 -c "<ci.yml step assertion>"  # validation step present, 1 job
$ grep -l "SDD.md" AGENTS.md .opencode/agents/*.md  # 6/6 link it
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd  # OK, exit 0
```

## Deviations and Residual Risk

- None. No validator semantics changed; no adapter edits were needed.
- Live CI observation remains with the owner at the Actions page (E00-T05
  residual); the added step runs the same command proven green here.
