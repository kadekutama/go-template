# Verification Evidence: E00-T03

**Task:** E00-T03  
**Commit:** branch `feat/E00-T03-lint-config` merged into `main` via `--no-ff`
(PR equivalent; pushed to origin)  
**Environment:** Linux, python3+pyyaml; NO golangci-lint in sandbox (live run
deferred to CI — see Deviations)  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-13

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E00-T03-R01/R02, S01 | YAML parse: 18/18 required linters enabled, gocyclo `min-complexity: 12`, fail-closed issues section, zero exclusions | pass | `.golangci.yml` |
| E00-T03-R03, S03 | Inspection: `Makefile:53` `lint` target runs `golangci-lint run ./...`; E00-T05 packet records the `ci.yml` contract | pass | `Makefile`, `tasks/specs/E00-T05.md` (follow-up) |
| E00-T03-R04, S02 | `golangci-lint run ./...` live | deferred | tool absent in sandbox (see Deviations) |
| Packet gate | `check-tasks.py --format --graph --sdd` | pass | exit 0 |

## Commands

```text
$ python3 -c "<yaml parse + 18-linter assertion>"  # 18 enabled, none missing, gocyclo 12
$ grep -n golangci-lint Makefile                   # GOLANGCI_LINT ?= ...; lint target
$ golangci-lint run ./...                          # NOT RUN — command not found
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd  # OK, exit 0
```

## Follow-up fix (2026-09-13, on `fix/E00-T05-ci-lint-tool`, unpushed)

- **CI failure observed by owner:** `can't unmarshal config … 'output.formats'
  expected a map, got 'slice'` — the config used the v1 list form; v2.13.2
  requires the map form. Second error after that fix: `gofmt is a formatter`
  — v2 moved gofmt/goimports from `linters.enable` to `formatters.enable`.
- **Proof (exact pinned binary `v2.13.2`, downloaded to sandbox):**
  reproduced the CI error verbatim before the fix; after the fix the config
  loads and `linters`/`formatters` list all 16 required linters plus
  gofmt/goimports as formatters. `run ./...` then fails only on the missing
  Go toolchain (`go: executable file not found`), which CI provides.
- Packet R01 corrected to the v2 section split (lower-precedence source
  correction per `tasks/SDD.md §1`; intent unchanged).
- **Live proof still requires a CI run after push** (needs owner push approval
  per the no-push-without-permission rule).

## Deviations and Residual Risk (original)

- **No golangci-lint** in sandbox: live `run ./...` deferred to CI (`E00-T05`
  gates every PR/push on `make lint`). Justification: empty tree (five no-op
  mains) plus strict fail-closed config; first CI run exercises it. Residual
  risk: low.
- Task completed on parse + wiring + validator proofs above.
