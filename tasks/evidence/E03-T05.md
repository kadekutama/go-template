# Verification Evidence: E03-T05

**Task:** E03-T05
**Commit:** branch `feat/E03-money-movement` from `57de5c86cbc31bb73d69e604a4b93d3783b7285a` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1 (`/tmp/opencode/go-sdk/go/bin`)
**Verified By:** opencode-agent (opencode / Muse Spark)
**Verified At:** 2026-09-14T07:25:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E03-T05-R01–R05, S01–S04 | `TestConvert`, `TestGainLoss`, `TestLotsBalance`, `TestNewFxPair`, `TestFxRateValidate`, `TestFxRateIsStale` | PASS | `internal/domain/service/fx_service_test.go`, `fx_service.go`, `valueobject/fx_rate.go`, `fx_pair.go` |
| E03-T05-R06 | guard test | PASS | shell output |
| All | `go test ./internal/domain/... -count=3` | exit 0 | shell output |

## Commands

```text
$ go test ./internal/domain/... -count=1
PASS all 7 packages
```

## Deviations and Residual Risk

- Gain/loss helper is pure arithmetic over accepted quotes; rate fetching/versioned mark-to-market policy owned by E10.

## Follow-up audit (2026-09-14, same owner/session, claim reopened per SDD §6)

- Novel `fxError`/`FxCode` type removed: `FxRate.Validate` follows the `money.go`/`ids.go` plain-error convention; service maps shape failures to `FX_RATE_INVALID`. Packet `GainLoss` signature corrected to the implementation.
- Re-verified: `go test ./internal/domain/... -count=3` exit 0, incl. overflow branches; domain coverage 94.2%; `go vet`/`gofmt` clean; `check-tasks.py --format --graph --sdd` OK.

## Exact-shape rewrite + ctx revert (2026-09-14, same owner/session, claim reopened per SDD §6)

- Reverted the mistaken `_ context.Context` threading from all T05 production code; signatures are ctx-free again. Tests rewritten to the exact shape (error-free `GainLoss` rows carry no `expectedError` field).
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 96.6%; `go vet`/`gofmt` clean.

## Normative-policy compliance (2026-09-14, same owner/session, claim reopened per SDD §6)

- All T05 parent tests (service + `fx_rate` suite) run parallel (`t.Parallel()`) with sequential deterministic subtests (no `tc := tc`, no inner `t.Parallel()`) — `-race` clean.
- Mandatory protocol: `gofmt -s` clean, `make lint` 0 issues, `CGO_ENABLED=1 CC=clang go test -race ./internal/domain/... -count=1` green, `gate-check.sh G2` PASSED, `check-tasks.py --format --graph --sdd --specs --events --codes` OK.

## Uniform-table exemplar (2026-09-14, same owner/session, claim reopened per SDD §6)

- Test-only change: shared `TestParseAndTransitionTables` exemplar (covers FX pair + payout vocab); no T05 production change.
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 95.2%; `go vet`/`gofmt` clean.
