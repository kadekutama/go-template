# Verification Evidence: E06-T05

**Task:** E06-T05
**Commit:** branch `feat/E06-application` from `a23e32e70cca5e32cb6096d76f33c47b82c6f1ec` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain (`$HOME/.pixi/bin` on PATH)
**Verified By:** muse-spark
**Verified At:** 2026-09-15T11:30:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E06-T05-R01, S01 | `TestReconTriggerExecute` (trigger + match + break + facts) | PASS | `command/reconciliation.go`, `reconciliation_test.go` |
| E06-T05-R02, S02, S03 | `TestBreakResolve` (resolve + fact, self-approval fail-closed, acknowledge) | PASS | same as above |
| E06-T05-R03, S04 | `TestPeriodLifecycle` (open/close/reopen + facts; dirty close carries all 3 codes, stays open) | PASS | `command/period.go`, `ops_test.go` |
| E06-T05-R04, S05 | `TestReportGenerate` (all 10 templates + URL + fact; unknown rejected) | PASS | `command/report.go`, `ops_test.go` |
| E06-T05-R05, S06 | `TestComplianceWorkflows` (screening record; CSV export validated; unknown type/field rejected) | PASS | `command/compliance.go`, `ops_test.go` |
| Reads | `TestOpsReads` (run/break/period/report/export delegation) | PASS | `query/ops_test.go` |
| All | `make lint` (0 issues) + `go test -race ./...` (zero races) + full validator | PASS/OK | — |

## Commands

```text
$ gofmt -s -l internal/ test/    # empty
$ goimports -l -w .               # clean
$ make generate-mocks            # exit 0 (compliance port amendments regenerated)
$ make lint                       # 0 issues (exit 0; shared report runner for gocyclo ≤ 12)
$ go vet ./...                    # exit 0
$ go test ./internal/application/... -count=3                # pass
$ go test -race ./... -count=1                               # all packages ok, zero races
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes  # OK
```

## Deviations and Residual Risk

- Additive port fields (`BreakResolutionRequest.{IdempotencyKey,AdjustmentPostingID}`, `PeriodRequest.{Start,End,Timezone,Approver,UnresolvedWorkflows,SubledgerDeltas,FXRevalued}`, `ScreeningRequest.IdempotencyKey`, `RegulatoryExportRequest.{Fields,IdempotencyKey}`) + T12 amendment notes.
- Break status/decisions are application workflow state around the stateless domain break (no status field on the entity).
- Adjustment amount = |expected − actual| per break side (documented in code).
- Report summaries render request parameters; projection-powered renderers arrive with later adapter work.
- Screening decisions persist without webhook (catalog has none); export reuses `report.generated.v1`.

## Audit fixes (2026-09-15, owner-ordered)

- Period close/reopen and recon `ExecuteRun` load inside `Do` after replay check (data-flow §2 order).
- Break adjustment amount derived as `|expected − actual|`; list handlers for runs/breaks/periods/reports.
- `ReconStore.ListBreaksByRun` additive (T09 also consumes).
- Re-verified: `make lint` 0 issues, `go test -race ./...` green, G3 PASSED, validator OK after claim release.
