# Verification Evidence: E00-T01

**Task:** E00-T01  
**Commit:** branch `feat/E00-T01-go-skeleton` merged into `main` via `--no-ff`
(PR equivalent; pushed to origin)  
**Environment:** Linux, git; NO Go toolchain and no Docker daemon in sandbox
(build proofs deferred to CI — see Deviations)  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-13

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E00-T01-R01, S01-part | Inspection: `head -n 3 go.mod` → `module github.com/kadekutama/go-template` + `go 1.27.1` | pass | `go.mod` |
| E00-T01-R02, S02 | Inspection: all `SPEC.md §4` dirs exist; `.gitkeep` in every otherwise-empty dir | pass | tree + 39 `.gitkeep` files |
| E00-T01-R03, S01-part | Inspection: exactly five `cmd/*/main.go` no-op mains | pass | `ls cmd/*/main.go` (5 files) |
| E00-T01-R03, S01/S03 | `go build ./...` / `go list ./cmd/...` | deferred | no Go toolchain in sandbox (see Deviations) |
| E00-T01-R04 | `git check-ignore -v`: `bin/`, `coverage/`, `*.log`, `.env*`, `.air.toml.local` ignored; `.env.example` NOT ignored (exit 1) | pass | `.gitignore` |
| E00-T01-R05, S04 | `find . -mindepth 2 -name .git -print` empty; no remote added; no `go.sum` (no deps) | pass | single top-level `.git` only |
| Packet gate | `check-tasks.py --format --graph --sdd` | pass | exit 0 |

## Commands

```text
$ head -n 3 go.mod                     # module github.com/kadekutama/go-template / go 1.27.1
$ ls cmd/*/main.go                     # 5 files
$ git check-ignore -v bin/app coverage/c.out app.log .env .env.local .air.toml.local
# all matched by .gitignore
$ git check-ignore .env.example; echo $?  # 1 (trackable)
$ find . -mindepth 2 -name .git -print    # (empty)
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd  # OK, exit 0
$ go build ./...                       # NOT RUN — go: command not found
$ go list ./cmd/...                    # NOT RUN — go: command not found
```

## Deviations and Residual Risk

- **No Go toolchain** (`go: command not found`) and no Docker daemon in this
  sandbox, so `go build ./...` and `go list ./cmd/...` could not run here.
  Justification: the skeleton is five dependency-free no-op mains under one
  module with no imports; compilation risk is minimal and CI (`E00-T05`,
  `make build-all` on every PR/push) runs both commands from a clean checkout.
  Owner can confirm at the repo Actions page after push. Residual risk: low.
- Task completed on structural + ignore + validator proofs above.
