# Verification Evidence: E00-T04

**Task:** E00-T04  
**Commit:** branch `feat/E00-T04-dev-loop` merged into `main` via `--no-ff`
(PR equivalent; pushed to origin)  
**Environment:** Linux, bash, python3+tomllib; NO air/Go/Docker daemon
(live-rebuild proof deferred — see Deviations)  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-13

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E00-T04-R01, S01 | TOML parse: build cmd `./cmd/rest-api`, `delay = 500`, excludes bin/vendor/test/docs/tmp, `include_ext` has go, `include_file` watches `config/*.yaml` | pass | `.air.toml` |
| E00-T04-R02, S02 | `bash -n` ×3 valid; `test -x` ×3; `set -euo pipefail` + root-relative `ROOT` + `$@` passthrough in each | pass | `scripts/dev/*.sh` |
| E00-T04-R03, S02 | `grep` Makefile: dev-up/dev-down/dev-logs targets reference the exact script paths (plus `need-script` guards) | pass | `Makefile:89-100` |
| E00-T04-S03 | Live air rebuild on stub edit | deferred | needs air + Go (see Deviations) |
| Packet gate | `check-tasks.py --format --graph --sdd` | pass | exit 0 |

## Commands

```text
$ bash -n scripts/dev/dev-up.sh && bash -n scripts/dev/dev-down.sh && bash -n scripts/dev/dev-logs.sh
$ test -x scripts/dev/dev-up.sh && test -x scripts/dev/dev-down.sh && test -x scripts/dev/dev-logs.sh
$ grep -n "scripts/dev/dev-*.sh" Makefile   # wiring resolves
$ python3 -c "<tomllib assertions>"         # cmd, delay=500, excludes, go+yaml
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd  # OK, exit 0
```

## Deviations and Residual Risk

- **`.git/` exclusion**: the epic lists it, but air ignores `.git` internally
  with no config knob; all other excludes are explicit. Equivalent behavior,
  no gap.
- **Live air rebuild deferred** (no air/Go/Docker daemon here); scripts are
  thin `docker compose` wrappers whose failure modes surface verbatim.
  Residual risk: low; first dev-machine run exercises S03.
- Task completed on parse + syntax + mode + wiring + validator proofs above.
