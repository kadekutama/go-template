# Verification Evidence: E06-T13

**Task:** E06-T13
**Commit:** branch `feat/E06-application` from `a23e32e70cca5e32cb6096d76f33c47b82c6f1ec` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain (`$HOME/.pixi/bin` on PATH)
**Verified By:** muse-spark
**Verified At:** 2026-09-15T08:40:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E06-T13-R01, S01, S02 | `TestPostingServiceExecute` (happy path with cursor+outbox; unknown account; unbalanced lot commits nothing) | PASS | `internal/application/command/posting.go`, `posting_test.go` |
| E06-T13-R02, S03, S04, S06 | `TestPostingServiceExecute` (replay/conflict) + `TestPostingServiceUnknownOutcome` (ambiguity then replay) | PASS | same as above |
| E06-T13-R05, S07 | `TestPostingServiceConcurrent` (8 callers, one commit, identical responses, `-race -count=3`) | PASS | same as above |
| E06-T13-R03, S08 | `TestPostingQueryExecute` / `TestBalanceExecute` (normal-side signing, holds, multipage, released holds) / `TestEntriesExecute` (cursor/limit clamp) | PASS | `internal/application/query/`, `query_test.go` |
| E06-T13-R04, S08 | `TestToPostingDTO` / `TestToBalanceDTO` (minor units, asset, as-of, cursor; no workflow state) | PASS | `internal/application/dto/`, `dto_test.go` |
| S05 | `TestPostingServiceExecute` (denied + missing actor/key fail pre-commit) | PASS | `posting_test.go` |
| All | `make lint` (0 issues) + `go test -race ./...` (zero races) + full validator | PASS/OK | — |

## Commands

```text
$ gofmt -s -l internal/ test/    # empty
$ goimports -l -w .               # clean
$ make lint                       # 0 issues (exit 0)
$ go vet ./internal/application/...                          # exit 0
$ go test ./internal/application/... -race -count=3          # pass
$ go test -race ./... -count=1                               # all packages ok, zero races
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes  # OK
```

## Deviations and Residual Risk

- `encoding/json` replaced with the `pkg/jsonparser` seam in `command/posting.go` after the codec-seam guard failed; behavior identical (stdlib-compatible profile).
- Entry `AccountSeq` is the deterministic 1-based per-account position within the posting (domain requires ≥ 1); durable global sequencing is assigned at commit by E07-T01 per data-flow step 5 (`UNIQUE(account_id, account_seq)`). Recorded as E07 follow-up; E11-T14 pilot verifies end-to-end.
- Account-set reads happen before `Do` (construction snapshot); E07 re-validates scope/status under deterministic locks at commit.
- Financial posting path: PR review by repo-owner required before merge (packet approval note; SDD §3.4.3).

## Amendment (Muse Spark Review Polish 2026-09-16)

- Decoupled `query/transfer.go` and `query/dashboard.go` from `command.TransferStore` by promoting `TransferRecord`, `BatchRecord`, `BatchItem`, and `TransferListFilter` into `internal/application/port/usecase_transfer.go`, and defining consumer-owned `TransferReader` interfaces in `query/`.
- Restored query edge-case tests:
  - `BALANCE_OVERFLOW` in `TestBalanceExecute`: MinInt64 negation checked in `signedEntryAmount`, entry addition overflow in `scanPosted`, hold addition overflow in `sumActiveHolds`, and available balance subtraction underflow in `Execute`.
  - Expired-hold exclusion in `TestBalanceExecute`: verified that holds where `ExpiresAt <= now` are excluded and do not reduce available balance.
  - `STATEMENT_TOO_LARGE` in `TestTransactionQueryGetStatement`: verified bounded scanning aborts with error when scan exceeds `statementScanPages = 20`.
- Standardized pagination bounds across `command/paging.go` (`DefaultPageLimit = 50`, `MaxPageLimit = 100`) matching query pagination bounds in `account.go` and `dispute.go`.
