# Domain Layer

Pure business logic with zero external dependencies: entities, value objects,
aggregates, domain events, executable specifications, and repository ports
(`internal/domain/`). See [SPEC.md §3](../../SPEC.md) (architecture layers).
Content is filled by E18; domain implementation lives in E02–E05.
