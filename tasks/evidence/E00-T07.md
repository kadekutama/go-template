# Verification Evidence: E00-T07

**Task:** E00-T07  
**Commit:** branch `feat/E00-T07-scripts` merged into `main` via `--no-ff`
(PR equivalent; pushed to origin)  
**Environment:** Linux, bash; NO shellcheck in sandbox (live proof deferred
to CI `make lint` — see Deviations)  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-13

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E00-T07-R01, S01 | Inventory + `bash -n` + `test -x` + idiom scan (`set -euo pipefail`, `--help`, arg forwarding, root-relative) | 16/16 pass (2 stubs echo-reserve args — see Deviations) | 16 scripts |
| E00-T07-R02, S02/S03 | `--help` ×16 → usage, exit 0, no side effects; `mocks.sh` (no mockery) and `performance.sh` (no k6) → clear tool-missing error, exit 1; `tag.sh bad-version` → semver error, exit 1 | pass | script CLIs |
| E00-T07-R03, S04 | Makefile ref resolution: all owned refs exist + executable; `scripts/db/migrate.sh` (E07-T05) absent but loud-guarded | pass with documented conflict resolution | `Makefile`, packet Approval notes |
| E00-T07-R04 | `shellcheck scripts/**/*.sh` | deferred | tool absent (see Deviations) |
| Packet gate | `check-tasks.py --format --graph --sdd` | pass | exit 0 |

## Commands

```text
$ for s in scripts/*/*.sh; do bash -n $s && test -x $s && <idiom grep>; done  # 16/16
$ for s in scripts/*/*.sh; do $s --help; done                                 # all exit 0
$ ./scripts/generate/mocks.sh       # missing tool: mockery ...; exit 1
$ ./scripts/test/performance.sh     # missing tool: k6 ...; exit 1
$ ./scripts/release/tag.sh bad-version  # invalid semver tag ...; exit 1
$ grep -o '\./scripts/[a-z-]*/[a-z-]*\.sh' Makefile | sort -u  # 12 refs; 11 resolve, migrate.sh E07-T05-owned
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd  # OK, exit 0
$ shellcheck scripts/**/*.sh        # NOT RUN — command not found
```

## Deviations and Residual Risk

- **No shellcheck** in sandbox: live run deferred to CI (`make lint` runs it
  on every PR/push). Scripts use quoted vars, `set -euo pipefail`, and no
  unguarded globs. Residual risk: low.
- **`migrate.sh` conflict** resolved in packet Approval notes (Files list wins;
  no silent choice). **`chaos.sh`/`docker-push.sh`** echo-reserve extra args
  (no downstream tool exists yet to forward to) — same note.
- Task completed on inventory + behavior + wiring + validator proofs above.

## Version-injection addendum (2026-09-17, TAKEOVER muse-spark, owner-directed)

`scripts/build/version.sh` (new, executable) prints `$VERSION` override, else
`git describe --tags` with manual `-dirty` (untracked files count, unlike
describe's builtin flag), else `v0.0.0-<count>-g<sha>[-dirty]`, else `dev`.
`build-all.sh` stamps every binary via `-ldflags "-X main.version=..."`.
Verified matrix: dirty untagged tree → `v0.0.0-47-g0aedc38-dirty`;
`VERSION=v9.9.9-ci.42` → passthrough; no-git dir → `dev`; synthetic tagged
repo → `v1.4.2-1-g<sha>` clean and `-dirty` with untracked files. Stamped
`bin/rest-api` + `bin/cron` report the derived version on the `initialising`
line. Timestamps deliberately excluded (reproducible builds).
