# Verification Evidence: E00-T00

**Task:** E00-T00  
**Commit:** 9247bf5 (branch `feat/E00-T00-git-baseline`; packet/governance/epic refinements in follow-up commit on that branch)  
**Environment:** Linux, git (repo-local identity `go-template agent`), origin `https://github.com/kadekutama/go-template` (read-verified; no push credentials in this environment)  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-13

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E00-T00-R01 (boundary choice) | Owner decision 2026-09-13: standalone repo, recorded in packet + governance §1 | pass | `tasks/specs/E00-T00.md`, `docs/development/repository-governance.md` |
| E00-T00-R02 (shared baseline) | `git log --oneline` shows `6eca386` + `b51d5c9` + task commits; `git rev-parse --verify HEAD` resolves full SHA | pass | local history on `feat/E00-T00-git-baseline` |
| E00-T00-R03 (origin/merge policy) | `git remote get-url origin` + `git ls-remote origin` + `git fetch origin`; merge/claim-serialization policy in governance §§2–4 | pass (origin); push pending auth (see Deviations) | origin `https://github.com/kadekutama/go-template`, governance doc |
| E00-T00-R04 (CODEOWNERS/governance) | Inspection: `.github/CODEOWNERS` assigns `@kadekutama` over claims, migrations, generated contracts, ADRs; governance §5 ownership table | pass | `.github/CODEOWNERS`, `docs/development/repository-governance.md` |
| E00-T00-R05/S02 (fresh checkout + validator) | `check-tasks.py --format --graph --sdd --specs --events --handlers --ports --subjects --docs --codes` | pass | exit 0, 140 tasks / 20 epics / 432 SP |
| E00-T00-S01 (root/HEAD/origin resolve) | `git rev-parse --show-toplevel`, `symbolic-ref HEAD`, `git remote get-url origin`, `git ls-remote origin` | pass | toplevel + HEAD + origin all resolve; remote `main` = `2d7c5b7` |
| E00-T00-S03 (claim-race serialization) | Scratch-clone race: two contender branches add `tasks/claims/E00-T08.md`; merge A succeeds, merge B fails `CONFLICT (add/add)` → exactly one accepted | pass (local simulation; PR path needs push auth, see Deviations) | race transcript below; scratch clone removed afterwards |
| E00-T00-S04 (no sibling paths) | Parent `oc-free-models-test/` contains only `go-template/`; `git show --stat HEAD` adds no sibling paths; `find . -mindepth 2 -name .git` empty | pass | `git show --stat HEAD`, parent listing |

## Commands

```text
$ git remote add origin https://github.com/kadekutama/go-template
$ git remote get-url origin              # https://github.com/kadekutama/go-template
$ git ls-remote origin                   # HEAD + refs/heads/main at 2d7c5b7 (exit 0)
$ git fetch origin                       # new branch main -> origin/main (exit 0)
$ git merge-base HEAD origin/main        # exit 1: no common history (reconciliation decision required)
$ git rev-parse --show-toplevel          # .../go-template
$ git rev-parse --verify HEAD            # full branch SHA (see handoff Base/Head)
$ find . -mindepth 2 -name .git -print   # (empty)
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs \
    --events --handlers --ports --subjects --docs --codes
# tasks: 140, epics: 20, total SP: 432 — OK: structural checks passed (exit 0)

# S03 race (isolated clone /tmp/opencode/s03-race, deleted afterwards):
$ git clone <repo> /tmp/opencode/s03-race && git checkout -b s03-race-target
$ git checkout -b race/contender-a   # add tasks/claims/E00-T08.md (Owner: harness-a), commit
$ git checkout -b race/contender-b   # add tasks/claims/E00-T08.md (Owner: harness-b), commit
$ git merge --no-ff race/contender-a # success (1 file changed)
$ git merge --no-ff race/contender-b # CONFLICT (add/add): tasks/claims/E00-T08.md — rejected
$ git merge --abort && rm -rf /tmp/opencode/s03-race

$ git push -u origin feat/E00-T00-git-baseline
# fatal: could not read Username for 'https://github.com': No such device or address (exit 128)
```

## Deviations and Residual Risk

- **Push authentication unavailable** (exit 128 above): the task branch exists
  only locally, so other harnesses cannot fetch it yet. Owner action: from an
  authenticated checkout run `git push -u origin feat/E00-T00-git-baseline`,
  open a PR against `main`, merge, then flip E00-T00 to `completed`, tick
  `tasks/tracking/PROGRESS.md`, and set the claim to `released`. Credentials
  must never be committed to the repo.
- **Remote `main` shares no history** with the local baseline (`merge-base`
  exit 1; remote is a single "Initial commit" `2d7c5b7`). Merging the PR will
  surface this; reconciling the two `main` lines (keep local baseline as truth
  vs `merge --allow-unrelated-histories` preserving the README) needs an
  explicit owner decision. Never force-push without it.
- Task status stays `blocked`; claim stays `active`. This file supersedes the
  earlier E00-T00 evidence except as historical record.
