# Verification Evidence: E07-T01

**Task:** E07-T01
**Commit:** branch `feat/E07-persistence` from `3d794105864c4e91c6efd990feb2adc57380e029` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain (`$HOME/.pixi/bin` on PATH)
**Verified By:** antigravity
**Verified At:** 2026-09-16T12:50:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E07-T01-R01, R04, S04 | SQL review + `TestMigrationSQLContent` (BIGINT/NUMERIC(38,0), immutability + balance triggers, no float money) | PASS | `migration/versions/000001_*.sql`, `migration_test.go` |
| E07-T01-R02, S01 | Live 2026-09-16 (`TestMigrationsUpDown`: up to v4, down removes latest, up again) + numbering/embed unit | PASS | `migration/migrate.go`, `embed.go`, `migrate.sh` |
| E07-T01-R03, R06, S02, S03, S05–S07 | Live 2026-09-16: balanced commit, unbalanced reject (nothing persisted), unknown/frozen account rejection, seq monotonicity, cursor paging; unit: balance math (Credit-normal vs Debit-normal classes via `AvailableMinorForClass`), lock order, tenant-closed guards | PASS | `postgres/{db,posting/tx}`, `repositories/*`, `test/integration/persistence/` |
| E07-T01-R05 | Code review: no mutable balance column; checkpoints rebuildable reference (tenant-scoped for RLS); ADR-011 stays Proposed | PASS | `models/ledger.go` (CheckpointModel), `000001` SQL |
| All (traceability) | validator + `TestCodecSeam` (JSON only via `pkg/jsonparser`) | OK / PASS | structural checks passed |

## Commands

```text
$ go build ./...                                   # exit 0
$ go vet ./internal/infrastructure/...             # exit 0
$ go test ./internal/infrastructure/database/... -count=1   # ok
$ go test -race ./internal/infrastructure/... -count=1      # ok, zero races
$ make lint                                        # 0 issues
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes  # OK: structural checks passed
```

## Deviations and Residual Risk

- Live proof recorded 2026-09-16 vs PostgreSQL 18.6 with Docker daemon active.
- Revisions implemented: accounting semantics in `AvailableMinor` and `AvailableMinorForClass` honoring normal journal sides per `docs/ledger-core.md` (Liability/Equity/Revenue computed as Credit - Debit - Holds; Asset/Expense as Debit - Credit - Holds).
- Unit tests refactored to strict table-driven pattern (`AGENTS.md`) with zero out-of-table single-use variables.
- ADR-011 remains Proposed: interim checkpoint posture only; owner benchmark review still required before any mutable projection.
- New module `gorm.io/driver/postgres v1.6.0` (pgx v5.5.4 → v5.6.0 transitively) recorded in E07-T09 evidence.
