# Verification Evidence: E07-T09

**Task:** E07-T09
**Commit:** branch `feat/E07-persistence` from `3d794105864c4e91c6efd990feb2adc57380e029` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain (`$HOME/.pixi/bin` on PATH)
**Verified By:** muse-spark
**Verified At:** 2026-09-16T05:00:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E07-T09-R01, R02, S01 | `TestHarnessSmoke` live 2026-09-16 (postgres 18.6 + valkey 9.0.6 + nats 2.14.6 boot/terminate, 45s incl. pulls) | PASS | `test/testcontainers/{postgres,valkey,nats,services}.go`, `harness_test.go` |
| E07-T09-R02 | Code review: pinned image constants (`postgres:18.6`, `valkey/valkey:9.0.6`, `nats:2.14.6`, unleash/minio/maildev) + bounded `StartupTimeout` (120s local / 60s CI) | PASS | `test/testcontainers/*.go` |
| E07-T09-R03, S02, S04 | `TestTenantFixtureDeterministic`, `TestAccountFixturesChart`, `TestPostingFixturesBalanced`, `TestWorkflowAndStatementFixtures` (determinism + balance) | PASS | `test/fixtures/*.go`, `fixtures_test.go` |
| E07-T09-R04, S03 | `TestHarnessProfiles` + `SkipIfNoDocker` daemon probe (`docker info`, 10s bound) | PASS | `test/testcontainers/harness.go` |
| E07-T09-R05 | Code review: helpers return handles only, no feature assertions | PASS | `test/testcontainers/*.go` |
| All (traceability) | `python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes` | OK | structural checks passed |

## Commands

```text
$ go build ./...                                                        # exit 0
$ go vet ./test/testcontainers/... ./test/fixtures/...                  # exit 0
$ go test ./test/fixtures/... ./test/testcontainers/... -count=1        # ok (smoke skips: no daemon)
$ make lint                                                             # 0 issues (recorded at epic close)
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes  # OK
```

## Deviations and Residual Risk

- New modules `gorm.io/driver/postgres v1.6.0` + `testcontainers-go v0.38.0` added to `go.mod` (pgx v5.5.4 → v5.6.0 transitively). Required by E07 adapters; versions recorded here.
- Postgres readiness uses SQL-level `wait.ForSQL` (TCP-accept races first queries on slow engines — found live 2026-09-16).
- MinIO/Maildev/Unleash image tags verified at pull time in CI, not here.
