# Verification Evidence: E07.1-T02

**Task:** E07.1-T02
**Commit:** branch `feat/E07.1-distributed-persistence` from `0aedc3838e85aa0948e547070ede9df5e7636225` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Docker Compose v5.3.1, kubectl v1.34.3 (client only)
**Verified By:** muse-spark
**Verified At:** 2026-09-17T09:50:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E07.1-T02-R01 (3-node etcd) | `docker compose config --services` lists etcd1/2/3 with Raft cluster flags, volumes, healthchecks | PASS | `docker-compose.citus-patroni.yml` |
| E07.1-T02-R02 (coordinator 1+1 via Patroni, sync) | Config shows coordinator + standby, same scope, `synchronous_mode: true`, `synchronous_node_count: 1`, `synchronous_commit: on` | PASS | `citus-coordinator.yml`, compose file |
| E07.1-T02-R03 (2 worker shards 1+1) | worker1/worker2 scopes each with primary + standby services | PASS | `citus-worker.yml`, compose file |
| E07.1-T02-R04 (etcd DCS, Citus bootstrap, REST health) | Patroni yamls parse; etcd3 hosts point at etcd1..3; `shared_preload_libraries: citus`; healthcheck hits `:8008/health`; supervisor ships in `Dockerfile` (citus base + patroni[etcd3] pip) | PASS | `deployments/patroni/*` |
| E07.1-T02-R05 (traffic via coordinator primary) | Only coordinator publishes 5432; standbys/workers expose distinct or internal ports; app URL `citus-coordinator:5432` | PASS | compose `ports` |
| E07.1-T02-S01 (config validates) | `docker compose -f ... config` | PASS | exit 0, 10 services |
| E07.1-T02-S02 (healthy sweep) | Docker-gated: `up -d --build` + healthcheck sweep (CI with daemon; image build pulls citus 14 + pip patroni) | DEFERRED → CI | compose healthchecks defined per tier |
| E07.1-T02-S03 (pg_dist_node shows workers) | Docker-gated: `citus-init` one-shot runs `citus_add_node` for both workers and selects `pg_dist_node` | DEFERRED → CI | `citus-init` service |

## Commands

```text
$ docker compose -f deployments/docker/docker-compose.citus-patroni.yml config --services
etcd1 etcd2 etcd3 citus-coordinator citus-coordinator-standby citus-worker1 citus-worker1-standby citus-worker2 citus-worker2-standby citus-init
(exit 0)

$ go run /tmp/opencode/yamlcheck/main.go deployments/patroni/citus-coordinator.yml deployments/patroni/citus-worker.yml
OK: deployments/patroni/citus-coordinator.yml
OK: deployments/patroni/citus-worker.yml
```

## Deviations and Residual Risk

- Audit 2026-09-17 found `bin_dir: /usr/lib/postgresql/14/bin` while the
  `citusdata/citus:14.0` image ships PostgreSQL 18.1 only (`ls
  /usr/lib/postgresql/` → `18`). Fixed to `/usr/lib/postgresql/18/bin`;
  without it Patroni cannot find server binaries and no tier bootstraps.
- Patroni passwords in the tier yamls are dev-only compose placeholders
  (same posture as `config/config.yaml` dev defaults); each file documents
  the `PATRONI_*` env override path, production secrets come from OpenBao.

- Packet amended before implementation (per SDD §3.3): `deployments/patroni/Dockerfile` added to owned surface — no public image bundles Citus 14.0 + Patroni v4.1.5; the profile builds it from the Citus Debian base.
- S02/S03 live-container runs deferred to CI (compose build pulls ~1GB Citus base + pip install; fully defined but not executed in this session). Static validation (config + YAML parse + service inventory) recorded here.
- Standby/worker-standby nodes reuse their tier's patroni.yml with `PATRONI_SCOPE/NAME/connect-address` env overrides — one file per tier, no config drift.
- `post_init` scripts intentionally omitted: extension creation + `citus_add_node` live in the retrying `citus-init` service instead of Patroni bootstrap hooks.
