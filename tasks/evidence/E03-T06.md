# Verification Evidence: E03-T06

**Task:** E03-T06
**Commit:** branch `feat/E03-money-movement` from `57de5c86cbc31bb73d69e604a4b93d3783b7285a` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1 (`/tmp/opencode/go-sdk/go/bin`)
**Verified By:** opencode-agent (opencode / Muse Spark)
**Verified At:** 2026-09-14T07:25:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E03-T06-R01, S01 | `TestDispositionFor` (spots + full catalog) + `TestReturnCodes` (pinned) | PASS | `payment_service_test.go`, `valueobject/return_code.go` |
| E03-T06-R02, S02 | `TestValidateRefundToOriginalMethod` (all six rails) | PASS | `service/payment_service.go` |
| E03-T06-R03, S03 | `TestAddLink` + `TestPaymentLinkValidate` | PASS | `entity/payment_link.go` |
| E03-T06-R04, S04 | `TestSettlementBatchValidate` + `TestDuplicateDelivery` | PASS | `entity/settlement_batch.go`, `provider_object_link.go` |
| E03-T06-R05, S05 | `TestResolveTimeout` + `TestRequireStatusLookup` | PASS | `service/payment_service.go` |
| E03-T06-R06 | `TestTransitionPayment` + `TestParsePaymentStatus` + `TestCanTransitionPayment` (64-cell matrix) + `TestMethodCapabilities` | PASS | `valueobject/payment_status.go`, `payment_method.go` |
| E03-T06-R07 | guard test | PASS | shell output |
| All | `go test ./internal/domain/... -count=3` | exit 0 | shell output |

## Commands

```text
$ go test ./internal/domain/... -count=1
PASS all 7 packages
```

## Deviations and Residual Risk

- Provider HTTP adapters (E10) and SQL schema (E07) deferred; retention shapes defined here as the contract.

## Follow-up audit (2026-09-14, same owner/session, claim reopened per SDD §6)

- Dead `PaymentOutcomeUnknown` workflow state removed (provider timeouts stay at the service-level `OUTCOME_UNKNOWN` resolution per ledger-core §7); `TransitionPayment` now covered by a full 8×8 matrix test (was 0% covered); `AUTO`/`MANUAL` aliases removed; `ReturnCodes()` sorted via stdlib `slices`/`maps`; method capabilities converted to a data registry; settlement batches require a coverage window and validated item statuses.
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 94.2%; `go vet`/`gofmt` clean; `check-tasks.py --format --graph --sdd` OK.

## Testify migration (2026-09-14, same owner/session, claim reopened per SDD §6)

- Test-only change: `payment_service_test.go` converted to `testify require/assert` with `t.Run` tables (reversibility matrix over all six rails, outcome/delivery subtests).
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 95.2%; `go vet`/`gofmt` clean.

## Uniform-table exemplar (2026-09-14, same owner/session, claim reopened per SDD §6)

- Test-only change: shared `TestParseAndTransitionTables` exemplar (covers payment/method vocab, spot matrix, registries); no T06 production change.
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 95.2%; `go vet`/`gofmt` clean.

## Exact-shape rewrite + ctx revert (2026-09-14, same owner/session, claim reopened per SDD §6)

- Reverted the mistaken `_ context.Context` threading from all T06 production code; signatures are ctx-free again. Tests rewritten to the exact shape (hardcoded 22-row disposition table instead of the self-oracle loop, single list/loop/`t.Run` per function).
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 96.6%; `go vet`/`gofmt` clean.

## Normative-policy compliance (2026-09-14, same owner/session, claim reopened per SDD §6)

- All T06 subtests (service + `payment_method`/`payment_status`/`return_code`/`payment_link`/`settlement_batch`/`provider_object_link` suites) now run parallel (`tc := tc; t.Parallel()`) — `-race` clean.
- Mandatory protocol: `gofmt -s` clean, `make lint` 0 issues, `CGO_ENABLED=1 CC=clang go test -race ./internal/domain/... -count=1` green, `gate-check.sh G2` PASSED, `check-tasks.py --format --graph --sdd --specs --events --codes` OK.
