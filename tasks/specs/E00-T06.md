# Task Specification: E00-T06 — Seed the Docs Skeleton

**Task:** E00-T06  
**Spec Status:** ready  
**Author:** opencode-agent  
**Reviewer:** opencode-agent (self-review; low-risk docs task per `tasks/SDD.md §3.1`)  
**Last Updated:** 2026-09-13

## Objective

Land directory index files now so code tasks (E01+) can link ADRs and layer
docs as they go; content itself is filled by E18.

## Scope

### In Scope

- `docs/architecture/README.md` (ADR index table + how-to-add note) and
  `docs/{domain,application,infrastructure,api,development}/README.md`
  (one-paragraph scope each, copied from `SPEC.md §3`).

### Out of Scope

- Layer content, ADRs for future decisions, API publishing (all E18).

## Normative Inputs

- `tasks/epics/E00-foundation.md#E00-T06` for the file set.
- `SPEC.md §3` (architecture layers) as the scope source.
- `SPEC.md §4` (layout) for the directory set.
- `tasks/SDD.md` for evidence and completion rules.

## Requirements

| ID | Requirement | Priority |
|----|-------------|----------|
| E00-T06-R01 | All six README files MUST exist. | must |
| E00-T06-R02 | The ADR index MUST have `ID \| Title \| Status` columns, list the accepted/proposed ADRs known at bootstrap, and explain how to add one. | must |
| E00-T06-R03 | Each layer README MUST state its scope in one paragraph from `SPEC.md §3` and link back to `SPEC.md §3`. | must |

## Executable Scenarios

| ID | Given | When | Then |
|----|-------|------|------|
| E00-T06-S01 | The docs tree | The six paths are checked | All exist. |
| E00-T06-S02 | The ADR index | It is inspected | Columns present; known ADRs listed with status; add-note present. |
| E00-T06-S03 | Each layer README | It is inspected | SPEC §3 scope paragraph + backlink present. |

## Interfaces and Data

Markdown links are the interface: READMEs link to `SPEC.md §3`; the ADR index
links to `./ADR-xxx-*.md` files. No code interfaces change.

## Invariants and Failure Semantics

- No invented ADRs: only ADRs already present under `docs/architecture/` plus
  the known pending ADR-011 may be listed.
- Existing layer docs (`go-conventions.md`, `repository-governance.md`) are
  preserved untouched.

## Change Surface

**Owns:** the six README files (replaces those dirs' `.gitkeep` files).  
**Coordinates:** E18 (fills content later).  
**Must Not Touch:** existing docs content, task statuses other than E00-T06,
Git remotes.

## Verification Plan

| Requirement/Scenario | Proof | Command | Expected Evidence |
|----------------------|-------|---------|-------------------|
| E00-T06-R01, S01 | file inventory | `ls` the six paths | all exist |
| E00-T06-R02, S02 | content inspection | `grep` columns/ADRs/add-note | all match, no invented ADRs |
| E00-T06-R03, S03 | content inspection | `grep` scope + SPEC backlink | 5/5 match |
| All | repo gates | `python3 tasks/scripts/check-tasks.py --format --graph --sdd` (+ `--docs` link check) | exit 0 |

## Acceptance Mapping

- [x] Every requirement and scenario appears in the verification plan.
- [x] Backlog acceptance criteria are represented without weakening them.
- [x] Relevant gate checks are named: focused task checks and G1.

## Open Questions

None.

## Approval

**Decision:** approved  
**Approved By:** opencode-agent (self-review recorded per `tasks/SDD.md §3.1`; low-risk docs)  
**Date:** 2026-09-13  
**Notes:** None.
