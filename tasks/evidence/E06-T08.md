# Verification Evidence: E06-T08

**Task:** E06-T08
**Commit:** branch `feat/E06-application` from `a23e32e70cca5e32cb6096d76f33c47b82c6f1ec` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain (`$HOME/.pixi/bin` on PATH)
**Verified By:** muse-spark
**Verified At:** 2026-09-15T13:00:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E06-T08-R01, S01 | `gate-check.sh G3` (`-race -count=3`, coverage gate) | PASSED at 86.6% | `tasks/scripts/gate-check.sh` |
| E06-T08-R02, S02 | `check-tasks.py --handlers --ports` | GREEN (11 groups; adapter ports) | validator output |
| E06-T08-R03 | `go test ./internal/application/workflow/ -race -count=3` (crash-resume + §10 matrix) | PASS | `workflow/*_test.go` |
| E06-T08-R04 | Full application suite `-race -count=3` (per-package below) | PASS | `internal/application/...` |
| All | `make lint` (0 issues) + full validator | PASS/OK | — |

## Commands

```text
$ ./tasks/scripts/gate-check.sh G3
# ok command 86.6%+ → Gate G3: PASSED (application 86.6%)
# per-package: command 81.7%+, dto 100%, middleware 94.3%, port [no statements],
# query 92.3% (gate run), service 88%, workflow 88.9%
$ go test ./internal/application/... -race -count=3   # all ok
$ gofmt -s -l .                                       # empty
$ make lint                                           # 0 issues (exit 0)
$ go test -race ./... -count=1                        # all packages ok, zero races
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes  # OK
$ python3 tasks/scripts/check-tasks.py --handlers --ports                               # green
```

## Deviations and Residual Risk

- Coverage-gap tests (`coverage_*_test.go`) are the T08 deliverable: corrupt-replay matrices, read/delegate sweeps, validation envelopes, overflow/bound edges, duplicate-cancel replays (which fixed two real ordering bugs: reserve-before-state-check in both cancels).
- G3 recorded completed here with evidence; G2 status left to owner review (pre-existing, cross-epic).
- Per-package floor: command 81%+ is the tail; DTOs at 100%; ports carry no statements by design.
