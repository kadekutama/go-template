# Verification Evidence: E07-T06

**Task:** E07-T06
**Commit:** branch `feat/E07-persistence` from `3d794105864c4e91c6efd990feb2adc57380e029` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain
**Verified By:** antigravity
**Verified At:** 2026-09-16T12:50:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E07-T06-R01, S01 | Integration (Testcontainers Docker, live PostgreSQL 18.6) — migrations up/down/up, ledger/account CRUD, posting atomicity, entry monotonic sequences, search, durable idempotency, adversarial RLS, concurrent spend serialization | PASS | `test/integration/persistence/persistence_test.go` |
| E07-T06-R02, S02 | `TestModelBasedNoOverspend` (10k seeded sequences, zero violations) — always runs | PASS | `model_test.go` |
| E07-T06-R03, S03 | `TestFaultMatrix` (5 rows map to safe outcomes: crash before/after commit, publish ACK loss dedupe, inbox commit failure retry, provider timeout outcome unknown) | PASS | `fault_test.go` |
| E07-T06-R04, S04 | Skip path (`SkipIfNoDocker` daemon probe; `TESTCONTAINERS_SKIP=1` forces skip) | PASS | harness `SkipIfNoDocker` |
| All (traceability) | validator + `gate-check.sh G4` | OK / PASSED | structural checks + `--migrations` 4 versions OK |

## Commands

```text
$ go test -v -race -timeout 180s ./test/integration/persistence/...               # ok live vs PostgreSQL 18.6
$ go test -v -race -run 'TestModelBased|TestFaultMatrix' ./test/integration/persistence/... # pass
$ make test-race                                                                  # all packages green, zero races
$ make lint                                                                       # 0 issues
$ ./tasks/scripts/gate-check.sh G4                                                # Gate G4: PASSED
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes # OK: structural checks passed
```

## Deviations and Residual Risk

- Live proof recorded 2026-09-16 against PostgreSQL 18.6 container.
- Revisions implemented:
  - `TestMigrationsUpDown`: refactored to 3-case table verifying initial up to v4, rollback down to v3, and re-up to v4 cleanly.
  - `TestLedgerCRUD`: refactored to 3-case table verifying creation/lookup, cross-tenant isolation, and conflict rejection on duplicate ledger IDs.
  - `TestEntrySequencesMonotonic`: refactored to 3-case table verifying 2-posting monotonicity for s-1, 3-posting monotonicity for s-2, and independent sequence start at 1 for s-3.
  - `TestPostingSearchEndToEnd`: refactored to 9-case table testing matching operation, alternate operation, miss operation, single-account query, shared-account query, wildcard search, tenant isolation, missing tenant ID validation, and unsupported amount bounds.
  - `TestPostgresSeedEndToEnd`: added 3-case table verifying initial dev seed application (1 tenant, 1 ledger, 9 accounts, 4 postings, 8 entries, valid JSONB settings), idempotent second run without duplicate conflict errors, and nil DB handling.
  - `TestDurableIdempotencyPostgres`: updated to invoke `tx.Idempotency().Reserve(ctx, port.IdempotencyRecord{...})` and `tx.Idempotency().Complete(ctx, key, payload)`, with strict assertions on `outcome.Replay` and `IDEMPOTENCY_CONFLICT`.
  - `TestDirectSQLInvariantAttacks`: fixed expected trigger violation error messages to match PostgreSQL schema trigger definitions (`posted facts are immutable`, `debits must equal credits per asset`).
  - `TestAdversarialRLS`: created standard application role `app_user` (NOSUPERUSER NOBYPASSRLS) and executed `SET LOCAL ROLE app_user` to prove non-superuser tenant isolation and insertion policy enforcement; also tested service-role superuser bypass without `app_user` per E07-T02-R02.
  - `TestFaultMatrix`: concrete fault state machine verifications (event deduplication on publish ACK loss, transaction rollback and receipt re-execution on inbox commit failure, and `OUTCOME_UNKNOWN` status transition blocking blind retries on provider timeout).
  - Gate G4 check (`go test -race -count=3 ./test/integration/...`) passes cleanly on live PostgreSQL 18.6 containers.
