# Verification Evidence: E00-T00

**Task:** E00-T00  
**Commit:** b51d5c94f01cdb96c6f5f55a0802da2c786af6fa (base; work on `feat/E00-T00-git-baseline`, packet/CODEOWNERS/governance/claim/handoff in follow-up commits on that branch)  
**Environment:** Linux, git (repo-local identity `go-template agent`), no remote; python3 task validator  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-13T05:08:36Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E00-T00-R01 (boundary choice) | Owner decision 2026-09-13: standalone repo, recorded in packet + governance §1 | pass | `tasks/specs/E00-T00.md`, `docs/development/repository-governance.md` |
| E00-T00-R02 (shared baseline) | `git log --oneline` shows `6eca386` + `b51d5c9`; `git rev-parse --verify HEAD` resolves full SHA | pass | commits `6eca386`, `b51d5c9` |
| E00-T00-R03 (origin/merge policy) | Branch `main` + merge/claim-serialization policy documented in governance §§2–4 | partial | governance doc; origin absent (see Deviations) |
| E00-T00-R04 (CODEOWNERS/governance) | Inspection: `.github/CODEOWNERS` covers claims, migrations, generated contracts, ADRs; governance §5 ownership table | pass | `.github/CODEOWNERS`, `docs/development/repository-governance.md` |
| E00-T00-R05/S02 (fresh checkout + validator) | `check-tasks.py --format --graph --sdd --specs --events --handlers --ports --subjects --docs --codes` | pass | exit 0, 140 tasks / 20 epics / 432 SP |
| E00-T00-S01 (root/HEAD/origin resolve) | `git rev-parse --show-toplevel`, `symbolic-ref HEAD`, `git remote -v` | partial | toplevel + HEAD resolve; origin absent (see Deviations) |
| E00-T00-S03 (claim-race serialization) | Two-harness merge-path exercise | not run | blocked on shared remote (see Deviations) |
| E00-T00-S04 (no sibling paths) | Parent `oc-free-models-test/` contains only `go-template/`; `git show --stat HEAD` adds no sibling paths; `find . -mindepth 2 -name .git` empty | pass | `git show --stat HEAD`, parent listing |

## Commands

```text
$ git rev-parse --show-toplevel        # /home/kadekutama/workspace/oc-free-models-test/go-template
$ git symbolic-ref HEAD                # refs/heads/feat/E00-T00-git-baseline (main exists, unchanged)
$ git rev-parse --verify HEAD          # b51d5c94f01cdb96c6f5f55a0802da2c786af6fa
$ git remote -v                        # (empty — no origin yet)
$ git log --oneline                    # b51d5c9 docs: close spec/plan gaps ... / 6eca386 chore: initial planning baseline
$ ls /home/kadekutama/workspace/oc-free-models-test   # go-template
$ find . -mindepth 2 -name .git -print # (empty)
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs \
    --events --handlers --ports --subjects --docs --codes
# tasks: 140, epics: 20, total SP: 432 — OK: structural checks passed (exit 0)
$ python3 tasks/scripts/check-tasks.py --ready  # 0 tasks (E00-T00 in_progress; rest depend on it)
```

## Deviations and Residual Risk

- **Origin/remote not configured** (E00-T00-R03, E00-T00-S01, E00-T00-S03
  partial/unproven): owner chose "provide a remote URL" but no URL has been
  received yet. Push, PR workflow, and the two-harness claim-race exercise are
  still blocked. Next step: owner supplies URL → `git remote add origin <url>`
  → `git push -u origin main` + push task branch → PR → run S03 exercise →
  complete task and release claim.
- `CODEOWNERS` uses the `@repository-owner` placeholder; replace with the real
  GitHub user/team when origin is created (tracked in handoff).
- Task status is `blocked` (waiting on owner remote URL), claim stays `active`.
  Prior baseline evidence (2026-09-13, root commit `6eca386`) is superseded by
  this file except for its historical record.
