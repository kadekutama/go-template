# Verification Evidence: E01-T03

**Task:** E01-T03  
**Commit:** branch `feat/E01-platform-core` from `559f045a20ab087793e72553de6fcd2928189c2d` (uncommitted per owner instruction; PR review pending)  
**Environment:** Linux x86_64, Go 1.27.1, koanf v2.3.0 + companions (providers/file v1.2.1, providers/env v1.1.0, parsers/yaml v1.1.1, parsers/json v1.0.1), validator v10.22.0  
**Verified By:** opencode-agent (opencode / Muse Spark)  
**Verified At:** 2026-09-14T02:10:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E01-T03-R01/R02, S01/S03/S05 | `TestLoadBaseConfig`, `TestEnvOverrideWins` (`APP_SERVER__PORT=9999`), `TestOverlayWins` | PASS | `loader_test.go` |
| E01-T03-R03, S02 | `TestMissingRequiredListsAllViolations` (names Server/Database/Cache/Auth/NATS) | PASS | same suite |
| E01-T03-R04, S04 | `TestSecretRefFailsClosed` (typed `*SecretRefError`, names E09-T05) + `TestStagingAndProductionFailOnSecretRefs` | PASS | same suite |
| E01-T03-R05, S06 | `TestSchemaAlignsWithStruct` (reflection over koanf tags; required[] check) | PASS | same suite |
| E01-T03-R06 | `make validate-config` | exit 0 | `scripts/config/validate.sh` |
| All | `check-tasks.py --format --graph --sdd` | pass, exit 0 | validator output |

## Commands

```text
$ go test ./internal/infrastructure/config/ -v   # 7/7 PASS
$ make validate-config                            # exit 0
$ go build ./... && go vet ./...                  # exit 0
```

## Deviations and Residual Risk

- `config.local.yaml` ships as `.example` + `.gitignore` entry; loader never
  reads local implicitly (packet records interpretation).
- `Makefile` + `.gitignore` one-line additive coordination (E00 owners; recorded in packet).
- koanf/validator blanks pruned from `internal/shared/deps` (real imports retain them).
- Companion modules recorded in packet In Scope. No secret values committed
  (staging/production carry `{{ secret:… }}` refs that fail closed pre-E09).

## Audit fix addendum (2026-09-17, TAKEOVER muse-spark, owner-directed "fix them all")

| Requirement/Scenario | Proof | Result |
|----------------------|-------|--------|
| E01-T03-R07, S07 (pool knobs) | `TestDatabasePoolSettings` (explicit maps, unset stays zero) | PASS |
| E01-T03-R08, S08 (env slice decode) | `TestCoordinationEnvOverrides` (comma list → 2 endpoints, scalar TTLs) | PASS |

Change surface delta: `DatabaseConfig` pool fields + `PoolSettings()`, schema + 4 YAMLs, koanf `StringToSliceHookFunc(",")` added alongside replicated defaults (duration hook, weak typing). `make validate-config` green; pool consumers land in E11.

## Process-env contract addendum (2026-09-17)

`.env.example` (new, committable; `.env` stays gitignored) documents all 11
`os.Getenv` sites: `APP_FX_*` (2), `APP_COORDINATION__*` (4),
`DATABASE_URL`, `ATLAS_DEV_URL`, `VERSION`, `TESTCONTAINERS_*` (2). Verified
by key cross-check (11/11 present) and clean `source` smoke test. R09/S09.

## Process-env & Configuration Alignment Addendum (2026-09-18, antigravity)

| Requirement/Scenario | Proof | Result |
|----------------------|-------|--------|
| `OBJECT_STORAGE_URL` process-env | Added to `.env.example`, documented with S3/MinIO endpoint format | PASS |
| Harmonized Database DSN | `.env.example` aligned with `config.yaml` (`postgres:postgres@127.0.0.1:5432/app`) and documented Patroni HA port | PASS |
| Multi-environment templates | `.env.staging.example` and `.env.production.example` added with safe secret placeholders | PASS |
| `.gitignore` security hardening | Updated `.gitignore` to broadly ignore `.env*` while preserving `!.env*.example` | PASS |
| `LeaderKeyPrefix` configuration | Added to `CoordinationConfig`, schema, and all 4 YAML overlays; verified in `loader_test.go` | PASS |
| `LogLevel` configuration | Added to `ObservabilityConfig`, schema, and all 4 YAML overlays; verified in `loader_test.go` | PASS |
| Validation & Lint | `make validate-config`, `make lint`, `go test -v ./internal/infrastructure/config/...` (9/9 PASS) | PASS |

