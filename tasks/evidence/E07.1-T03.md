# Verification Evidence: E07.1-T03

**Task:** E07.1-T03
**Commit:** branch `feat/E07.1-distributed-persistence` from `0aedc3838e85aa0948e547070ede9df5e7636225` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, kubectl v1.34.3 (client), Kustomize v5.7.1
**Verified By:** muse-spark
**Verified At:** 2026-09-17T10:00:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E07.1-T03-R01 (coordinator Cluster CR, 3 instances, citus bootstrap, backup) | `kubectl kustomize` renders `Cluster/citus-coordinator` (instances 3, postInitSQL CREATE EXTENSION, backup stanza + retention) | PASS | `citus-coordinator.yaml` |
| E07.1-T03-R02 (2 worker Cluster CRs, backup, registration script) | Rendered `Cluster/citus-worker1|2` (instances 2, backup) + `Job/citus-register-workers` (`citus_add_node` x2, extension everywhere) | PASS | `citus-workers.yaml` |
| E07.1-T03-R03 (Services, secrets, netpols) | 3 NetworkPolicies rendered; secrets only via `secretKeyRef` (backup S3, superuser); Services are CNPG-generated `-rw` endpoints referenced by name | PASS | both yamls (grep: secretKeyRef, NetworkPolicy) |
| E07.1-T03-R04 (kustomization wires resources) | `kubectl kustomize` builds 7 resources from 2 files | PASS | `kustomization.yaml` |
| E07.1-T03-R05, S01 (dry-run validation) | `kubectl apply --dry-run=client -k`: Job + 3 NetworkPolicies created (dry run); Cluster CRs need CNPG CRDs on the target cluster (`ensure CRDs are installed first`) | PASS with noted boundary | kubectl output |
| E07.1-T03-S02 (extension + registration review) | grep: `CREATE EXTENSION IF NOT EXISTS citus` x4, `citus_add_node` present | PASS | grep output |
| E07.1-T03-S03 (backup review) | grep: `backup:` + `retentionPolicy` on all 3 clusters | PASS | grep output |

## Commands

```text
$ kubectl kustomize deployments/k8s/cloudnativepg | grep -E "^(kind:|  name:)"
kind: Job / name: citus-register-workers
kind: NetworkPolicy x3
kind: Cluster / citus-coordinator, citus-worker1, citus-worker2
(exit 0)

$ kubectl apply --dry-run=client -k deployments/k8s/cloudnativepg
job.batch/citus-register-workers created (dry run)
networkpolicy x3 created (dry run)
Cluster x3: resource mapping not found (CNPG CRDs required on target) — expected client-side boundary
```

## Deviations and Residual Risk

- Audit 2026-09-17 found the worker and register-job NetworkPolicies
  selecting `app.kubernetes.io/component: citus-worker`, a label CNPG never
  places on pods (custom Cluster labels require `spec.inheritedMetadata`).
  Fixed to `cnpg.io/cluster` matchExpressions (`citus-worker1`,
  `citus-worker2`); the coordinator policy already used `cnpg.io/cluster`
  and was correct. The Job's own pod labels (set in its pod template) were
  always real pod labels and needed no change.

- `imageName: ghcr.io/cloudnative-pg/postgresql:18.6-citus14` is purpose-built (cloudnative-pg base + Citus 14.0); the build recipe is E17's image pipeline. No plaintext secrets anywhere (Secret references only).
- Cluster CR server-side validation requires a cluster with CNPG v1.30.0 CRDs; client-side kustomize + dry-run for known types is the recorded proof, matching the packet's fallback.
- Live K8s deploy (workers registered, failover) is outside this task's verification plan; T05 covers data-plane behavior on Citus via Testcontainers.

## Review Takeover Addendum (2026-09-17, antigravity)

| Requirement/Scenario | Proof | Result |
|----------------------|-------|--------|
| Kustomize deprecation fix | `kustomization.yaml` updated from deprecated `commonLabels` to modern `labels` block | PASS |
| Clean Kustomize build | `kubectl kustomize deployments/k8s/cloudnativepg/` runs with 0 deprecation warnings | PASS |

Verification commands:
```bash
$ kubectl kustomize deployments/k8s/cloudnativepg/ > /dev/null
# exit code 0, zero warnings
```

## Completion addendum (2026-09-17, TAKEOVER muse-spark from antigravity)

Prior revision (antigravity, recorded in claim): `commonLabels` → modern
`labels:` block with values preserved — reviewed and kept. Completion by
muse-spark: `kubectl kustomize` renders 7 resources with NO deprecation
warning (previously observed with `commonLabels`); `kubectl apply
--dry-run=client` passes Job + 3 NetworkPolicies, Cluster CRs awaiting
target-cluster CNPG CRDs (standing boundary). No further file change needed.
