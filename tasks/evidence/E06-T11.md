# Verification Evidence: E06-T11

**Task:** E06-T11
**Commit:** branch `feat/E06-application` from `a23e32e70cca5e32cb6096d76f33c47b82c6f1ec` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain (`$HOME/.pixi/bin` on PATH)
**Verified By:** muse-spark
**Verified At:** 2026-09-15T11:00:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E06-T11-R01, S01, S02 | `TestDisputeOpenEvidence` (open with deadline/fee; late payment; on-time/late evidence; replay) | PASS | `command/dispute.go`, `dispute_test.go` |
| E06-T11-R02, S03 | `TestDisputeRepresentClose` (second representment exhausted) | PASS | same as above |
| E06-T11-R03, S04, S05 | `TestDisputeRepresentClose` (SoD violation; lost close + closed fact; invalid outcome) | PASS | same as above |
| E06-T11-R04 | Closed-fact assertions (§10 names + outcome/status keys) | PASS | same as above |
| Reads/DTOs | Delegation + mapping tests | PASS | `query/dispute_test.go`, `dto/dispute_test.go` |
| All | `make lint` (0 issues) + `go test -race ./...` (zero races) + full validator | PASS/OK | — |

## Commands

```text
$ gofmt -s -l internal/ test/    # empty
$ goimports -l -w .               # clean
$ make generate-mocks            # exit 0 (DisputeUseCases mock added)
$ make lint                       # 0 issues (exit 0; shared dispute runner for gocyclo ≤ 12)
$ go vet ./...                    # exit 0
$ go test ./internal/application/... -count=3                # pass
$ go test -race ./... -count=1                               # all packages ok, zero races
$ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes  # OK
```

## Deviations and Residual Risk

- Added `port/usecase_dispute.go` (missed in E06-T12's five contexts) + T12 packet amendment note; mocks regenerated.
- Evidence auto-marks OPEN→EVIDENCE_DUE before submitting (both domain transitions, one call).
- Representment stage is workflow bookkeeping around the domain allowance check; evidence/represent stage no webhook (catalog has opened/closed only).
- Dispute path: PR review by repo-owner required before merge (packet approval note).
