# Verification Evidence: E03-T07

**Task:** E03-T07
**Commit:** branch `feat/E03-money-movement` from `57de5c86cbc31bb73d69e604a4b93d3783b7285a` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1 (`/tmp/opencode/go-sdk/go/bin`)
**Verified By:** opencode-agent (opencode / Muse Spark)
**Verified At:** 2026-09-14T07:25:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E03-T07-R01–R06, R08, S01–S05 | `TestOpenDispute`, `TestSubmitEvidence`, `TestMarkEvidenceDue`, `TestRepresentmentAllowed`, `TestCloseDispute`, `TestSealDispute`, `TestParseDisputeStatus`, `TestCanTransitionDispute`, `TestDisputeValidate` | PASS | `internal/domain/service/dispute_auth_test.go`, `internal/domain/entity/dispute_test.go`, `internal/domain/valueobject/dispute_status_test.go`, `dispute_service.go`, `entity/dispute.go`, `valueobject/dispute_status.go` |
| E03-T07-R07 | guard test | PASS | shell output |
| All | `go test ./internal/domain/... -count=3` | exit 0 | shell output |

## Commands

```text
$ go test ./internal/domain/... -count=1
PASS all 7 packages
```

## Deviations and Residual Risk

- Hold creation/persistence and provider filing deferred to E07/E10; hold reference + fee amount recorded here.

## Follow-up audit (2026-09-14, same owner/session, claim reopened per SDD §6)

- Status changes now flow through `CanTransitionDispute` (previously bypassed); `MarkEvidenceDue` (OPEN→EVIDENCE_DUE) and `SealDispute` (decided→CLOSED) added so every state is reachable; `Dispute.Status` typed as `valueobject.DisputeStatus` with validity check; packet R08/S05.
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 94.2%; `go vet`/`gofmt` clean; `check-tasks.py --format --graph --sdd` OK.

## Testify migration (2026-09-14, same owner/session, claim reopened per SDD §6)

- Test-only change: `dispute_auth_test.go` dispute half converted to `testify require/assert` with `t.Run` subtests.
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 95.2%; `go vet`/`gofmt` clean.

## Uniform-table exemplar (2026-09-14, same owner/session, claim reopened per SDD §6)

- Test-only change: shared `TestParseAndTransitionTables` exemplar (covers dispute vocab); no T07 production change.
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 95.2%; `go vet`/`gofmt` clean.

## Exact-shape rewrite + ctx revert (2026-09-14, same owner/session, claim reopened per SDD §6)

- Reverted the mistaken `_ context.Context` threading from all T07 production code; signatures are ctx-free again. Tests rewritten to the exact shape (one function per production function incl. `TestMarkEvidenceDue`/`TestSealDispute`).
- Re-verified: `go test ./internal/domain/... -count=3` exit 0; domain coverage 96.6%; `go vet`/`gofmt` clean.

## Normative-policy compliance (2026-09-14, same owner/session, claim reopened per SDD §6)

- All T07 parent tests (service + `dispute_status`/`dispute` suites) run parallel (`t.Parallel()`) with sequential deterministic subtests (no `tc := tc`, no inner `t.Parallel()`) — `-race` clean.
- Mandatory protocol: `gofmt -s` clean, `make lint` 0 issues, `CGO_ENABLED=1 CC=clang go test -race ./internal/domain/... -count=1` green, `gate-check.sh G2` PASSED, `check-tasks.py --format --graph --sdd --specs --events --codes` OK.
