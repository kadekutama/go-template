# Verification Evidence: E07-T02

**Task:** E07-T02
**Commit:** branch `feat/E07-persistence` from `3d794105864c4e91c6efd990feb2adc57380e029` (uncommitted per owner instruction; PR pending)
**Environment:** Linux x86_64, Go 1.27.1, `CGO_ENABLED=1 CC=clang`, Pixi toolchain
**Verified By:** antigravity
**Verified At:** 2026-09-16T12:50:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E07-T02-R01, R02, S01, S04 | Live 2026-09-16: migration 000004 applies (`FORCE ROW LEVEL SECURITY` on 18 tables); `TestLedgerCRUD` cross-tenant read resolves nothing; `TestAdversarialRLS` verifies tenant isolation for application role `app_user` AND verifies service-role bypass (`E07-T02-R02`); `set_config` scoping on every repository call | PASS | `versions/000004_rls_policies.*.sql`, `postgres/rls/`, `test/integration/persistence/` |
| E07-T02-R03, R04, S02, S03 | `TestWithTenantRejectsBlank`, `TestTenantFromContextFailsClosed`, `TestResolveTenantAgreement`, `TestApplyTenantGuards`, repo tenant-closed tests, `scopeTenant` calling `rls.ResolveTenant(ctx, tenant)` | PASS | `postgres/rls/tenant.go`, `tenant_test.go`, `repositories/scope.go` |
| All (traceability) | validator | OK | structural checks passed |

## Commands

```text
$ go test ./internal/infrastructure/database/postgres/rls/... -count=3   # pass
$ go test -race ./internal/infrastructure/... -count=1                   # zero races
$ go test -v -race -run TestAdversarialRLS ./test/integration/persistence/... # pass
$ make lint                                                             # 0 issues
```

## Deviations and Residual Risk

- Live proof 2026-09-16 (migration applies with `FORCE ROW LEVEL SECURITY`). Confused-deputy fix applied in `repositories/scope.go` to explicitly validate tenant agreement between context and parameter via `rls.ResolveTenant`.
- `TestAdversarialRLS` proves both requirements: (1) application roles (`app_user` without superuser/bypassrls) are strictly bounded by `app.current_tenant`, cross-tenant select returns 0 rows, cross-tenant insert violates RLS policy; and (2) service role bypasses RLS and sees all accounts per E07-T02-R02.
