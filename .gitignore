# Build output
bin/
dist/

# Coverage reports
coverage/
*.coverprofile
*.out

# Logs
*.log

# Local environment overrides (never commit secrets)
.env
.env.*
!.env.example
!.env.*.example

# Air hot-reload local overrides (base .air.toml from E00-T04 is tracked)
.air.toml.local
tmp/

# OS / editor noise
.DS_Store
.idea/
.vscode/*

# Go workspace leftovers
vendor/

# Local config overrides (untracked; copy config/config.local.yaml.example, E01-T03)
config/config.local.yaml

