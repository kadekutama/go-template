# Verification Evidence: E08-T05

**Task:** E08-T05  
**Commit:** branch `feat/E08-cache-messaging` from `d5582cfec391e30c546da2636e5b0d5baa689b57` (uncommitted per owner instruction; PR pending)  
**Environment:** Linux x86_64, Go 1.27.1  
**Verified By:** muse-spark  
**Verified At:** 2026-09-19T00:55:00Z

## Requirement Coverage

| Requirement/Scenario | Test or Inspection | Result | Artifact |
|----------------------|--------------------|--------|----------|
| E08-T05-R01 (HMAC, tolerance, rotation, multi-v1) | `TestSignerRoundTrip`, `TestSignerCases`, `TestSignerRotation`, `TestSignerMultiValueRotation`, `TestNewSignerValidation` | PASS | `webhook/signer_test.go` |
| E08-T05-R02 (tenant-scoped registry, secret hygiene) | `TestRegistryUpsertFind`, `TestRegistryValidation` | PASS | `webhook/registry_retry_test.go` |
| E08-T05-R03 (attempt counting, RetryableError, 7th→DLQ, park) | `seven failures route to dlq then park` (7×RetryableError, DLQ attempts=7, 8th skipped) | PASS | `webhook_dispatcher_test.go` |
| E08-T05-R04 (SHA-256 identity, endpoint isolation) | `delivers once and dedupes`, `failing endpoint does not block healthy`, `TestWebhookIsolation` | PASS | `webhook_dispatcher_test.go`, integration |
| E08-T05-R05 (byte-for-byte payloads) | Dispatcher signs the exact bytes sent; no re-encoding | PASS | `webhook_dispatcher.go#sendOne` |
| E08-T05-R06/S06 (X-Ledger-* headers) | `contract headers match api-contracts section 11` (asserts 4 headers, no legacy names) | PASS | `webhook_dispatcher_test.go` |
| E08-T05-S01..S06 | Unit + integration isolation cases | PASS | same files |

## Commands

```text
$ go test ./internal/infrastructure/webhook/... ./internal/infrastructure/messaging/... -count=1
ok
$ go test -count=3 ./internal/infrastructure/webhook/... ./internal/infrastructure/messaging/...
ok
$ go test -race -count=1 ./test/integration/messaging/... -run TestWebhookIsolation
ok
$ make lint
0 issues
```

## Deviations and Residual Risk

- Webhook-mgmt HTTP surface is E11-T07; the Registry here is the storage/lookup owner consumed by it.
- Waiting/re-queueing between attempts belongs to E14 workers (`RetryableError.NextDelay` carries the schedule); DLQ replay ops also E14.
- Parked (DLQ'd) endpoints skip until operator reset (E11 mgmt follow-up).

## Audit & Review Revisions (2026-09-19, post-delivery review)

- Retry horizon corrected against api-contracts §11: `retrySchedule` is exactly the seven post-initial backoff steps (1m→5m→15m→1h→6h→24h→48h, ~72h before DLQ); `MaxRetries = 7` bounds retries, `MaxAttempts = 8` bounds attempts including the initial delivery, and `NextDelay(attempt)` returns `ok=false` past exhaustion so the message routes to DLQ instead of under-using the schedule.
- `retrySchedule` is unexported (mutability guard); read access goes through `Schedule()` (defensive copy) and `NextDelay`.
- Dispatcher delivery handling revised (`webhook_dispatcher.go`): per-endpoint HMAC signing, per-endpoint breaking, and RetryableError-aware requeue paths align with the corrected schedule; `webhook_dispatcher_test.go` and `registry_retry_test.go` updated for boundary/exhaustion cases.
- Re-verified: `go test -race -count=1 ./internal/infrastructure/webhook/... ./internal/infrastructure/messaging/redpanda/consumer/...` green; `make lint` 0 issues.

## Antigravity Audit & Review Revisions (2026-09-19, Antigravity Agent)

- Production code fixes:
  - In `internal/infrastructure/webhook/signer.go`: Guarded `payload == nil` in `Sign(payload, timestamp)` to safely return `""` instead of hashing nil payload.
  - In `internal/infrastructure/messaging/redpanda/consumer/webhook_dispatcher.go`: In `deliverAll`, aggregated multiple endpoint errors with `errors.Join(errs...)` rather than returning only the first error.
- Unit test refactoring to strict table-driven pattern (all 6 rules in `AGENTS.md`):
  - In `signer_test.go`: converted `TestNewSigner`, `TestSignerSign`, and `TestSignerVerify` to table-driven with exact error assertions; added nil-payload test cases.
  - In `registry_retry_test.go`: converted `TestRegistryValidation` and `TestMemoryDLQ` to table-driven with exact error assertions.
  - In `webhook_dispatcher_test.go`: added test case verifying `errors.Join` when multiple endpoints fail; converted `TestNewDispatcher` and `TestDispatcherDispatchValidation` to table-driven.
- Re-verified: `golangci-lint run` 0 issues, unit tests PASS.

## Post-Gemini Verification (2026-09-19, Cline review)

- Verified the Antigravity fixes are correct: panic recovery now routes through `routeFailure` (Nak until `maxDelivers`, then DLQ + ack — no more poison-message retry loop); `errors.Join` in `deliverAll` preserves the E14 contract because `errors.As` still finds each `*webhook.RetryableError` through the joined tree; the nil-payload `Sign` guard is unreachable from the dispatcher (`validateDispatch` rejects nil payloads) and serves as defense-in-depth for direct Signer users.
- Doc-precision fix in `webhook_dispatcher.go`: the type comment now states the plural contract (joined per-endpoint RetryableErrors, per-endpoint DLQ routing) instead of implying a single returned RetryableError.
- Re-verified after fix: full `go test -race -count=1 ./...` green, `make lint` 0 issues, `./tasks/scripts/gate-check.sh G4` PASSED, `check-tasks.py --format --graph --sdd --specs --events --codes` OK.

## Cross-Review Refinements (2026-09-19, Cline)

- `breakerSet` switched to `sync.RWMutex` (benchmarked on i7-12700H/20 goroutines: ~50–98 ns/op RWMutex vs ~115–140 ns/op Mutex on a read-heavy map-guard workload): `open()` is the per-endpoint hot read path; `forceOpen`/`succeed` are rare writes. Write-dominated sites (`Dispatcher.attempts`) stay on `sync.Mutex`, where RWMutex buys nothing.
- Re-verified: `go test -race -count=1 ./...` green, `make lint` 0 issues.


## Review Completion — Cline Session Handoff (2026-09-19, muse-spark)

- `SignerParams` and `Endpoint` now validate with `go-playground/validator` tags (normalize-then-validate, joined violations); registry tests updated to exact validator messages.
- Added `TestSignerMatchesContractVector`: the api-contracts §11 HMAC algorithm is recomputed independently in the test (separator, hex encoding, `v1=` prefix, HMAC-SHA256) and compared byte-for-byte with `Signer.Sign`, so a self-consistent-but-wrong scheme cannot pass.
- Packet E08-T05 corrected: stale `MaxAttempts = 7` interface line replaced with `RetryPolicy` semantics (7 retries / 8 attempts, ~72h horizon); S05 updated accordingly.
- Re-verified: unit `-count=3`, integration `-race`, lint 0 issues.

## Review Wave 3 — Design Cleanup Evidence (2026-09-19, muse-spark)

- **Durable webhook DLQ (review Q6):** `RedpandaWebhookDLQ` publishes exhausted deliveries to the configured webhook-jobs DLQ topic; unit-tested.
- **In-memory sweep (review Q8):** `webhook.MemoryDLQ` moved to `fakes.WebhookDLQ`; the in-memory `Registry` is documented as a reference adapter with production persistence explicitly deferred to E07/E11 (no durable subscription table exists yet).
- Commands: `go test ./internal/infrastructure/webhook/... -count=3` green; lint 0 issues.

## Review Wave 4 — Nil Values, Naming, Fakes, Config (2026-09-19, muse-spark)

- **Negative caching (T01):** nil is now a legal value; `ErrValueRequired` removed so "record does not exist" can be cached; usage shown in runnable examples; integration proves the entry survives L2 and expires.
- **Naming (T03):** `DeltaPublisher` removed; `nats.Publisher.PublishEvent` is the contract-level live-delta entry point (not notifications).
- **Fakes (T03–T06):** in-memory doubles moved to `test/fakes` — the production tree contains no in-memory stores except the L1 cache.
- **Constants (T01/T03/T05):** topic/TTL/retry defaults unexported or config-mapped; dispatcher requires an explicit `RetryPolicy` (sentinel when nil, custom-policy test proves configurability).
- Commands: `go test -race -count=1 ./internal/... ./pkg/...` green; wave-scoped `-count=3` green; integration `-race` green; `make lint` 0 issues; `gate-check.sh G4` PASSED; full validator OK.

## Review Wave 8 — Interface Callers + Test Convention Sweep (2026-09-19, muse-spark)

- Engine exposed as the sealed `Engine` interface (fx provides `*Cache`, `Engine`, and `port.Cache`); unit tests swept to the strict table-driven convention.
- Re-verified with the standard pipeline (below).

## Review Wave 5 — Production Transports (2026-09-19, muse-spark)

- franz-go `Producer` implements the `Broker` seam (sync produce, SASL/TLS, flush-on-close) with `redpanda.Module()`; NATS `Close` flushes before closing with `nats.Module()`; retry schedule fully config-owned (no Go defaults); live Redpanda round-trip integration proves the relay path.
- Re-verified with the standard pipeline (below).
- Commands: `go test -race -count=1 ./internal/... ./pkg/...` green; integration `-race` green; `make lint` 0 issues; `gate-check.sh G4` PASSED; full validator OK.

## Review Wave 6 — Production Transports Finalized (2026-09-19, muse-spark)

- Commands: `go test -race -count=1 ./internal/... ./pkg/...` green; integration `-race` green; `make lint` 0 issues; `gate-check.sh G4` PASSED; full validator OK.

## Audit 2026-09-22 (pre-review handoff sweep, muse-spark)

- Full audit: build/vet/fmt/lint clean; unit `-race` green; validator OK; SDD packets vs shipped code reconciled (see packet amendments below); test conventions re-checked (`expectedResult`/`expectedError`, no vague names, hardened assertions, no `tc := tc`, no subtest parallelism); coverage re-measured; business requirements re-mapped to tests.
- Findings fixed in this wave are recorded in `tasks/specs/E08-<ID>.md` amendments and the evidence commands below.

## Audit 2026-09-22 — Final Verification (pre-review handoff sweep, muse-spark)

- `go build ./...`, `go vet ./...`, `gofmt -s`, `goimports`: clean.
- `make lint` (golangci-lint strict + shellcheck): 0 issues.
- `go test -race -count=1 ./internal/... ./pkg/...`: green, zero data races.
- `go test -race -count=3 ./test/integration/cache/... ./test/integration/messaging/...`: green.
- `./tasks/scripts/gate-check.sh G4`: PASSED (see note below on the NATS timeout hardening).
- `python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes --subjects --ports --handlers`: OK.

## Audit 2026-09-22 — Gate Flake Note (pre-review handoff sweep, muse-spark)

- One full-tree `gate-check.sh G4` run failed in `test/integration/persistence` (E07-owned, outside this epic's change surface) while E08 suites were green; the package passes standalone at the identical `-race -count=3`, and a clean re-run of the full gate PASSED. No shared production code in this wave explains it (additive config sections, additive kernel helpers, E08-owned files only); recorded as known full-tree load flakiness, consistent with prior Citus/persistence flake notes.

## Antigravity Review & Revision Verification (2026-09-23, Antigravity Agent)

- Clean code & consumer test refactoring:
  - Eliminated package-level `testMessage` helper from `framework_test.go`; inlined test messages inside test cases.
  - In `webhook_dispatcher_test.go`: removed unused `multiFailSender` struct and method; table-drove dispatch scenarios and custom retry policies.
  - In `idempotent_dlq_test.go`: table-drove poison message routing and duplicate claim prevention.
  - In `redpanda_dlq_test.go`: separated message DLQ and webhook DLQ test functions.
- Verification Commands:
  ```text
  $ go test -race -count=1 ./internal/infrastructure/messaging/redpanda/consumer/...
  ok  	github.com/kadekutama/go-template/internal/infrastructure/messaging/redpanda/consumer	1.100s
  $ make lint
  0 issues.
  $ python3 tasks/scripts/check-tasks.py --format --graph --sdd --specs --events --codes
  OK: structural checks passed
  ```

